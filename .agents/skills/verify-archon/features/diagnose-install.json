{
  "version": 1,
  "id": "diagnose-install",
  "description": "The isolated local installation is healthy.",
  "impact_paths": [
    "packages/cli/src/commands/doctor*",
    "packages/server/src/index.ts",
    "packages/server/src/routes/api.ts",
    "packages/core/src/db/adapters/**"
  ],
  "behaviors": [
    {
      "id": "install.health",
      "description": "The isolated local installation is healthy.",
      "priority": "P1",
      "scenarios": ["install.health"]
    }
  ],
  "scenarios": [
    {
      "id": "install.health",
      "runner": {
        "kind": "cli",
        "recipe": "diagnose-install"
      },
      "proof_obligations": ["cli-action", "api-response"]
    }
  ]
}
