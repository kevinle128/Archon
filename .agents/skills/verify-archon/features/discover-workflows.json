{
  "version": 1,
  "id": "discover-workflows",
  "description": "List and validate workflows through CLI and the project HTTP catalog.",
  "impact_paths": [
    "packages/workflows/src/loader*",
    "packages/workflows/src/workflow-discovery*",
    "packages/cli/src/commands/validate*"
  ],
  "behaviors": [
    {
      "id": "workflows.discovery",
      "description": "List and validate workflows through CLI and the project HTTP catalog.",
      "priority": "P1",
      "scenarios": ["workflows.discovery"]
    }
  ],
  "scenarios": [
    {
      "id": "workflows.discovery",
      "runner": {
        "kind": "cli",
        "recipe": "discover-workflows"
      },
      "proof_obligations": ["cli-action", "api-response", "first-party-write"]
    }
  ]
}
