{
  "version": 1,
  "id": "inspect-run",
  "description": "Inspect a real paused run with CLI get/status and HTTP read-back.",
  "impact_paths": ["packages/cli/src/commands/workflow*", "packages/core/src/db/workflows*"],
  "behaviors": [
    {
      "id": "runs.inspection",
      "description": "Inspect a real paused run with CLI get/status and HTTP read-back.",
      "priority": "P1",
      "scenarios": ["runs.inspection"]
    }
  ],
  "scenarios": [
    {
      "id": "runs.inspection",
      "runner": {
        "kind": "cli",
        "recipe": "inspect-run"
      },
      "proof_obligations": ["cli-action", "api-response", "persistence-read-back"]
    }
  ]
}
