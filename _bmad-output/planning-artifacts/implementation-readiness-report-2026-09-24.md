---
stepsCompleted:
  - step-01-document-discovery
  - step-02-prd-analysis
  - step-03-epic-coverage-validation
  - step-04-ux-alignment
  - step-05-epic-quality-review
  - step-06-final-assessment
targetSlug: agent-node-room
assessmentInput: _bmad-output/specs/spec-agent-node-room/SPEC.md
mockupManifest: _bmad-output/planning-artifacts/mockup-manifests/agent-node-room.json
filesIncluded:
  - _bmad-output/specs/spec-agent-node-room/SPEC.md
  - _bmad-output/planning-artifacts/epics-agent-node-room/epics.md
  - _bmad-output/planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09/DESIGN.md
  - _bmad-output/planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09/EXPERIENCE.md
  - _bmad-output/planning-artifacts/architecture/architecture-Archon-readable-agent-transcript-2026-09-12/ARCHITECTURE-SPINE.md
  - _bmad-output/planning-artifacts/architecture/architecture-Archon-live-agent-steering-2026-09-12/ARCHITECTURE-SPINE.md
---

# Implementation Readiness Assessment Report

**Date:** 2026-09-24
**Project:** Archon
**Target:** agent-node-room
**Assessor:** BMAD Implementation Readiness workflow

## Document Discovery

The canonical requirement source is `_bmad-output/specs/spec-agent-node-room/SPEC.md`.
The selected Epic frontmatter names that SPEC, its eight target contract and test files, the two target UX documents, both target Architecture spines, and the approved Claude Design handoff.
The prior readiness reports in the Epic folder are historical reports and are excluded as assessment authority.
Generic root PRDs and other target folders have different lineages and are excluded.
The target selection and selected Epic inputDocuments are explicit in the dispatch and source frontmatter, so no document-selection decision is missing.

| Type              | Selected source                                                                                                                                                      | Discovery result                                                                                      |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Requirements      | `spec-agent-node-room/SPEC.md` plus its target contract files                                                                                                        | One canonical target SPEC; historical source snapshots under `sources/` are excluded.                 |
| UX                | `ux-Archon-agent-node-room-2026-09-09/DESIGN.md` and `EXPERIENCE.md`                                                                                                 | Two complementary target documents; no competing target whole/sharded version.                        |
| Architecture      | Both `architecture-Archon-readable-agent-transcript-2026-09-12/ARCHITECTURE-SPINE.md` and `architecture-Archon-live-agent-steering-2026-09-12/ARCHITECTURE-SPINE.md` | Two complementary capability spines for the same target.                                              |
| Epics and Stories | `epics-agent-node-room/epics.md`                                                                                                                                     | One current target Epic with Stories; older readiness reports are excluded.                           |
| Approved mockup   | `claude-design/design_handoff_node_room_transcript_steering/`                                                                                                        | Manifest source set has four `.dc.html` files and `support.js`; validator passed with current hashes. |

## Frozen Mockup Change Boundary

Current-scope changes are the TODO strip position on both rooms, the full-room shared queue disclosure and truthful accepted-queue label, per-item mid-turn Send now on true soft-inject providers, universal Run N occurrence headers, the sender-name treatment for the row made by accepted per-item Send now during generation, and delivered status after matching message_id confirmation.
The approved HTML conflicts with user decisions on the queue label, that selected row's sender name, and the delivered gate; the static refusal of per-item send applies only to queue-only providers.
Stored operator attribution remains intact.

Manifest source fingerprint: `6a5d71f0dd47812d004a48f7fb05073c6d353e4fd75194b37ab099771419bf1b`.
Mockup scope: `ALL_VISIBLE_PRODUCT_FEATURES_ARE_CURRENT`.
Comparison policy: `MOCKUP_VS_CURRENT_PRODUCT_AND_COMPLETED_EPICS`.
Every approved product inventory ID has one classification: 9 CHANGE_FEATURE and 61 UNCHANGED_CONTEXT.
The outer state, node-kind, and transport controls above the product frame are review scaffolding based on their HTML placement, fixture-only handlers, and the extractor skill's exclusion rule; the in-product Execution selector is classified in the Context Inventory.
The mockup-side fields below were copied from the validated manifest before planning-document mapping.
The manifest phrase “operator rows without sender names” is limited by M008 and the user answer to the transcript row produced when one selected queued item is accepted through per-item `Send now` during generation.
Stored operator attribution remains unchanged, and this assessment does not extend the visual rule to other delivery paths.

## Mockup Behavior Evidence Ledger

### M001 — Console TODO strip placement and disclosure

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: running prompt node.
- Inventory IDs: I007.
- Action key: `None`; status: PROVEN.
- **Visible presentation:** One collapsible TODO strip on the Console node room directly below the transcript and above the steering dock
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`: The TODO strip sits below the transcript and directly above the queue or dock.
- **Precondition:** The selected agent node has todo calls
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`: The selected running agent node has todo calls and a visible current todo item.
- **Action or trigger:** The selected node room renders or the operator toggles the strip
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#showTodoStrip`: The mockup enables the strip in the selected agent room.
- **Target identity:** The selected node todo strip
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`: The strip belongs to the selected implement node.
- **Target cardinality:** One strip for the selected node
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`: One strip appears for the selected node.
- **Timing:** Pinned and visible while the transcript scrolls
  - annotation `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;todo`: The todo checklist is pinned while the transcript scrolls.
- **Effect on active work:** None; expanding the strip changes only its display
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo`: The toggle only changes the strip open state; agent work is not changed.
- **Collection mutation:** None
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo`: The toggle updates local disclosure state and does not alter a queue or transcript collection.
- **Expected result:** The current todo item and progress remain visible; expansion shows the checklist
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`: The collapsed strip shows the current item and 4/6 progress; expansion exposes checklist items.
- **Remaining or next state:** The strip is collapsed or expanded while the node keeps its agent sub-state
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo`: The strip changes between collapsed and expanded while the selected node stays running.
- Classification evidence:
  - implementation `packages/web/src/experiments/console/components/ConsoleNodeRoom.tsx#RoomRegion`: The current room mounts TodoStrip before the transcript scroller, above the transcript.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5`: The completed Story describes the strip as pinned above the transcript; the mockup renders it below.

### M002 — Legacy TODO strip placement and disclosure

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: running prompt node.
- Inventory IDs: I070.
- Action key: `None`; status: PROVEN.
- **Visible presentation:** One collapsible TODO strip on the Legacy node room directly below the transcript and above the steering dock
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`: The TODO strip sits below the transcript and directly above the queue or dock.
- **Precondition:** The selected agent node has todo calls
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`: The selected running agent node has todo calls and a visible current todo item.
- **Action or trigger:** The selected node room renders or the operator toggles the strip
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#showTodoStrip`: The mockup enables the strip in the selected agent room.
- **Target identity:** The selected node todo strip
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`: The strip belongs to the selected implement node.
- **Target cardinality:** One strip for the selected node
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`: One strip appears for the selected node.
- **Timing:** Pinned and visible while the transcript scrolls
  - annotation `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;todo`: The todo checklist is pinned while the transcript scrolls.
- **Effect on active work:** None; expanding the strip changes only its display
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo`: The toggle only changes the strip open state; agent work is not changed.
- **Collection mutation:** None
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo`: The toggle updates local disclosure state and does not alter a queue or transcript collection.
- **Expected result:** The current todo item and progress remain visible; expansion shows the checklist
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`: The collapsed strip shows the current item and 4/6 progress; expansion exposes checklist items.
- **Remaining or next state:** The strip is collapsed or expanded while the node keeps its agent sub-state
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo`: The strip changes between collapsed and expanded while the selected node stays running.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/NodeTranscriptPane.tsx#RoomRegion`: The current room mounts TodoStrip before the transcript scroller, above the transcript.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5`: The completed Story describes the strip as pinned above the transcript; the mockup renders it below.

### M003 — Per-item Send now during generation

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: agent generating on soft-inject transport.
- Inventory IDs: I009.
- Action key: `send-queued-item-now`; status: PROVEN.
- **Visible presentation:** A Send now button on each queued item
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-send-now`: Every queued item has a Send now button.
- **Precondition:** An agent is generating on a soft-inject transport with a queued item in the live iteration
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#canSendNow`: The code shows the button only with softInject, Queue mode, no Stop request in flight, and the live iteration selected.
- **Action or trigger:** The operator selects that queued item’s Send now button
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow`: The per-item button calls that item’s sendNow handler.
- **Target identity:** The queued message beside the selected button
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow`: The handler is attached to one mapped queued item whose accessible label contains its text.
- **Target cardinality:** Exactly one selected queued item.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Timing:** Immediately upon provider acceptance during the running turn, without waiting for turn end.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Effect on active work:** The active turn and tool call continue without interruption or a new turn.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#stateNote=soft-inject`: The mockup note says this path does not interrupt the turn or current tool call.
- **Collection mutation:** Exactly the selected accepted item leaves the shared queue; other queued items remain.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Expected result:** The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Remaining or next state:** That item is absent from the queue; its transcript row reads sent until the provider confirms the same stamped message_id, then delivered.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current queued-item JSX renders message text, sent, and Delete, but has no per-item Send now button.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.2`: The completed queue and withdraw stories define Queue and Delete, but no per-item soft-inject action.

### M004 — Per-item Send now during generation

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: agent generating on soft-inject transport.
- Inventory IDs: I029.
- Action key: `send-queued-item-now`; status: PROVEN.
- **Visible presentation:** A Send now button on each queued item
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-send-now`: Every queued item has a Send now button.
- **Precondition:** An agent is generating on a soft-inject transport with a queued item in the live iteration
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#canSendNow`: The code shows the button only with softInject, Queue mode, no Stop request in flight, and the live iteration selected.
- **Action or trigger:** The operator selects that queued item’s Send now button
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow`: The per-item button calls that item’s sendNow handler.
- **Target identity:** The queued message beside the selected button
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow`: The handler is attached to one mapped queued item whose accessible label contains its text.
- **Target cardinality:** Exactly one selected queued item.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Timing:** Immediately upon provider acceptance during the running turn, without waiting for turn end.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Effect on active work:** The active turn and tool call continue without interruption or a new turn.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#stateNote=soft-inject`: The mockup note says this path does not interrupt the turn or current tool call.
- **Collection mutation:** Exactly the selected accepted item leaves the shared queue; other queued items remain.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Expected result:** The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name.
  - user_answer `user_answer:coordinator-260924-send`: Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Remaining or next state:** That item is absent from the queue; its transcript row reads sent until the provider confirms the same stamped message_id, then delivered.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current queued-item JSX renders message text, sent, and Delete, but has no per-item Send now button.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.2`: The completed queue and withdraw stories define Queue and Delete, but no per-item soft-inject action.

### M005 — Shared queued-message band and disclosure

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: agent generating with accepted queued guidance.
- Inventory IDs: I008.
- Action key: `toggle-shared-queue`; status: PROVEN.
- **Visible presentation:** A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`: The full room shows the QUEUED count, ordered rows, and a misleading this-tab-only label.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Precondition:** At least one accepted message is queued for the selected live node.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`: Two queued items are visible.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Action or trigger:** The operator selects the queue disclosure header.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The queue header invokes onToggleDraft.
- **Target identity:** The selected node's shared accepted-message queue band.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The handler changes the draftOpen state used by the queued list.
- **Target cardinality:** One band for the selected node.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`: One QUEUED band contains the accepted items.
- **Timing:** The band expands or collapses immediately when selected.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The handler updates local draftOpen state directly.
- **Effect on active work:** None; disclosure changes only the queue display.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The handler only toggles draftOpen.
- **Collection mutation:** None; opening or closing the band does not add, send, or delete a queued item.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The handler only toggles draftOpen.
- **Expected result:** The operator can show or hide the accepted rows while their shared count and order remain available.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`: The expanded band shows its accepted rows.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The disclosure toggles row visibility.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Remaining or next state:** The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`: The state changes only draftOpen.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- Classification evidence:
  - implementation `packages/web/src/experiments/console/components/ConsoleComposerDock.tsx#queueBandHeader`: The current room shows a flat accepted-message band with sent and Delete, without mockup ordinals, next-out mark, or disclosure.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1`: The completed story covers a shared QUEUED list and local unsent draft; it does not define the mockup disclosure and row treatment.

### M006 — Shared queued-message band and disclosure

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: agent generating with accepted queued guidance.
- Inventory IDs: I028.
- Action key: `toggle-shared-queue`; status: PROVEN.
- **Visible presentation:** A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`: The full room shows the QUEUED count, ordered rows, and a misleading this-tab-only label.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Precondition:** At least one accepted message is queued for the selected live node.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`: Two queued items are visible.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Action or trigger:** The operator selects the queue disclosure header.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The queue header invokes onToggleDraft.
- **Target identity:** The selected node's shared accepted-message queue band.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The handler changes the draftOpen state used by the queued list.
- **Target cardinality:** One band for the selected node.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`: One QUEUED band contains the accepted items.
- **Timing:** The band expands or collapses immediately when selected.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The handler updates local draftOpen state directly.
- **Effect on active work:** None; disclosure changes only the queue display.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The handler only toggles draftOpen.
- **Collection mutation:** None; opening or closing the band does not add, send, or delete a queued item.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The handler only toggles draftOpen.
- **Expected result:** The operator can show or hide the accepted rows while their shared count and order remain available.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`: The expanded band shows its accepted rows.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The disclosure toggles row visibility.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- **Remaining or next state:** The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.
  - interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`: The state changes only draftOpen.
  - user_answer `user_answer:coordinator-260924-queue`: Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/ComposerDock.tsx#queueBandHeader`: The current room shows a flat accepted-message band with sent and Delete, without mockup ordinals, next-out mark, or disclosure.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1`: The completed story covers a shared QUEUED list and local unsent draft; it does not define the mockup disclosure and row treatment.

### M007 — Run N occurrence separator

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`; rendered state: multi-occurrence node.
- Inventory IDs: I052.
- Action key: `None`; status: PROVEN.
- **Visible presentation:** Run 1, Run 2, and later Run N separators, with a contextual suffix such as retry or iteration when applicable.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The first displayed variant shows Run 1, Run 2 · retry, and Run 3 · iteration 2.
  - user_answer `user_answer:coordinator-260924-occurrence`: For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.
- **Precondition:** The selected node has more than one occurrence.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note`: The sheet says separators appear only for more than one occurrence.
  - user_answer `user_answer:coordinator-260924-occurrence`: For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.
- **Action or trigger:** The transcript renders occurrence groups.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The mockup renders separators between occurrence groups.
- **Target identity:** The selected node's transcript occurrence groups.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The separators are shown in the occurrence-header section.
- **Target cardinality:** One separator per occurrence when there are multiple occurrences.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The first variant renders a separator for each of three occurrences.
  - user_answer `user_answer:coordinator-260924-occurrence`: For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.
- **Timing:** When the multi-occurrence transcript is rendered.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note`: The sheet states the condition for rendering the headers.
- **Effect on active work:** None; the separator labels existing transcript history.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The section presents read-only transcript headers.
- **Collection mutation:** None; displaying separators does not alter transcript rows.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The section presents read-only transcript headers.
- **Expected result:** Each occurrence has a Run N header with the relevant suffix; Pass N and reason-only are not primary headers.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`: The first variant shows numbered Run headers with retry or iteration suffixes.
  - user_answer `user_answer:coordinator-260924-occurrence`: For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.
- **Remaining or next state:** The multi-occurrence transcript remains grouped under Run N headers; a single occurrence has no separator.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note`: The sheet omits the header for one occurrence.
  - user_answer `user_answer:coordinator-260924-occurrence`: For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.
- Classification evidence:
  - implementation `packages/web/src/lib/occurrence-groups.ts#factsOf`: The current grouping labels loop occurrences Iteration N instead of a universal Run N header.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed story selects Run, Iteration, Pass, or reason-only by context; the user selected universal Run N with suffixes.

### M008 — Operator transcript row without sender name

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html`; rendered state: operator exchange.
- Inventory IDs: I063.
- Action key: `None`; status: PROVEN.
- **Visible presentation:** An operator message row with full-strength text and a sent badge, without the sender's name.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=operator-record;element=operator-rows`: The approved static example shows operator · kevin and sent; the user changed only the name display.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Precondition:** One selected message is accepted by the provider and recorded in the transcript.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Action or trigger:** That accepted operator message renders in the transcript.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Target identity:** The transcript row for that selected stamped operator message.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Target cardinality:** Exactly one transcript row for the selected item.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Timing:** Immediately on provider acceptance.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Effect on active work:** None from the name display; stored attribution remains intact.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Collection mutation:** None from the visual name treatment; provider acceptance still removes the selected queue item and creates its operator transcript row.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Expected result:** The row shows the operator message and sent badge without the sender's name.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Remaining or next state:** The row remains sent until matching message_id confirmation, then reads delivered.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory`: The current operator row includes operatorDisplayName in its label and a sent badge.
  - implementation `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory`: The Console row also includes operatorDisplayName in its label.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8`: The completed story requires operator · <display name> and a sent badge; the user removed the visible name only.

### M009 — Delivered operator-message status

- Mockup: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html`; rendered state: message status after provider confirmation.
- Inventory IDs: I068.
- Action key: `None`; status: PROVEN.
- **Visible presentation:** A delivered badge on the matching operator message row.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered`: The status examples render a delivered badge.
  - user_answer `user_answer:coordinator-260924-delivery`: Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.
- **Precondition:** The operator transcript row is sent and the provider can confirm its stamped message_id.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
  - user_answer `user_answer:coordinator-260924-delivery`: Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.
- **Action or trigger:** The provider confirms the same stamped message_id.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Target identity:** The operator transcript row with that matching stamped message_id.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Target cardinality:** Exactly one matching operator message row.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Timing:** After the provider confirms the same stamped message_id.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Effect on active work:** None; confirmation updates the displayed delivery status.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Collection mutation:** None; the row stays in the transcript and only its status changes.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
- **Expected result:** The matching row changes from sent to delivered.
  - rendered `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered`: The sheet shows the delivered status example.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
  - user_answer `user_answer:coordinator-260924-delivery`: Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.
- **Remaining or next state:** The matching operator row reads delivered; unconfirmed rows continue to read sent.
  - user_answer `user_answer:coordinator-260924-receipt`: On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.
  - user_answer `user_answer:coordinator-260924-delivery`: Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.
- Classification evidence:
  - implementation `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory`: The current row always renders sent and has no confirmation transition.
  - implementation `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory`: The Console row always renders sent and has no confirmation transition.
  - epic `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8`: The completed story leaves every v1 provider row at sent; its later delivered transition is not part of that completed baseline.

## Context Inventory — UNCHANGED_CONTEXT

These items remain current product scope.
The current target does not have to specify them as new features.

| ID   | Visible context and state                                                           | Mockup                                                                                    | Implementation evidence                                                                                                                                                                                        | Completed Epic evidence                                                                                                                                                                  |
| ---- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C001 | Run shell, Running status, and separate Cancel control (running prompt node)        | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/routes/RunDetailPage.tsx#RunDetailPage`: The current product implements run shell, running status, and separate cancel control on this surface.                          | `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5`: The completed Epic records the existing run shell, running status, and separate cancel control contract. |
| C002 | Log, Graph, Artifacts, Tool calls, and System controls (running prompt node)        | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/routes/RunDetailPage.tsx#RunDetailPage`: The current product implements log, graph, artifacts, tool calls, and system controls on this surface.                          | `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5`: The completed Epic records the existing log, graph, artifacts, tool calls, and system controls contract. |
| C003 | Node log with completed, running, and pending steps (running prompt node)           | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/ConsoleNodeRoom.tsx#ConsoleNodeRoom`: The current product implements node log with completed, running, and pending steps on this surface.                     | `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5`: The completed Epic records the existing node log with completed, running, and pending steps contract.    |
| C004 | Selected implement node transcript and close control (running prompt node)          | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/inspect/ConsoleRoomHeader.tsx#ConsoleRoomHeader`: The current product implements selected implement node transcript and close control on this surface.        | `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5`: The completed Epic records the existing selected implement node transcript and close control contract.   |
| C005 | Assistant text and tool calls grouped by run (running prompt node)                  | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#ConsoleAgentHistoryList`: The current product implements assistant text and tool calls grouped by run on this surface.    | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing assistant text and tool calls grouped by run contract.                  |
| C006 | Expandable tool row and Raw control (running prompt node)                           | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#ToolHistory`: The current product implements expandable tool row and raw control on this surface.                         | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing expandable tool row and raw control contract.                           |
| C010 | Per-item Delete controls (agent generating)                                         | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current product implements per-item delete controls on this surface.                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.2`: The completed Epic records the existing per-item delete controls contract.                                   |
| C011 | Message composer and Queue control (agent generating)                               | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#composer`: The current product implements message composer and queue control on this surface.                                                          | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing message composer and queue control contract.                   |
| C012 | Stop current agent turn (agent generating)                                          | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#stop`: The current product implements stop current agent turn on this surface.                                                                         | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing stop current agent turn contract.                              |
| C013 | Stopping transient while node remains running (agent interrupting)                  | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#interrupting`: The current product implements stopping transient while node remains running on this surface.                                           | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing stopping transient while node remains running contract.        |
| C014 | Will send queue and Send now control (agent idle after interrupt)                   | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#idle`: The current product implements will send queue and send now control on this surface.                                                            | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing will send queue and send now control contract.                 |
| C015 | Interrupted tool glyph and retained-file disclosure (agent idle after interrupt)    | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/lib/steering-dock.ts#STEERING_INTERRUPT_DISCLOSURE`: The current product implements interrupted tool glyph and retained-file disclosure on this surface.                                     | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing interrupted tool glyph and retained-file disclosure contract.  |
| C016 | Operator messages in transcript and active work resumes (agent generating again)    | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory`: The current product implements operator messages in transcript and active work resumes on this surface. | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8`: The completed Epic records the existing operator messages in transcript and active work resumes contract.    |
| C017 | Read-only Never sent draft list (completed node with undelivered messages)          | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#finished`: The current product implements read-only never sent draft list on this surface.                                                             | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing read-only never sent draft list contract.                    |
| C018 | No steering dock (completed clean node)                                             | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#steeringDockMode`: The current product implements no steering dock on this surface.                                                                    | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing no steering dock contract.                                   |
| C019 | Failed 30-minute state and Never sent drafts (failed after idle interrupt)          | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/lib/steering-dock.ts#neverSentDisclosure`: The current product implements failed 30-minute state and never sent drafts on this surface.                                                      | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing failed 30-minute state and never sent drafts contract.       |
| C020 | Not steerable disclosure without composer (detached running node)                   | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE`: The current product implements not steerable disclosure without composer on this surface.                                                | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing not steerable disclosure without composer contract.          |
| C021 | Per-iteration log entries and Execution selector (running loop node)                | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/experiments/console/components/inspect/ConsoleRoomHeader.tsx#ConsoleRoomHeader`: The current product implements per-iteration log entries and execution selector on this surface.            | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing per-iteration log entries and execution selector contract.           |
| C022 | Read-only old iteration and Go to live iteration (finished loop iteration selected) | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#finished-iteration`: The current product implements read-only old iteration and go to live iteration on this surface.                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing read-only old iteration and go to live iteration contract.           |
| C023 | Per-item Send now absent (running queue-only provider)                              | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`    | `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current product implements per-item send now absent on this surface.                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing per-item send now absent contract.                             |
| C024 | Legacy run shell, status, Cancel, and tabs (running prompt node)                    | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/WorkflowExecution.tsx#WorkflowExecution`: The current product implements legacy run shell, status, cancel, and tabs on this surface.                                    | `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5`: The completed Epic records the existing legacy run shell, status, cancel, and tabs contract.             |
| C025 | Legacy node list with two implement runs (running prompt node)                      | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/WorkflowExecution.tsx#buildLogRows`: The current product implements legacy node list with two implement runs on this surface.                                           | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing legacy node list with two implement runs contract.                   |
| C026 | Selected implement transcript and Execution selector (running prompt node)          | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/NodeRoomHeader.tsx#NodeRoomHeader`: The current product implements selected implement transcript and execution selector on this surface.                                | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing selected implement transcript and execution selector contract.       |
| C027 | Expandable tool rows and Raw output (running prompt node)                           | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/NodeRoom.tsx#ToolHistory`: The current product implements expandable tool rows and raw output on this surface.                                                          | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing expandable tool rows and raw output contract.                           |
| C030 | Stopping transient (agent interrupting)                                             | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#interrupting`: The current product implements stopping transient on this surface.                                                                      | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing stopping transient contract.                                   |
| C031 | Will send and Send now with interrupted tool (agent idle after interrupt)           | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#idle`: The current product implements will send and send now with interrupted tool on this surface.                                                    | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing will send and send now with interrupted tool contract.         |
| C032 | Operator correction in transcript (agent generating again)                          | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory`: The current product implements operator correction in transcript on this surface.                                                        | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8`: The completed Epic records the existing operator correction in transcript contract.                          |
| C033 | Read-only Never sent list (completed with undelivered messages)                     | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#finished`: The current product implements read-only never sent list on this surface.                                                                   | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing read-only never sent list contract.                          |
| C034 | No steering dock (completed clean node)                                             | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#steeringDockMode`: The current product implements no steering dock on this surface.                                                                    | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing no steering dock contract.                                   |
| C035 | Failure and Never sent list (failed after idle interrupt)                           | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/lib/steering-dock.ts#neverSentDisclosure`: The current product implements failure and never sent list on this surface.                                                                       | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing failure and never sent list contract.                        |
| C036 | Not steerable disclosure (detached running node)                                    | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE`: The current product implements not steerable disclosure on this surface.                                                                 | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing not steerable disclosure contract.                           |
| C037 | Iteration selector and per-iteration entries (running loop node)                    | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/NodeRoomHeader.tsx#NodeRoomHeader`: The current product implements iteration selector and per-iteration entries on this surface.                                        | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing iteration selector and per-iteration entries contract.               |
| C038 | Read-only earlier iteration and live navigation (finished loop iteration selected)  | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#finished-iteration`: The current product implements read-only earlier iteration and live navigation on this surface.                                   | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing read-only earlier iteration and live navigation contract.            |
| C039 | Per-item Send now absent (running queue-only provider)                              | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current product implements per-item send now absent on this surface.                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing per-item send now absent contract.                             |
| C040 | Tool name chips use family colour (tool family palette)                             | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#CHIP_TONE`: The current product implements tool name chips use family colour on this surface.                                                              | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing tool name chips use family colour contract.                             |
| C041 | Five status glyphs (tool status key)                                                | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/tool-presentation.ts#OUTCOME_GLYPH`: The current product implements five status glyphs on this surface.                                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing five status glyphs contract.                                            |
| C042 | Family-specific one-line summaries (collapsed tool rows)                            | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/tool-presentation.ts#toolRowPresentation`: The current product implements family-specific one-line summaries on this surface.                                                            | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing family-specific one-line summaries contract.                            |
| C043 | Middle-elided path preserves filename (collapsed long path)                         | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#ToolHeadline`: The current product implements middle-elided path preserves filename on this surface.                                                       | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing middle-elided path preserves filename contract.                         |
| C044 | Multiline script headline and shell fallback chip (collapsed Codex shell row)       | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/tool-presentation.ts#shellHeadline`: The current product implements multiline script headline and shell fallback chip on this surface.                                                   | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing multiline script headline and shell fallback chip contract.             |
| C045 | Server and tool headline (collapsed MCP row)                                        | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/tool-presentation.ts#mcpLabel`: The current product implements server and tool headline on this surface.                                                                                 | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing server and tool headline contract.                                      |
| C046 | Generic scalar summary (collapsed unknown tool row)                                 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/tool-presentation.ts#genericFacts`: The current product implements generic scalar summary on this surface.                                                                               | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing generic scalar summary contract.                                        |
| C047 | Terminal output body and Raw control (expanded shell row)                           | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#ToolBodySwitch`: The current product implements terminal output body and raw control on this surface.                                                      | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing terminal output body and raw control contract.                          |
| C048 | Inline edit diff body (expanded file row)                                           | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#InlineDiff`: The current product implements inline edit diff body on this surface.                                                                         | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing inline edit diff body contract.                                         |
| C049 | Family-specific bodies (expanded web, search, glob, and code rows)                  | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#ToolBodySwitch`: The current product implements family-specific bodies on this surface.                                                                    | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing family-specific bodies contract.                                        |
| C050 | Folded latest checklist and earlier call summaries (expanded todo row)              | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeTranscriptPane.tsx#TodoStrip`: The current product implements folded latest checklist and earlier call summaries on this surface.                                   | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5`: The completed Epic records the existing folded latest checklist and earlier call summaries contract.         |
| C051 | Batch and single-agent dispatch bodies (expanded task row)                          | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#TaskBody`: The current product implements batch and single-agent dispatch bodies on this surface.                                                          | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing batch and single-agent dispatch bodies contract.                        |
| C053 | No occurrence header (single-occurrence node)                                       | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/lib/occurrence-groups.ts#groupByOccurrence`: The current product implements no occurrence header on this surface.                                                                            | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7`: The completed Epic records the existing no occurrence header contract.                                       |
| C054 | Raw JSON toggle (expanded tool row)                                                 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`    | `packages/web/src/components/workflows/NodeRoom.tsx#ToolHistory`: The current product implements raw json toggle on this surface.                                                                              | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1`: The completed Epic records the existing raw json toggle contract.                                               |
| C055 | Composer, Queue, and Stop beneath transcript (agent generating)                     | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#composer`: The current product implements composer, queue, and stop beneath transcript on this surface.                                                | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing composer, queue, and stop beneath transcript contract.         |
| C056 | Stopping transient and Queue (agent interrupting)                                   | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#interrupting`: The current product implements stopping transient and queue on this surface.                                                            | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing stopping transient and queue contract.                         |
| C057 | Will send, Send now, and retained-file disclosure (agent idle after interrupt)      | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#idle`: The current product implements will send, send now, and retained-file disclosure on this surface.                                               | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing will send, send now, and retained-file disclosure contract.    |
| C058 | Resume with Stop and Queue (agent generating again)                                 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#composer`: The current product implements resume with stop and queue on this surface.                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing resume with stop and queue contract.                           |
| C059 | Read-only Never sent state (completed with undelivered messages)                    | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#finished`: The current product implements read-only never sent state on this surface.                                                                  | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing read-only never sent state contract.                         |
| C060 | No dock (completed clean node)                                                      | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/lib/steering-dock.ts#steeringDockMode`: The current product implements no dock on this surface.                                                                                              | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing no dock contract.                                            |
| C061 | Failure and Never sent state (failed after 30-minute idle)                          | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/lib/steering-dock.ts#neverSentDisclosure`: The current product implements failure and never sent state on this surface.                                                                      | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing failure and never sent state contract.                       |
| C062 | Not steerable disclosure (detached running node)                                    | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE`: The current product implements not steerable disclosure on this surface.                                                                 | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing not steerable disclosure contract.                           |
| C064 | Sent message status example (message statuses)                                      | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory`: The current product implements sent message status example on this surface.                                                              | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8`: The completed Epic records the existing sent message status example contract.                                |
| C066 | No Enter-to-send shortcut (running agent)                                           | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/lib/steering-dock.ts#isQueueShortcut`: The current product implements no enter-to-send shortcut on this surface.                                                                             | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7`: The completed Epic records the existing no enter-to-send shortcut contract.                            |
| C067 | Per-item Delete control (agent generating)                                          | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`     | `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current product implements per-item delete control on this surface.                                                                   | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.2`: The completed Epic records the existing per-item delete control contract.                                    |
| C069 | Never sent message status example (message status example)                          | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#finished`: The current product implements never sent message status example on this surface.                                                           | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12`: The completed Epic records the existing never sent message status example contract.                  |
| C065 | Per-item send absent for a queue-only provider (refused-actions table)              | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` | `packages/web/src/components/workflows/ComposerDock.tsx#queue-list`: The current queued list offers Delete but no per-item Send now, including queue-only operation.                                           | `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1`: The completed queue story provides Queue and a queued list, without a per-item action.                       |

## Frozen Action Identity Audit

| Action key           | Feature IDs | Frozen outcome | Differences   | Decision and mockup evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------- | ----------- | -------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| send-queued-item-now | M003, M004  | SAME_ACTION    | None recorded | `user_answer:coordinator-260924-send` (user_answer): Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>`user_answer:coordinator-260924-receipt` (user_answer): On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| toggle-shared-queue  | M005, M006  | SAME_ACTION    | None recorded | `user_answer:coordinator-260924-queue` (user_answer): Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` (rendered): Console disclosure toggles its accepted-list display.<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` (rendered): Legacy disclosure toggles its accepted-list display.                                                                                          |

## PRD Analysis

The canonical SPEC is the target PRD equivalent.
Its 13 numbered capabilities are the complete functional requirement set.
The text below preserves each capability, intent, success condition, and explicit data contract.

### Functional Requirements

- **CAP-1** — Scan a node's work without expanding anything
  - **intent:** A reader can scan a node's tool calls one line each and tell what ran, what it ran on, and whether it worked.
  - **success:** A node with forty tool calls renders forty single-line rows, each carrying a family chip, a status glyph, a headline naming the salient argument, and right-aligned badges.
    Successful calls are collapsed on first render and failed calls are expanded.
    No collapsed row contains serialized-data punctuation.

- **CAP-2** — Expand a call into a body shaped for that kind of tool
  - **intent:** A reader can open any tool call and see its input and output rendered for the kind of tool it is.
  - **success:** Every family renders its declared body arm per `tool-presentation-contract.md`.
    A tool matching no family renders at most three scalar `key: value` pairs, with objects and arrays collapsed to `{…}` / `[n]`, and never a JSON dump.
    Measured against the production corpus, the generic fallback claims under 2% of rows.

- **CAP-3** — Follow the agent's checklist as state, not as mutations
  - **intent:** A reader can see the agent's current todo list, with phases and per-item status, instead of a sequence of opaque updates.
  - **success:** Both provider shapes normalize to the same `TodoPhase[]` per `todo-fold-contract.md`.
    Every todo call in the transcript collapses to a one-line row; the current checklist lives only in the pinned todo strip, not inline in the transcript.
    A phase emptied by `rm` disappears rather than rendering an empty header.
    The **pinned todo strip** is after the transcript and immediately above the queue or dock, stays visible while transcript rows scroll, and is absent when the node has no todos.
    Its collapsed header shows the current item, completed count, and segmented meter.
    Its expanded state shows phases and items and exposes Raw.
    Terminal presentation marks remaining items done when the node completes and resets the current item to todo after the 30-minute interruption failure, without changing stored provider rows.

- **CAP-4** — See what a subagent dispatch asked for
  - **intent:** A reader can see the brief a task dispatch carried and which subtasks it spawned.
  - **success:** Both provider shapes normalize to `TaskSubtask[]`.
    The card renders batch context as markdown when the provider sends any, then one collapsible card per subtask naming the subtask and its agent.

- **CAP-5** — See what a file edit changed
  - **intent:** A reader can see the actual change a file edit made, inline, without leaving the transcript.
  - **success:** When the payload carries both before and after content, a line diff renders through `react-diff-view`.
    Claude always qualifies — `FileEditInput` declares `old_string` and `new_string` as required.
    Any persisted file tool row without both before/after string sides falls back to path plus preview, and a diff is never fabricated from one side.
    Scope is presentation over persisted rows only: current Codex `file_change` events are emitted as `system` chunks (`codex/provider.ts:709`) that the executor debug-logs as `dag.system_message_unhandled` (`dag-executor.ts:2691-2748`) instead of appending them to the node transcript, so they never become persisted file rows and are explicitly excluded here.
    Making successful Codex file changes visible in the transcript is separately tracked work.
  - **data contract:** `packages/web/src/lib/diff-hunks.ts` is the only `structuredPatch` caller, on `diff@9.0.0` — deterministic bounds (65,536-byte and 2,000-line side caps, `context: 4`, `maxEditLength: 2000`, no wall-clock timeout) and dual-bounded memoization (256 entries capped at 1,048,576 source code units).
    `tool-presentation.ts` qualifies a pair only after the family resolves to `file`, only as own-property strings (`''` valid; inherited keys, throwing accessors, one-sided, and wrong types never qualify), and caches the result per input record in a `WeakMap` so summary and body compute once.
    Collapsed rows carry `+n`/`−m` badges; expanded rows carry `N hunk(s)` or `no changes` plus `replace_all` when the input sends it as an own boolean.
    Line content is display-sanitised — ANSI/C0/C1 stripped, `Cf`/`U+2028`/`U+2029` escaped as `\u{HEX}`, 1,024-code-unit ceiling — while diffing runs on raw strings and Raw keeps the original payload.
    Refused, identical, and non-qualifying pairs degrade honestly; a failed edit still shows its attempted diff alongside the normalized failure output.

- **CAP-6** — Tell attempts and loop iterations apart
  - **intent:** A reader can tell which attempt or loop iteration produced a given tool call.
  - **success:** A node whose rows span more than one `occurrence_id` renders a header per group; a single-occurrence node renders none.
    Grouping keys on `occurrence_id`, never on `attempt_id`.
    Headers use `Run N` for repeated top-level execution, `Iteration N` for a loop occurrence, `Pass N` for another provider turn inside the same occurrence, and a non-numbered reason-only header for interruption or recovery.
    A **loop-iteration selector** lets the reader navigate directly between occurrence groups; the per-group headers remain its targets, and it is absent on a single-occurrence node.
    On a live loop node, selecting a **finished** iteration through the **`Execution` selection controls** (header select, Logs row, or graph occurrence; `Jump to` stays scroll-only) renders a read-only dock, a `Go to iteration N` control, and a read-only band that mirrors the node-scoped shared pending queue across operators and tabs.
    The composer and steering actions are absent in this view.
    Every steering mutation carries the selected `retry_epoch`, and the server fails closed when that epoch is stale, so a delayed or replayed mutation from a finished iteration cannot affect the live iteration.
    The authenticated node-scoped `GET …/queue` read is allowed so the band stays current.

- **CAP-7** — Keep the raw payload reachable
  - **intent:** A developer debugging a provider can still read the exact bytes the provider sent.
  - **success:** Every card exposes a Raw toggle revealing the original JSON, closed by default.
    This is the **only** place serialized JSON appears.
    Today's `canLoadFullOutput` / `onLoadFullOutput` flow keeps working.

### Write — steer the live agent

- **CAP-8** — Compose while the agent works
  - **intent:** An operator watching a running node can write a message without disturbing it.
  - **success:** The composer is mounted and enabled while the node runs, and the send control reads `Queue`.
    Sending holds the message; the node is untouched, no tool call is interrupted and nothing is lost.
    The message is delivered as the next turn when the current turn ends naturally.
    The words `this tab only` apply only to unsent composer draft content, including when that draft shares one combined surface with queued items.
    A queued message is node-scoped, shared across operators and tabs, and server-process-local; it survives a tab close and dies only on a server restart.

- **CAP-9** — Interrupt the agent's thinking; the node keeps running
  - **intent:** An operator can stop the agent's _current generation_ to redirect it, without stopping the node or abandoning the run.
  - **success:** An interrupt control in the node's composer dock ends the agent's **current turn** — via the provider's own primitive (claude `interrupt()`) or a stream-abort — and the **provider session stays alive**.
    The **node stays `running`** throughout (its agent moves to a projected `idle-after-interrupt` sub-state) — never paused, never `pending`, never `node_failed`.
    The transcript shows the in-flight tool call as _interrupted_ rather than _failed_ (this is CAP-1's status glyph `⚠`).
    Interrupting the agent is **not** stopping the node: that is the existing **Cancel** feature, separate and untouched.
    Nothing suggests the interrupt undid work already written.

- **CAP-10** — Redirect and continue on the same live session
  - **intent:** After interrupting, the operator sends what to do instead and the agent carries on from there — on the same session, in the same node.
  - **success:** Once the agent is `idle-after-interrupt`, the send control reads `Send now`.
    Sending dispatches the newly typed message (the queued messages are already on the registry); the executor then flushes the registry — the already-queued messages followed by this one, **in receipt/written order** — as the **next turn on the same provider session** (reusing the `attemptResumeId` re-ask seam).
    The node **continues** — it never "resumes" from a pause, because it never paused.
    Ordering is enforced by us, not assumed of the provider.
    Both controls follow the agent's projected sub-state, not a remembered mode.

- **CAP-11** — The exchange is part of the record
  - **intent:** Anyone reading the transcript afterwards can see what the operator said, and when, relative to what the agent did.
  - **success:** The operator's messages and the interrupted tool call appear as ordinary transcript rows in the order they happened — between the call they interrupted and the one they caused.
    An operator row is visibly the operator's, never mistakable for the agent's own text.
    _This is where the write half writes into the read half — see Cross-half dependency._

- **CAP-12** — Mid-turn delivery, as fast as each provider's transport allows
  - **intent:** The operator's message reaches a running agent without interrupting it, and sooner on a provider whose transport can take it mid-turn.
  - **success:** Sending to a running agent is an **ordinary prompt**, not a separate steer primitive.
    `Queue` remains available on every provider and delivers at the next natural boundary without an interrupt.
    Every queued item also shows `Send now` while the agent is generating.
    Selecting it soft-injects that item into the active turn when the selected provider supports soft-inject, with no interrupt, interrupted tool call, or turn-start event.
    On providers without soft-inject, the same visible control returns a clear refusal state and leaves the item queued.
    Claude streaming input and OMP RPC mode are current-scope soft-inject transports.
    The PRD defers Grok hooks as G3; the Scope Decision Audit below checks whether current approved Send now behavior depends on that hook.

- **CAP-13** — The interface claims only what it knows
  - **intent:** An operator can tell whether a message merely left the browser or actually reached the agent.
  - **success:** A message reads `sent` until the provider confirms the exact caller-stamped message id, at which point it reads `delivered`.
    Correlation is by id alone.
    `delivered` is current product behavior and is reachable only after the Claude dependency is at `@anthropic-ai/claude-agent-sdk` **≥ 0.3.246**.
    Providers that cannot confirm the id remain at `sent`.

Total functional capabilities: **13**.

### Non-Functional Requirements

The SPEC has no independent numbered NFR list.
Its normative non-functional constraints are:

1. Both Legacy and Console rooms ship together; shared render-neutral logic stays in `packages/web/src/lib/`, with thin JSX on each surface (`SPEC.md`, Constraints, Shared).
2. Strict TypeScript, zero ESLint warnings, and `bun run validate` are required (`SPEC.md`, Constraints, Shared).
3. Read-side changes are retroactive and need no schema, migration, or backend change (`SPEC.md`, Why and Constraints, Read half).
4. Status meaning must remain available without color (`SPEC.md`, Constraints, Read half).
5. Tool presentation must avoid default serialized JSON; Raw remains explicit, and unclassifiable assistant text stays unchanged (`SPEC.md`, Constraints, Read half).
6. Provider shape differences normalize at the edge; renderers do not branch on provider identity (`SPEC.md`, Constraints, Read half).
7. Steering remains in process and has no durable steering state; detached runs cannot be steered (`SPEC.md`, Constraints, Write half).
8. Authenticated steering mutations carry `retry_epoch`; stale epochs fail closed without changing queue or transcript state (`SPEC.md`, Constraints, Write half).
9. Queue messages are node-scoped and shared after acceptance; unsent draft content is tab-local (`SPEC.md`, CAP-8 and Constraints, Write half).
10. Confirmation of `delivered` uses the caller-stamped message id, never text or time (`SPEC.md`, CAP-13 and Constraints, Write half).
11. A server restart drops the live handle and queue; ambiguous non-terminal runs are not failed from staleness (`SPEC.md`, Constraints, Write half).

These are 11 grouped constraints, not invented `NFR` identifiers.
The target Epic has a numbered NFR inventory that will be checked in the Epic validation step.

### Additional Requirements and Source Integrity

The SPEC frontmatter names eight target contract and test companions, two UX documents, two Architecture spines, and the approved mockup handoff.
The SPEC says 64 visible current-scope behaviors (`SPEC.md`, Capabilities), while the validated frozen manifest inventories 70 visible items.
The count discrepancy does not change any manifest behavior or classification.
The write-half Constraints contain literal unresolved merge markers at `SPEC.md:221`, `SPEC.md:276`, and `SPEC.md:287`.
The HEAD side makes G1, G2, and G4 current release scope; the other side labels G1, G2, and G4 post-v1 gated backlog.
These are normative and mutually exclusive release constraints; one implementation cannot satisfy both release gates.
The current-scope CAP-12 and CAP-13 text and approved visible mockup behaviors support the HEAD side, but the stale branch still remains in the canonical contract.

### PRD Completeness Assessment

The 13 capability descriptions cover the main read and write flows.
The unresolved merge and contradictory release gate make the canonical requirement contract internally inconsistent.
The 64-versus-70 inventory count needs correction after reconciliation; item-level scope follows the validated manifest.

## Epic Coverage Validation

The Epic FR Coverage Map at `epics.md:187–209` claims coverage for all 13 canonical capabilities.
Story references below are structural coverage only; behavioral equivalence is tested in the mockup matrix.

| Requirement   | Canonical capability               | Epic and Story coverage           | Structural status |
| ------------- | ---------------------------------- | --------------------------------- | ----------------- |
| FR1 / CAP-1   | Readable tool-call row             | 1.1, 3.1                          | COVERED           |
| FR2 / CAP-2   | Family-shaped expanded tool body   | 1.3                               | COVERED           |
| FR3 / CAP-3   | Folded and pinned todo strip       | 1.5, 3.1                          | COVERED           |
| FR4 / CAP-4   | Subagent dispatch and subtasks     | 1.6                               | COVERED           |
| FR5 / CAP-5   | Inline file-edit diff              | 1.4                               | COVERED           |
| FR6 / CAP-6   | Occurrence grouping and navigation | 1.7, 2.9, 2.10, 3.2, 3.6          | COVERED           |
| FR7 / CAP-7   | Explicit Raw payload access        | 1.2                               | COVERED           |
| FR8 / CAP-8   | Compose and queue while running    | 2.1, 2.2, 2.9, 2.10, 3.3, 3.4     | COVERED           |
| FR9 / CAP-9   | Interrupt turn and preserve node   | 2.3–2.7, 3.6                      | COVERED           |
| FR10 / CAP-10 | Redirect on same session           | 2.3–2.7                           | COVERED           |
| FR11 / CAP-11 | Operator exchange in transcript    | 2.8, 2.13                         | COVERED           |
| FR12 / CAP-12 | Boundary and soft-inject delivery  | 2.1, 2.3–2.7, 3.4, 3.5; G3 future | COVERED           |
| FR13 / CAP-13 | Id-confirmed delivered status      | 2.8, 3.5                          | COVERED           |

### Missing Requirements

No canonical CAP/FR number is absent from the Epic coverage map.
The Epic has additional explicit terminal reconciliation, idle-await safety, concurrency, and all-mockup-behavior rows; these expand the canonical capabilities.

### Coverage Statistics

- Canonical functional requirements: 13.
- Numbered requirements mapped by Epic: 13.
- Structural FR coverage: 100%.
- Behavioral match: assessed separately below; structural coverage does not prove readiness.

## UX Alignment Assessment

### UX Document Status

The target has final `DESIGN.md` and `EXPERIENCE.md` documents in `ux-Archon-agent-node-room-2026-09-09/`.
Both UX files and both selected Architecture spines are within the target Epic input lineage.

### Alignment Findings

| Behavior                            | UX and Architecture evidence                                                                                                                                                                | Alignment result                                                                                                                                                                                    |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M001–M002 todo strip                | `DESIGN.md:586–587`, `EXPERIENCE.md:166`, readable Architecture AD-17, and steering Architecture AD-13 place one pinned strip below the transcript and above the queue or dock.             | Aligned with the frozen mockup and the current CAP-3/Story 3.1 acceptance criterion.                                                                                                                |
| M005–M006 shared queue band         | `DESIGN.md:733–741`, `EXPERIENCE.md:179`, and steering Architecture AD-13 state full-room band, disclosure, ordinals, shared accepted queue, and tab-local unsent draft.                    | Aligned with the frozen mockup, CAP-8, and Story 3.3.                                                                                                                                               |
| M003–M004 per-item Send now         | `DESIGN.md:748–750`, `EXPERIENCE.md:177,180`, and steering Architecture AD-3 specify exactly one selected accepted item into the active turn without interrupt on Claude or OMP.            | The supported-provider path aligns; the documents additionally require a visible queue-only refusal control, while frozen context C065 says that per-item action is absent on queue-only providers. |
| M007 occurrence heading             | `DESIGN.md:684`, `EXPERIENCE.md:97–100,170`, readable Architecture CAP-6/AD-17, and steering Architecture AD-13 assign primary `Iteration N`, `Pass N`, or reason-only headings by context. | CONFLICT with the frozen M007 user decision for universal primary `Run N` with contextual suffix.                                                                                                   |
| M008 selected per-item operator row | `DESIGN.md:694` and `EXPERIENCE.md:173` require a visible sender name for multi-operator rows; steering Architecture AD-12 resolves that name for display.                                  | CONFLICT only for the row created by accepted per-item Send now during generation; M008/user answer requires no visible sender name on that row, while stored attribution remains.                  |
| M009 delivered status               | `DESIGN.md:698–701`, `EXPERIENCE.md:174`, and steering Architecture AD-8 confirm by the exact caller-stamped message id.                                                                    | Aligned with the frozen mockup and CAP-13/Story 3.5.                                                                                                                                                |

### Warnings and Reconciliation

The queue-only refusal control is a separate state and target from the supported-provider M003–M004 action.
The documents can share one implementation only if the queue-only control is visible, while frozen C065 records the opposite approved state; implementation cannot satisfy both visible states in the same queue-only context.
The occurrence label difference is normative presentation in the same multi-occurrence context.
A contextual suffix on `Run N` cannot also make `Iteration N`, `Pass N`, or a reason-only string the primary heading.
The sender-name difference is normative only on the selected per-item acceptance path; retaining server-side attribution and names on other paths can coexist with M008.
No missing UX document warning applies.

## Epic Quality Review

| Epic                                   | User value and independence                                                            | Story structure                                                                                                                 | Finding                                                                                                                                                                               |
| -------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Epic 1 — Readable Agent Transcript     | A reader can scan stored runs immediately; it has no forward Epic dependency.          | Stories 1.1–1.7 are user actions with testable Given/When/Then criteria and backward dependencies only.                         | Story 1.5’s user-story line says “above the transcript” (`epics.md:354`), while its acceptance criterion says “below the transcript” (`epics.md:362`); the latter matches the mockup. |
| Epic 2 — Live Agent Steering           | Operators can queue, interrupt, continue, and audit a live node using Epic 1’s reader. | Stories 2.1–2.13 have explicit provider, error, ordering, identity, and terminal cases; all stated dependencies point backward. | The completed baseline in Story 2.8 demands a visible sender name; its current-scope corrective owner must state the narrow M008 exception.                                           |
| Epic 3 — Approved-Behavior Remediation | Corrective stories deliver visible changes to both rooms after Epics 1 and 2.          | Stories 3.1–3.6 have user-visible acceptance criteria and backward dependencies. Story 3.7 is a conformance gate after them.    | Several criteria contradict frozen approved behavior; the 64-row conformance target is stale against the validated 70-item inventory.                                                 |

### Dependency and Sizing Review

The explicit dependencies for Stories 1.1–1.7, 2.1–2.13, and 3.1–3.7 point only to earlier Stories or delivered baselines.
No forward dependency or technical-only Epic was found.
The read half requires no new database table; the write half uses an in-process registry and additive row metadata.
The brownfield integration seams are named in the affected Stories.
Story 3.7 is a verification gate rather than an independently shippable UI behavior; as an Epic completion criterion it is coherent, but its 64-row target must reflect the validated inventory.

### Quality Defects

1. **Critical — contradictory acceptance for occurrence labels.**
   Story 1.7 (`epics.md:429–434`) and Story 3.2 (`epics.md:938–944`) require context-dependent primary headings, while frozen M007 requires universal primary `Run N` with suffixes.
   The same occurrence cannot present both as its primary heading.
2. **Critical — incomplete and conflicting selected-row acceptance.**
   Story 3.4 (`epics.md:1015–1038`) defines the selected per-item delivery but does not state the row’s no-name projection.
   Completed Story 2.8 (`epics.md:701–708`) requires `operator · <display name>` on operator rows, and Story 3.5 does not narrow that old rule.
   M008 requires the no-name visual treatment only for the accepted per-item Send now row; other operator rows are outside this change.
3. **Critical — queue-only control state conflicts with approved context.**
   Story 3.5 (`epics.md:1051–1056`) requires a visible per-item Send now refusal for a queue-only provider.
   Frozen C065 records the queue-only provider state without that action.
   This context remains current scope even though it is classified UNCHANGED_CONTEXT.
4. **Major — conformance count is stale.**
   Story 3.7 and the Epic 3 completion criteria demand 64 matched behaviors (`epics.md:883–888, 1141–1175`).
   The validated manifest has 70 classified visible items, including 9 changed and 61 unchanged.
   The behavior count cannot certify the current approved set.
5. **Major — Story 1.5 contains opposing placement wording.**
   Its user-story goal says above the transcript (`epics.md:354`), while the acceptance criterion says below (`epics.md:362`).
   The lower placement is consistent with the mockup, SPEC CAP-3, both Architecture spines, and Story 3.1.
6. **Major — Story 3.6 treats mockup review controls as product controls.**
   Its numbered state, node-kind, and transport criteria (`epics.md:1092–1108`) describe controls above the product frame that only change fixture state.
   Their HTML placement and fixture-only handlers identify them as review scaffolding under the extractor skill; the in-product Execution selector and actual lifecycle, navigation, artifact, and close controls remain in the approved product inventory.

### Recommendations for Planning Owners

Reconcile Story 1.5’s placement wording, Story 3.2’s occurrence criteria, the narrow per-item operator-row presentation, the queue-only action state, Story 3.6’s review-control criteria, and Story 3.7’s inventory count against the immutable approved manifest.
Preserve the completed Stories as history while changing only current corrective acceptance criteria in a later repair task.

## Mockup Feature Behavior Matrix

Each row below represents one atomic CHANGE_FEATURE behavior from the frozen manifest.
The complete field-level mockup and user provenance is in the Mockup Behavior Evidence Ledger above.
The matrix repeats the frozen values without filling them from planning prose.

| ID and exact mockup location                                                                                                                                                          | Mockup precondition                                                                        | User action                                                      | Target and cardinality                                                                                               | Timing                                                                                      | Effect on active work and expected result                                                                                                                                                                                                                                                                                                 | Remaining or next state                                                                                                                    | Exact PRD requirement                                                                                                                                                                                                | Exact Architecture decision                                                                                                                                  | Exact Epic or Story acceptance criterion                                                                                                                          | Status   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| M001<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`<br>running prompt node                             | The selected agent node has todo calls                                                     | The selected node room renders or the operator toggles the strip | The selected node todo strip; One strip for the selected node                                                        | Pinned and visible while the transcript scrolls                                             | Active work: None; expanding the strip changes only its display<br>Collection: None<br>Result: The current todo item and progress remain visible; expansion shows the checklist                                                                                                                                                           | The strip is collapsed or expanded while the node keeps its agent sub-state                                                                | `SPEC.md` CAP-3 (`:94–107`): pinned strip after transcript, current item/count/meter, expansion, no strip without todos.                                                                                             | Readable Architecture AD-17 (`:306–326`) and steering Architecture AD-13 (`:313–316`): below transcript and above queue/dock.                                | `epics.md` Story 3.1 AC (`:902–918`), plus Story 1.5 AC (`:360–386`).                                                                                             | MATCHED  |
| M002<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`<br>running prompt node                              | The selected agent node has todo calls                                                     | The selected node room renders or the operator toggles the strip | The selected node todo strip; One strip for the selected node                                                        | Pinned and visible while the transcript scrolls                                             | Active work: None; expanding the strip changes only its display<br>Collection: None<br>Result: The current todo item and progress remain visible; expansion shows the checklist                                                                                                                                                           | The strip is collapsed or expanded while the node keeps its agent sub-state                                                                | `SPEC.md` CAP-3 (`:94–107`): same Legacy strip behavior.                                                                                                                                                             | Readable Architecture AD-17 (`:306–326`) and steering Architecture AD-13 (`:313–316`): both shells.                                                          | `epics.md` Story 3.1 AC (`:902–918`), plus Story 1.5 AC (`:360–386`).                                                                                             | MATCHED  |
| M003<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-send-now`<br>agent generating on soft-inject transport  | An agent is generating on a soft-inject transport with a queued item in the live iteration | The operator selects that queued item’s Send now button          | The queued message beside the selected button; Exactly one selected queued item.                                     | Immediately upon provider acceptance during the running turn, without waiting for turn end. | Active work: The active turn and tool call continue without interruption or a new turn.<br>Collection: Exactly the selected accepted item leaves the shared queue; other queued items remain.<br>Result: The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name. | That item is absent from the queue; its transcript row reads sent until the provider confirms the same stamped message_id, then delivered. | `SPEC.md` CAP-12 (`:172–180`) and `control-states.md:94–108`: one selected item soft-injects during generation; `SPEC.md:277` also calls G2/G4 post-v1. Neither PRD clause states the selected row hides its sender. | Steering Architecture AD-3 (`:139–159`), AD-6 (`:205–215`), AD-12 (`:289–303`): one item, no turn interruption, attributed row; no exact no-name projection. | `epics.md` Story 3.4 AC (`:1015–1038`) covers one-item delivery; Story 2.8 AC (`:698–708`) demands the visible sender name.                                       | CONFLICT |
| M004<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-send-now`<br>agent generating on soft-inject transport   | An agent is generating on a soft-inject transport with a queued item in the live iteration | The operator selects that queued item’s Send now button          | The queued message beside the selected button; Exactly one selected queued item.                                     | Immediately upon provider acceptance during the running turn, without waiting for turn end. | Active work: The active turn and tool call continue without interruption or a new turn.<br>Collection: Exactly the selected accepted item leaves the shared queue; other queued items remain.<br>Result: The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name. | That item is absent from the queue; its transcript row reads sent until the provider confirms the same stamped message_id, then delivered. | `SPEC.md` CAP-12 (`:172–180`) and `control-states.md:94–108`: one selected item soft-injects during generation; `SPEC.md:277` also calls G2/G4 post-v1. Neither PRD clause states the selected row hides its sender. | Steering Architecture AD-3 (`:139–159`), AD-6 (`:205–215`), AD-12 (`:289–303`): one item, no turn interruption, attributed row; no exact no-name projection. | `epics.md` Story 3.4 AC (`:1015–1038`) covers one-item delivery; Story 2.8 AC (`:698–708`) demands the visible sender name.                                       | CONFLICT |
| M005<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`<br>agent generating with accepted queued guidance | At least one accepted message is queued for the selected live node.                        | The operator selects the queue disclosure header.                | The selected node's shared accepted-message queue band.; One band for the selected node.                             | The band expands or collapses immediately when selected.                                    | Active work: None; disclosure changes only the queue display.<br>Collection: None; opening or closing the band does not add, send, or delete a queued item.<br>Result: The operator can show or hide the accepted rows while their shared count and order remain available.                                                               | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                 | `SPEC.md` CAP-8 (`:140–150`) and `control-states.md:110–122`: node-shared accepted queue, tab-local unsent draft, collapse without mutation, ordinals and next-out mark.                                             | Steering Architecture AD-13 (`:314–322`): full-room band, one shared queue projection and local collapse.                                                    | `epics.md` Story 3.3 AC (`:975–1001`): full-bleed band, count/order/ordinals/next-out and no accepted-item tab-local label.                                       | MATCHED  |
| M006<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`<br>agent generating with accepted queued guidance  | At least one accepted message is queued for the selected live node.                        | The operator selects the queue disclosure header.                | The selected node's shared accepted-message queue band.; One band for the selected node.                             | The band expands or collapses immediately when selected.                                    | Active work: None; disclosure changes only the queue display.<br>Collection: None; opening or closing the band does not add, send, or delete a queued item.<br>Result: The operator can show or hide the accepted rows while their shared count and order remain available.                                                               | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                 | `SPEC.md` CAP-8 (`:140–150`) and `control-states.md:110–122`: same Legacy queue behavior.                                                                                                                            | Steering Architecture AD-13 (`:314–322`): both shells use the full-room band.                                                                                | `epics.md` Story 3.3 AC (`:975–1001`): both full rooms.                                                                                                           | MATCHED  |
| M007<br>`claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`<br>multi-occurrence node                          | The selected node has more than one occurrence.                                            | The transcript renders occurrence groups.                        | The selected node's transcript occurrence groups.; One separator per occurrence when there are multiple occurrences. | When the multi-occurrence transcript is rendered.                                           | Active work: None; the separator labels existing transcript history.<br>Collection: None; displaying separators does not alter transcript rows.<br>Result: Each occurrence has a Run N header with the relevant suffix; Pass N and reason-only are not primary headers.                                                                   | The multi-occurrence transcript remains grouped under Run N headers; a single occurrence has no separator.                                 | `SPEC.md` CAP-6 (`:122–136`) and Capabilities context (`:76–79`): primary Run/Iteration/Pass/reason-only by context.                                                                                                 | Readable Architecture AD-7 (`:167–199`) and steering Architecture AD-13 (`:313`): same context-dependent primary labels.                                     | `epics.md` Story 1.7 AC (`:429–444`) and Story 3.2 AC (`:938–944`): same context-dependent primary labels.                                                        | CONFLICT |
| M008<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=operator-record;element=operator-rows`<br>operator exchange                  | One selected message is accepted by the provider and recorded in the transcript.           | That accepted operator message renders in the transcript.        | The transcript row for that selected stamped operator message.; Exactly one transcript row for the selected item.    | Immediately on provider acceptance.                                                         | Active work: None from the name display; stored attribution remains intact.<br>Collection: None from the visual name treatment; provider acceptance still removes the selected queue item and creates its operator transcript row.<br>Result: The row shows the operator message and sent badge without the sender's name.                | The row remains sent until matching message_id confirmation, then reads delivered.                                                         | `SPEC.md` CAP-11 (`:166–170`) and write constraint (`:256`): distinct operator row and stored sender identity; no exact no-name visual clause for this selected per-item path.                                       | Steering Architecture AD-6 (`:205–215`) and AD-12 (`:289–303`): stored attribution and derived display name; no explicit path-specific name suppression.     | `epics.md` Story 2.8 AC (`:698–708`) and UX-DR5 (`:168–170`) require `operator · <display name>` for the row; Story 3.4 does not name the selected-row exception. | CONFLICT |
| M009<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered`<br>message status after provider confirmation      | The operator transcript row is sent and the provider can confirm its stamped message_id.   | The provider confirms the same stamped message_id.               | The operator transcript row with that matching stamped message_id.; Exactly one matching operator message row.       | After the provider confirms the same stamped message_id.                                    | Active work: None; confirmation updates the displayed delivery status.<br>Collection: None; the row stays in the transcript and only its status changes.<br>Result: The matching row changes from sent to delivered.                                                                                                                      | The matching operator row reads delivered; unconfirmed rows continue to read sent.                                                         | `SPEC.md` CAP-13 (`:182–187`) requires exact-id confirmation and current delivered state; conflicting write constraint (`:277`) calls G1 post-v1.                                                                    | Steering Architecture AD-8 (`:223–231`) and AD-13 (`:326–332`): exact-id transition, G1 current.                                                             | `epics.md` Story 3.5 AC (`:1063–1072`) requires sent until matching-id confirmation then delivered.                                                               | CONFLICT |

### Atomic Coverage

- CHANGE_FEATURE rows: 9.
- MATCHED: 4 (M001, M002, M005, M006).
- CONFLICT: 5 (M003, M004, M007, M008, M009).
- PARTIAL: 0.
- MISSING: 0.
- UNCLEAR: 0.
- Full behavioral match: 4/9 = **44.4%**.
- Approved product-item classification: 70/70 = **100%** classified once (9 changed, 61 unchanged).
- Outer review controls above the product frame are excluded based on the HTML and extractor skill; the product Execution selector is included.

### Normative Conflict and Reconciliation Tests

| Finding                                   | Frozen behavior and planning claim                                                                                                                                                                                                                                                        | Claim types                                                                                                                            | Reconciliation attempted                                                                                                      | Why it fails                                                                                                                                                           |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M003–M004 and M008 selected-row name      | M003–M004 expected result and M008 user answer say the accepted selected item creates a row without the sender name; `epics.md` Story 2.8 (`:698–708`) says operator rows show `operator · <display name>`.                                                                               | Mockup/user decision: normative visible projection for one per-item path. Story 2.8: normative acceptance criterion for operator rows. | Keep `operator_user_id` and the derived server field, but omit its visible name only for per-item Send now during generation. | Story 2.8 has no exception and Story 3.4 does not supply one; the current criteria still require the name on that same row. Other delivery paths are not part of M008. |
| M007 occurrence label                     | M007 user answer requires primary `Run N` for each multi-occurrence separator with contextual suffix; `SPEC.md` CAP-6 (`:122–136`), readable Architecture AD-7 (`:175–183`), and Story 3.2 (`:938–944`) require context-dependent primary `Iteration N`, `Pass N`, or reason-only labels. | All are normative display rules for the same header.                                                                                   | Put iteration/retry text after `Run N`.                                                                                       | A suffix cannot make `Iteration N`, `Pass N`, or reason-only the primary header.                                                                                       |
| M009/G1 and M003–M004/G2/G4 release scope | Current visible delivered and soft-inject behavior is explicit in frozen M009/M003/M004, `SPEC.md` CAP-12/13 (`:172–187`), Architecture AD-3/8, and Epic 3; `SPEC.md:277` says all G1/G2/G4 are post-v1.                                                                                  | Current behavior and post-v1 gate are opposing normative scope constraints.                                                            | Treat the `SPEC.md:277` branch as historical release text.                                                                    | Literal merge markers keep it active in the canonical SPEC; no source instruction has removed it.                                                                      |
| Queue-only context C065                   | Frozen C065 records per-item Send now absent for a queue-only provider; `control-states.md:94–108`, steering Architecture AD-3 (`:149–152`), and Story 3.5 (`:1051–1056`) require that same control visible with refusal.                                                                 | Mockup/user answer: normative queue-only visible state. Planning sources: normative control visibility and acceptance.                 | Use a refusal state instead of actual soft-inject.                                                                            | This still displays the control in the very context where approved C065 says it is absent.                                                                             |

## Action Identity Audit — Final Comparison

The frozen manifest groups two pairs of same-intent controls.
All ten behavior-signature values match within each pair; the file-specific sources below prove the corresponding control and state on each surface.
This identity result does not override planning conflicts in the feature matrix.

### send-queued-item-now — M003, M004

Audit result: **MATCHED action identity across the two approved surfaces**.
The manifest records no behavior differences and no open question.

| Signature field       | Frozen value on both surfaces                                                                                                              | M003 field provenance                                                                                                                                                              | M004 field provenance                                                                                                                                                             |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Visible presentation  | A Send now button on each queued item                                                                                                      | rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-send-now`                                           | rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-send-now`                                           |
| Precondition          | An agent is generating on a soft-inject transport with a queued item in the live iteration                                                 | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#canSendNow`                                                                 | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#canSendNow`                                                                 |
| Trigger               | The operator selects that queued item’s Send now button                                                                                    | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow`                                                         | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow`                                                         |
| Target identity       | The queued message beside the selected button                                                                                              | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow`                                                         | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow`                                                         |
| Target cardinality    | Exactly one selected queued item.                                                                                                          | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                          | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                         |
| Timing                | Immediately upon provider acceptance during the running turn, without waiting for turn end.                                                | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                          | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                         |
| Effect on active work | The active turn and tool call continue without interruption or a new turn.                                                                 | user_answer `user_answer:coordinator-260924-send`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#stateNote=soft-inject` | user_answer `user_answer:coordinator-260924-send`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#stateNote=soft-inject` |
| Collection mutation   | Exactly the selected accepted item leaves the shared queue; other queued items remain.                                                     | user_answer `user_answer:coordinator-260924-receipt`                                                                                                                               | user_answer `user_answer:coordinator-260924-receipt`                                                                                                                              |
| Expected result       | The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name.           | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                          | user_answer `user_answer:coordinator-260924-send`<br>user_answer `user_answer:coordinator-260924-receipt`                                                                         |
| Next state            | That item is absent from the queue; its transcript row reads sent until the provider confirms the same stamped message_id, then delivered. | user_answer `user_answer:coordinator-260924-receipt`                                                                                                                               | user_answer `user_answer:coordinator-260924-receipt`                                                                                                                              |

The group decision evidence is preserved in the Frozen Action Identity Audit above.

### toggle-shared-queue — M005, M006

Audit result: **MATCHED action identity across the two approved surfaces**.
The manifest records no behavior differences and no open question.

| Signature field       | Frozen value on both surfaces                                                                                                                       | M005 field provenance                                                                                                                                                                                                                                                                                                       | M006 field provenance                                                                                                                                                                                                                                                                                                     |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Visible presentation  | A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label. | rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                  | rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                 |
| Precondition          | At least one accepted message is queued for the selected live node.                                                                                 | rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                  | rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                 |
| Trigger               | The operator selects the queue disclosure header.                                                                                                   | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                               | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                              |
| Target identity       | The selected node's shared accepted-message queue band.                                                                                             | user_answer `user_answer:coordinator-260924-queue`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`                                                                                                                                         | user_answer `user_answer:coordinator-260924-queue`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`                                                                                                                                        |
| Target cardinality    | One band for the selected node.                                                                                                                     | rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`                                                                                                                                                                                        | rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`                                                                                                                                                                                       |
| Timing                | The band expands or collapses immediately when selected.                                                                                            | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                               | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                              |
| Effect on active work | None; disclosure changes only the queue display.                                                                                                    | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                               | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                              |
| Collection mutation   | None; opening or closing the band does not add, send, or delete a queued item.                                                                      | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                               | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`                                                                                                                                                                                              |
| Expected result       | The operator can show or hide the accepted rows while their shared count and order remain available.                                                | rendered `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`<br>user_answer `user_answer:coordinator-260924-queue` | rendered `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`<br>interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`<br>user_answer `user_answer:coordinator-260924-queue` |
| Next state            | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                          | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                         | interaction_code `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft`<br>user_answer `user_answer:coordinator-260924-queue`                                                                                                                                        |

The group decision evidence is preserved in the Frozen Action Identity Audit above.

## Scope Decision Audit

The audit treats each future, post-v1, later, deferred, gated, optional, stretch, removed, superseded, or out-of-scope use by what it applies to.
Historical text and implementation mechanisms cannot defer a visible approved behavior.

| Scope label and source                                                                                                                                                                                                                                    | Visible mockup applicability                                                                                                                                                                                                                              | Decision and dependency check                                                                                                                                                                                                                                                                                     | Audit result                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| G1 delivered post-v1, G2 Claude soft-inject post-v1, and G4 OMP soft-inject post-v1 in `SPEC.md:277`                                                                                                                                                      | M009, M003, and M004 show these as current behavior; Epic 3 (`epics.md:871–888, 1008–1084`) also calls them current.                                                                                                                                      | The owner-ratified 2026-09-15 gate is a historical decision that conflicts with the 2026-09-24 frozen approved scope. A new approved mockup would be required to defer these visible features.                                                                                                                    | **CONFLICT**                                                                                                        |
| G3 Grok hook soft-inject future/deferred/gated in `SPEC.md:180,226,277,356,365`; `provider-steering-matrix.md:81–90`; steering Architecture AD-3 and Deferred; `epics.md:1183–1195`; UX spines                                                            | Frozen C065 has no per-item Send now on a queue-only provider; `provider-steering-matrix.md:20–36,81–90` identifies Grok as queue-only until G3. M003–M004 apply only to true soft-inject transports. No approved mockup requires Grok mid-turn delivery. | `SPEC.md:277` records the owner-ratified G3 post-v1 gate. `provider-steering-matrix.md:20–36,81–90` and steering Architecture AD-3 (`:139–159`) distinguish independent Claude streaming and OMP RPC paths from Grok’s unexercised hook. G3 is not a technical dependency of current M003–M004 or delivered M009. | **ACCEPTED FUTURE SCOPE** for the hook itself; queue-only control visibility remains a separate conflict with C065. |
| Detached-run steering and restart survival deferred in `SPEC.md:319–329,360–362` and steering Architecture AD-5 (`:193–203`)                                                                                                                              | Frozen C062 shows “Not steerable” disclosure on a detached running node, not an active detached steering control.                                                                                                                                         | The canonical SPEC explicitly selects in-process scope; the Architecture explains that a detached executor has no reachable live handle. This does not remove an approved visible behavior.                                                                                                                       | **CONSISTENT NON-GOAL**                                                                                             |
| Existing Cancel/abort path called out of scope in `SPEC.md:232,278,319–323` and steering Architecture AD-1 (`:106–115`)                                                                                                                                   | Cancel is visible as existing context, not a new steering action; its manifestation is preserved by `epics.md` Story 3.6 (`:1098–1111`).                                                                                                                  | “Out of scope” applies to changing Cancel internals, not to removing the visible control. It is classified as unchanged context in the frozen manifest.                                                                                                                                                           | **CONSISTENT BOUNDARY**                                                                                             |
| Read-side new persisted data, draft persistence, auto-send queue, other product surfaces, run-level Files changed, unused providers, thinking/prompt/advisor notifications excluded in `SPEC.md:201–205,319–336`; read Architecture Deferred (`:423–431`) | None is a visible current-change feature in the four approved node-room mockups.                                                                                                                                                                          | These are explicit canonical SPEC non-goals or separate surfaces; no current M or C inventory item is removed by their exclusion.                                                                                                                                                                                 | **CONSISTENT NON-GOALS**                                                                                            |
| Handoff README stale scope and placement (`README.md:62,82–86,361–370`)                                                                                                                                                                                   | The frozen M001/M002 strip is below the transcript; M003/M004 per-item Send now and M009 delivered are current.                                                                                                                                           | The README calls G2 post-v1 and the strip top-placed; these historical notes cannot override the frozen approved behavior. Its `:202–204` review-scaffolding note agrees with the HTML placement and fixture-only handlers.                                                                                       | **CONFLICT** for its stale visible behavior labels; review-scaffolding scope is accepted.                           |
| Old durable stop-and-resume design superseded in steering Architecture frontmatter (`:11`)                                                                                                                                                                | Approved Stop/Queue/Send now states and current SPEC CAP-9/10 use the live session and keep the node running.                                                                                                                                             | The frontmatter says the old design was owner-ratified as superseded on 2026-09-13. It does not defer current steering.                                                                                                                                                                                           | **CONSISTENT HISTORICAL SUPERSESSION**                                                                              |

The word “optional” in `EXPERIENCE.md:73`, `DESIGN.md:762`, and related markup describes data shape or DOM content, not optional product scope.
The word “removed” in token names and “later” in timing examples does not label a visible feature as future work.
No other selected-artifact future label changes the 70-item frozen visible inventory.

## Visible Label and Internal Action Audit

| Visible word and context                          | Internal action                                                                  | Target, timing, and result check                                                                                                                                                                                                               | Audit                              |
| ------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| `Queue` while generating (C011)                   | `intent:'queue'` in `steering-api-contract.md:27–29`                             | The current turn continues; one accepted message enters the shared node queue at press time (`SPEC.md` CAP-8 and Story 2.1).                                                                                                                   | MATCHED                            |
| Per-item `Send now` during generation (M003–M004) | `intent:'soft_inject'` in `steering-api-contract.md:27–29,110–111`               | The selected queued id alone targets the active turn without Stop; the internal name preserves the visible action’s target, cardinality, and timing on a proven transport. The row presentation and stale scope gate still conflict elsewhere. | MAPPING MATCHED; PLANNING CONFLICT |
| Dock `Send now` after Stop (C014/C031)            | `intent:'send_now'` in `steering-api-contract.md:27–29`                          | It flushes all queued messages plus the new draft as the next turn after the agent is idle (`SPEC.md` CAP-10; Story 2.3). Its timing and cardinality differ from per-item Send now, and the frozen inventory keeps them separate.              | MATCHED DISTINCT ACTION            |
| Per-item `Delete` (C010/C067)                     | Idempotent withdraw route in `steering-api-contract.md:17–29` and Story 2.2      | One selected queued message leaves before delivery; a repeated withdraw is a success no-op.                                                                                                                                                    | MATCHED                            |
| `Stop` (C012/C015)                                | Provider-turn interrupt in `SPEC.md` CAP-9 and steering Architecture AD-2        | The active turn ends while the node and session remain live.                                                                                                                                                                                   | MATCHED                            |
| `Cancel` (C001/C024)                              | Existing node-level abort in `SPEC.md` Non-goals and Story 3.6                   | It is a separate lifecycle action, not a synonym for Stop.                                                                                                                                                                                     | MATCHED DISTINCT ACTION            |
| `delivered` (M009)                                | Exact stamped-id confirmation in `SPEC.md` CAP-13 and steering Architecture AD-8 | The matching sent row changes status only after provider confirmation. The stale post-v1 label remains a scope conflict.                                                                                                                       | MAPPING MATCHED; SCOPE CONFLICT    |

The same visible words do not license a shared target or timing.
The per-item and dock-level `Send now` controls have different approved preconditions and cardinality, so the action mapping keeps them separate.

## Review-Control Scope Audit

The numbered state controls, `prompt`/`loop ×3` selector, and transport toggle in both HTML files sit above the bordered product frame and change only mockup fixture state.
The source locations are `Console Node Room.dc.html:40–55,505–509,522–529,599` and `Legacy Node Room.dc.html:40–55,468–472,486,553`.
They are review scaffolding, so the validated manifest correctly excludes them from the 70 approved product items.
The real `Execution` selector inside the product frame is in frozen C021/C026/C037/C038.
The selected planning documents still call the outer review controls product UI: `SPEC.md:298–300`, readable Architecture AD-17 (`:306–314`), steering Architecture AD-13 (`:303–313`), and `epics.md` Story 3.6 (`:1092–1108`).
The extractor skill excludes review scaffolding, and the handoff README (`:202–204`) confirms that these outer controls are scaffolding.
This is an **extra-scope planning conflict**, not a manifest defect.
The report does not assign a product behavior signature to those fixture controls.

## Summary and Recommendations

### Overall Readiness Status

**NOT READY.**
Five of nine changed-behavior rows conflict with at least one selected planning source.
The canonical SPEC contains unresolved merge markers and opposite release gates for three visible current behaviors.
All 70 approved product inventory items are classified.
The outer review controls are excluded by the HTML evidence and extractor skill rule; no mockup behavior question remains open.

### Critical Issues Requiring Action

1. Resolve the canonical `SPEC.md` merge and remove the stale post-v1 gate for visible G1, G2, and G4 behavior.
   Keep the owner-ratified G3 Grok hook gate only after preserving its separation from Claude and OMP current paths.
2. Align SPEC CAP-6, both Architecture context rules, UX, and Stories 1.7/3.2 with M007’s primary `Run N` rule and its suffixes.
3. Specify the no-visible-sender-name rule only for the operator row made by accepted per-item Send now during generation.
   Keep stored `operator_user_id` and any server read-model attribution; do not change other operator-row paths without a separate approved decision.
4. Align the queue-only per-item control state with frozen C065.
   The approved queue-only state has no per-item Send now, while the planning sources require a visible refusal control.
5. Replace the 64-behavior conformance count in the SPEC and Epic 3 with the validated 70-item product inventory and require every changed row to match.
6. Fix Story 1.5’s “above” user-story sentence so it agrees with its “below” acceptance criterion and the approved strip placement.
7. Remove the outer numbered state, node-kind, and transport review controls from product requirements in the SPEC, both Architecture spines, and Story 3.6.
   Preserve the real in-product `Execution` selector and the listed lifecycle, navigation, artifact, and close controls.

### Recommended Next Steps

1. Repair the planning sources in a separate task; preserve the approved mockups, validated manifest, and historical completed Stories as evidence.
2. Recheck M001–M009 against the repaired PRD, Architecture, and corrective Story acceptance criteria.
3. Rerun this readiness workflow after the scope and conflicting rows are reconciled.

### Final Note

This assessment found **seven distinct planning defects** across source integrity, behavior alignment, and conformance scope.
The five non-MATCHED rows are M003, M004, M007, M008, and M009; M003/M004 and M008 share the narrow sender-name defect.
The seventh defect is the planning documents’ treatment of outer review controls as product UI.
G3 is technically independent of the approved current Claude and OMP per-item paths.
A Grok hook is not needed for the approved queue-only Grok state, although the planning documents’ visible queue-only refusal control still conflicts with C065.
No implementation or planning repair was made in this assessment.

### Unresolved Questions

None.
