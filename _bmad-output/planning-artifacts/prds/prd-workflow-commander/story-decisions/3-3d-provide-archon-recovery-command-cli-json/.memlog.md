---
topic: 3-3d-provide-archon-recovery-command-cli-json
goal: clear-story-technical-decisions
updated: 2026-07-20T13:20
---

- (event) accepted intent: create; mode: guided
- (event) accepted intent: create; mode: batch
- (insight) Source reconciliation: normative PRD, architecture, Epic 3.3d, and canonical fixtures require real resume/retry/cancel provider actions through the shared envelope; runtime reality has validate-only resume JSON, no workflow retry/cancel subcommands, and legacy abandon/retry-node surfaces; exact result contracts and cross-process command ownership remain unsettled.
- (note) Lifecycle and authority self-critiques found no unrecorded material issue after adding the post-ack hard-crash boundary: ambiguous cross-process running state must be surfaced for explicit recovery and never auto-mutated.
- (event) final gate: BLOCKED; reviewStatus: PENDING; unresolved decisions: 9; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md
- (event) accepted intent: create; mode: batch; revision requested
- (direction by user) Preserve workflow resume --json as validate-only; make workflow retry [--node] --json perform the actual asynchronous recovery and return success or failure as soon as recovery is accepted rather than waiting for workflow completion; simplify workflow cancel --json to an immediate durable cancel result.
- (change) Revised TD-001 through TD-006, lifecycle ownership, source reconciliation, and executable proof: resume JSON remains validate-only; retry JSON owns actual detached recovery with immediate accepted success/failure; cancel success is the durable CAS result and is independent of cleanup completion.
- (note) Rejected prior reasoning: do not change resume --json into a dispatch command and do not add an Archon supervisor merely to envelope the CLI own hard crash; uncatchable missing or invalid subprocess output belongs to consumer classification, while post-ack retry failures belong to workflow status and events.
- (event) final gate after user-requested revision: BLOCKED; reviewStatus: PENDING; unresolved decisions: 9; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md
- (event) accepted intent: create; mode: batch; canonical reconciliation authorized
- (decision by user) Authorize local canonical reconciliation: resume-success and Epic use validate-only semantics; workflow.retry owns actual asynchronous recovery acknowledgement; update directly dependent Archon contract validation and tests.
- (change) Reconciled the local resume and whole-run retry fixtures, contract guidance and semantic validator, Architecture command baseline, Epic 3.3d acceptance criteria, dependent Archon envelope test, and technical-decision source reconciliation.
- (note) The historical implementation-readiness sprint change proposal remains unchanged because it records prior context rather than current canonical authority.
- (validation) Contract validation passed for all 17 command examples; exact representative-family reproduction passed 13/13; the complete focused test passed 48 semantic tests and failed only its intentional dirty-contract-package guard because this authorized canonical change is uncommitted.
- (event) final gate after canonical reconciliation: BLOCKED; reviewStatus: PENDING; unresolved decisions: 8; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md
- (decision by user) TD-002 uses a process-spawn acknowledgement boundary: the parent CLI succeeds once it creates the detached retry process; whether the worker later wins the workflow-run claim belongs to the worker and does not delay or change the parent response.
- (implication) The current retry-success fixture and proof cannot claim `state: running`, `resumed: true`, attempt advancement, or one-winner command success at the spawn boundary; the exact dispatch-only result shape remains to be reconciled.
- (decision by user) Approve the TD-002 whole-run result payload as `operation: retry`, `scope: run`, `dispatched: true`, and `detached: true`, with no claim-derived state, resumed, accepted, or attempt fields.
- (change) Revised the pending TD-002 suggestion, lifecycle ownership, source reconciliation, TD-006 dependency, and executable proof to use process-dispatch acknowledgement; canonical Epic, Architecture, retry fixture, validator, dependent test, and consumer changes remain explicitly pending separate authorization and TD-003 resolution.
- (decision by user) Approve TD-003 with the same process-spawn acknowledgement boundary and targeted result payload `operation: retry`, `scope: node`, requested `nodeId`, `dispatched: true`, and `detached: true`; the worker owns all later validation, claim, preparation, reset, invalidation, and execution outcomes.
- (change) Revised the pending TD-003 suggestion, lifecycle ownership, TD-006 fixture requirements, and executable proof to use targeted process-dispatch acknowledgement; canonical and consumer changes remain pending separate authorization.
- (decision by user) Approve TD-004 as a direct durable cancel compare-and-swap for running, paused, or failed runs; return immediately with `operation: cancel`, `state: cancelled`, and `terminal: true`, omit stale-prone `previousState`, return `UNEXPECTED_STATE` on a lost or invalid transition, and do not wait for worker quiescence or cleanup.
- (change) Revised the pending TD-004 suggestion, lifecycle ownership, TD-006 fixture requirements, and executable proof to use the minimal durable-cancel result; canonical and consumer changes remain pending separate authorization.
- (decision by user) Approve TD-005 producer-consumer error ownership: Archon envelopes every caught pre-response failure; Hermes classifies empty output, malformed or schema-invalid output, consumer-enforced timeout, and uncatchable process exit; no Archon supervisor is added; post-dispatch worker failures are workflow outcomes.
- (change) Revised the pending TD-005 suggestion, lifecycle ownership, source reconciliation, and executable proof to make the producer-consumer boundary explicit; PRD, Epic, error-example, and consumer changes remain pending separate authorization.
- (decision by user) Authorize all remaining local Archon canonical reconciliation and keep Hermes compatibility review as an external follow-up.
- (change) Replaced the whole-run retry fixture with the dispatch-only result, added the targeted retry fixture, removed `previousState` from cancel, synchronized PRD, Architecture, Epic, contract guidance, semantic validation, and dependent CLI fixture-reproduction tests.
- (note) TD-006 is now limited to Hermes consumer approval and compatibility proof; no unresolved local payload contradiction remains.
- (validation) Contract validation passed for all 18 command examples; the representative 12-family test passed 13/13; the separate targeted-retry fixture test passed; lint and decision-gate normalization passed with 8 pending batch items.
- (decision by user) Approve TD-007 as a small typed parent recovery error mapping: malformed request uses `MALFORMED_REQUEST` and exit 64; missing or ineligible resume or cancel state and cancel compare-and-swap loss use `UNEXPECTED_STATE` and exit 78; caught timeout uses `COMMAND_TIMEOUT` and exit 69; spawn, database, and other caught unexpected parent failures use non-retryable `INTERNAL_ERROR` and exit 70.
- (decision by user) Do not expose or remap `WorkflowRetryError` through the parent command response because node validation, claim, checkpoint, reset, and execution failures occur in the detached worker after successful dispatch.
- (change) Narrowed the pending TD-007 suggestion, source reconciliation, lifecycle gap, and executable proof from an exhaustive worker-error mapper to the four approved synchronous parent error outcomes.
- (correction by user) Retry is already implemented through the Web UI; Story 3.3d is exposing that feature through the CLI, so there is no product decision about caller cwd matching.
- (rejected prior reasoning) Legacy CLI short-id behavior and cwd-matching TODO tests were incorrectly elevated above the implemented UI/API and shared core retry operation; cwd is not part of the feature's retry identity, authorization, or path-selection contract.
- (decision by user) The CLI retry adapter reuses `runId`, persisted run and codebase context, requester audit context, and the existing shared retry operation; it does not add caller-cwd matching.
- (change) Removed TD-008 from unresolved decisions, recorded the adapter-reuse behavior as settled TD-N06, and updated lifecycle and executable proof to prevent cwd from overriding persisted run context.
- (decision by user) Approve TD-009 as JSON-only scope for the new `workflow retry` and `workflow cancel` spellings; keep existing non-JSON `workflow retry-node`, `workflow abandon`, and `workflow resume` behavior unchanged.
- (change) Revised the pending TD-009 evidence and executable proof to require clear non-JSON usage failures without adding duplicate human aliases.
- (event) final gate after TD-009 selection: BLOCKED; reviewStatus: PENDING; unresolved decisions: 7; local Archon decisions reviewed individually, whole-file batch approval and Hermes consumer compatibility proof remain outstanding; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md
- (direction by user) Complete and validate the Archon producer implementation first; Hermes implementation and consumer compatibility verification happen afterward.
- (rejected prior reasoning) Hermes compatibility proof was incorrectly retained as an Archon Story 3.3d implementation prerequisite even though the PRD excludes Hermes implementation from Archon and the Epic defines Hermes Story 3.4c as a downstream consumer.
- (change) Revised Architecture, contract guidance, Epic 3.3d, source reconciliation, lifecycle ownership, and TD-005 to use producer-first sequencing; removed TD-006 from unresolved decisions and recorded the sequencing as settled TD-N07.
- (validation) Shared contract validation still passes all 18 command examples; decision-gate normalization passes with 6 pending whole-file batch items.
- (event) final gate after producer-first sequencing correction: BLOCKED; reviewStatus: PENDING; unresolved decisions: 6; only explicit whole-file batch approval remains before the candidate PASS review; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md
- (approval by user) Approve the entire technical-decisions.md file.
- (change) Promoted TD-002, TD-003, TD-004, TD-005, TD-007, and TD-009 into material decisions with behavior, authority, rejected alternatives, affected contracts, and owners; set unresolved decisions to none and prepared the PASS handoff.
- (review) Lifecycle self-critique found and resolved one wording ambiguity: retry-claim compare-and-swap loss is a later worker outcome, while cancel compare-and-swap loss is an immediate `UNEXPECTED_STATE` parent response; no lifecycle decision remains open.
- (review) Authority self-critique found no remaining normative conflict: PRD, Architecture, Epic 3.3d, canonical recovery fixtures, validator, and dependent Archon contract tests agree; Hermes proof remains a downstream consumer responsibility.
- (validation) Contract validation passed all 18 command examples; targeted retry fixture reproduction passed 1/1; representative command-family reproduction passed 13/13 with exactly 12 command families.
- (event) final gate: PASS; reviewStatus: APPROVED; unresolved decisions: 0; artifact: \_bmad-output/planning-artifacts/prds/prd-workflow-commander/story-decisions/3-3d-provide-archon-recovery-command-cli-json/technical-decisions.md; next: bmad-create-story
- (event) session complete
