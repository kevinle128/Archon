---
scope: Archon Source Control tab — legacy workflow-run screen
purpose: build-substrate
altitude: feature
updated: 2026-09-05T21:51
---

- (event) create run; Fast path + spine-only; folder scoped architecture-Archon-source-control-2026-09-05 to avoid colliding with HITL spine architecture-Archon-workflow-run-view-hitl-2026-09-05
- (constraint) INHERITED SPEC CAP-1..7 v1; CAP-8 SHOULD; read-only; run.working_path resolved server-side from runId; realpath; never accept path from UI; git -C working_path on run branch not base dev; container-backend = CAP-6; events never the change list
- (constraint) INHERITED UX final 2026-09-05: 30/70 split resizable 20-70%; two-pane M with independent scroll; stack below 900px; reusable viewer; manual Reload; M/A/D letter-carried
- (decision) AD-1 [ADOPTED] D6/plans: pin + contain — UI sends runId+file/commit refs only; realpath(working_path); live content realpath must stay under checkout; commit-scoped via ls-tree -z + cat-file blob (no oid:path); reject NUL/absolute/encoded-..; colon/leading-dash/glob filenames SUCCESS
- (decision) AD-2 [ADOPTED] D1: structured git-read helpers live in @archon/git (changedFiles/fileDiff/fileAt/log) on execFileAsync; server routes stay thin (resolve, gate, transport)
- (decision) AD-3 [ADOPTED] D2: JSON OpenAPI for changes/log/hunks; raw wildcard app.get for file content/binary/ranges (artifacts precedent)
- (decision) AD-4 [ADOPTED] D3: canonical hunk JSON (not unified-diff wire); web adapter maps to react-diff-view HunkData/ChangeData; cursor+truncated pagination
- (version) react-diff-view 3.3.3 verified npm 2026-03-30; highlight.js ^11.11.1 and @tanstack/react-virtual ^3 already in @archon/web; do not add Shiki
- (decision) AD-5 [ADOPTED] D3: one new dep react-diff-view + installed hljs tokenize + react-virtual; A/D use raw content route
- (decision) AD-6 [ADOPTED] D5: container gate = conversation.isolation_env_id -> isolationEnvironments.getById.provider === container BEFORE any git; host availability = dir exists && is git at read time
- (decision) AD-7 [ADOPTED]: live SoT = git on run worktree; workflow events = provenance hints only
- (decision) AD-8 [ADOPTED] D4 seam only: CAP-8 write via WorkflowDeps finalize hook; fail-open; idempotent; v1 does not implement the write
- (decision) AD-9 [ADOPTED] UX: v1 surface is legacy /legacy/workflows/runs/:id only; viewer built as reusable component for later HITL; no console coupling
- (assumption) Route prefix /api/workflows/runs/:runId/git/{changes,log,diff} + /api/artifacts-style raw /api/workflows/runs/:runId/git/file/* — names are seed, OpenAPI schemas own the final paths
- (question) Child/subrun isolation_env resolution when conversation->env FK is missing — D5 left to build-time test; not an AD
- (event) adversarial gate: accepted pairs 1,2,3,5 into AD-4/AD-6/conventions; REJECTED pair 4 — workflow_runs.working_path already exists and is the SPEC pin; AD-1 clarified do-not-add-column
- (decision) AD-4 tightened: ref sentinel live for scope now; cursor opaque string echoed as query param; web must not parse as scroll offset
- (decision) AD-6 tightened: container gate on every git route; CAP-6 always HTTP 200 + emptyReason; never 404
- (event) reconcile UX/SPEC: AD-5 thresholds+a11y from viewer-rules; AD-6 CTA split + region empties vs CAP-6; AD-9 stale banner never-mutate + copy owned by EXPERIENCE Voice and Tone
- (event) rubric walker pass-with-fixes: applied AD-6 NULL isolation_env_id fall-through; Auth convention = artifacts/run-detail (no requireWebUser); AD-9 binding floor for quiet copy; operational envelope deferred (no new process/env/cache)
- (assumption) Route path seed and web folder name remain seed; revisit when OpenAPI schemas land — not a CE blocker
- (event) source files missing from disk at finalize: spec-archon-source-control/ and ux-Archon-source-control-2026-09-05 DESIGN/EXPERIENCE; spine distilled from memlog + prior reads; restore before bmad-spec
- (event) spine finalized
