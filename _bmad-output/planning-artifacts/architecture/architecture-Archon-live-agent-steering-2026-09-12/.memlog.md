---
scope: Live Agent Steering (spec-live-agent-steering) — the WRITE half of the node room: stop a running node without failing it, send a queued correction, resume; provider steering seam where available. Invariants at engine (@archon/workflows) + provider (@archon/providers) altitude, plus the frontend dock/operator-row that rides the two parent frontend spines.
purpose: build-substrate
altitude: feature
paradigm: ports-and-adapters (inherited from the HITL spine)
updated: 2026-09-13T19:53
---

- (direction) User invocation: 'create detail architecture, don't guess, ask me if unclear, consult advisor first.' Coaching path (not Fast). Consulted advisor before drafting. Artifacts in English to match the three existing spines; conversation in Vietnamese.
- (constraint) INHERITED from HITL spine (architecture-Archon-workflow-run-view-hitl-2026-09-05), binding read-only. HITL/AD-1: pendingInteractionSchema is canonical in packages/workflows/src/schemas; kind IN ('ask','permission') — CHECK-constrained in BOTH dialects (migrations/000_combined.sql:770, sqlite createSchema). Run status stays 'paused'; a node stays 'awaiting' until all its pending rows resolve.
- (constraint) INHERITED HITL/AD-2: pause without approval slot. Ask pause sets status='paused', must NOT write metadata.approval, is idempotent. In-flight siblings finish streaming (shouldContinueStreamingForStatus); next DAG layer does not start. ONE resume owner: the workflow-operations CAS moving the last pending row to answered/purged is the only caller of resumeWorkflowRun; the executor never resumes an Ask pause, it re-enters on hydrate-when-running. Exactly the blast radius the steering spec's CAP-2 accepted.
- (constraint) INHERITED HITL/AD-6: provider-owned resume injection. Executor re-enters with a normal sendQuery + SendQueryOptions.resumeInteractions: Array<{tool_use_id,payload,declined}> (all answered rows for that node, store order); answers are NOT put in the prompt. Precedent CAP-3 (send the queue on resume) extends. resumeInteractions at providers/src/types.ts:734.
- (constraint) INHERITED HITL/AD-7: dedicated refetch-trigger events (node_awaiting, interaction_resolved), GET embed of pending_interactions on existing run route, split typed POST endpoints. Retry-epoch/stale guard validated server-side at the tool boundary, never the browser (AGENTS.md determinism-after-interpretation).
- (constraint) INHERITED HITL/AD-8: additive schema only; both dialects mirrored; indexes/comments in trailing section; bun run check:schema-upgrades required. Run chrome 'awaiting' = status==='paused' AND count(pending)>0.
- (constraint) INHERITED HITL/AD-5+AD-9: AskHuman owned by @archon/workflows; branded throw rejected through sendQuery; a new provider capability is a flag + a generate:capability-matrix axis (askHuman was v1 Claude+Pi). Awaiting triggers ONLY on the tool invocation, never prose-sniffing. Precedent for a 'steer' capability axis.
- (constraint) INHERITED from Track A transcript spine (architecture-Archon-readable-agent-transcript-2026-09-12), binding read-only. TrackA/AD-1: one core two markup shells — what a row MEANS is decided in packages/web/src/lib and reaches shells as data; a shell picks markup only, never inspects input/output/tool-name. CAP-4's operator row is bound by this: its meaning is composed in lib/, not in either shell.
- (constraint) INHERITED TrackA/AD-3: the core never throws, always terminates; unparseable->generic; logs family+tool_use_id, NEVER payload or tool name. TrackA/AD-10: the core produces EVERY string a row displays or announces — glyph char, visually-hidden accessible name, family word, body-bar facts. The operator row's glyph + accessible name are core output. TrackA/AD-13: glyph is a total function of outcome alone.
- (constraint) INHERITED TrackA/AD-2: the Console boundary IS the no-restricted-imports lint rule at eslint.config.mjs:126-163 and nothing beyond it; packages/web/src/lib is shared ground both shells import; a new shared module needs no exception. Console imports nothing from @/lib/api functions.
- (note) SEAM 1 scouted: pending_interactions.kind CHECK-constrained ('ask','permission') both dialects (migrations/000_combined.sql:770, sqlite.ts). A new kind is a SQLite table rebuild (no ALTER COLUMN) — aion lesson ('reuse enum values, never widen a CHECK') rules it out. Operator stop should not ride a new pending_interactions.kind.
- (note) SEAM 2 scouted: streaming poll (dag-executor.ts:2298-2334) reads getWorkflowRunStatus -> string status ONLY (core/src/db/workflows.ts:614); paused keeps streaming. metadata.pending_writeback (dag-executor.ts:10146,10205) is the established cross-pause marker but read at RESUME (:10023), not the poll. A stop the poll can see needs a distinguishing run status OR the poll to read more than a status string — a real fork.
- (note) SEAM 3 scouted: ProviderCapabilities is per-provider (providers/src/types.ts), one capabilities.ts per provider; askHuman is the exact precedent for a 'steer' flag + matrix axis, default false. IAgentProvider.sendQuery (types.ts:903) is one-shot; ISP (AGENTS.md) => a steer inbound is a NEW narrow interface, not a method on IAgentProvider. Matrix: omp+claude are the two v1 yes-rows.
- (note) SEAM 4 scouted: POST /api/workflows/runs/{runId}/resume (api.ts:1301) takes {runId} only, NO body today. resumeInteractions (AD-6) is the text-injection contract. CAP-3 Send extends this route with a body or adds a new route; then: who writes the operator transcript row — server route at Send, or executor at re-entry (two writers of one entity = adversarial-lens hole; must become an AD).
- (direction) Deliverable settled by Kevin: spine + walkthrough.html (Track A precedent), not spine-only. Walkthrough built at Finalize step 5.
- (decision) AD (fork 1, Kevin): the operator-stop signal lives as a marker in workflow_runs.metadata, following the pending_writeback precedent (dag-executor.ts:10146,10205). Binds: the streaming poll, the cancel classifier, the store read. Prevents: a new pending_interactions.kind (CHECK-constrained, SQLite rebuild — aion lesson), a new run status (HITL/AD-8 bans widening workflowRunStatusSchema), and an in-memory-only signal (would miss detached runs). Rule: marker carries {node_id, retry_epoch}; the poll gains a store read that returns it (not just the status string, workflows.ts:614); zero schema change (metadata is JSON), Zod-guarded shape; durable across restart; reaches detached runs. Rejected in-memory (C) explicitly because the spec requires stop to reach detached runs via the DB.
- (decision) AD (fork 2, Kevin): the executor is the single writer of the operator transcript row, appended at node re-entry — NOT the server at Send. Binds: CAP-3, CAP-4, the resume path, node-message appends. Prevents: a second appender to remote_agent_workflow_node_messages (would break HITL/AD-3's single-writer), seq-assignment contention between server and executor, and an operator row whose position relative to the caused tool call is unguaranteed. Rule: Send persists the ordered operator messages into the fork-1 metadata marker (extending it from stop-request to send-with-queue); the re-entering executor reads them, appends each as a text row with metadata.origin='operator' (seq-ordered, between the interrupted call and the caused call per CAP-4), then hands them to the provider for injection in the AD-6 resumeInteractions shape. The resume route (api.ts:1301, {runId}-only, dispatches /workflow resume) is not given a payload param; the marker is the carriage. Steering has its own marker-clear-and-resume trigger, parallel to — not the same CAS as — HITL/AD-2's Ask-resume owner; the two are mutually exclusive because Send is blocked while any pending interaction exists (dag-executor.ts:10367-10370).
- (direction) Kevin reframed fork 3 and superseded the earlier B/A/C framing entirely. Steering is NOT a side interface: it is part of IAgentProvider, the common Archon provider contract. Every provider inherits and implements it; the disqualification gate is 'can this Impl deliver a steer at all'. He also corrected a conflation I made: deepseek's 2ms rejection of a concurrent prompt is a TRANSPORT detail, not a contract incapacity — the Impl absorbs it by queue-and-flush.
- (decision) AD (steering contract, Kevin, ratified): steering methods live on IAgentProvider (the common provider interface), alongside sendQuery — NOT a separate ISteerable. Binds: every registered provider (claude, codex, grok, omp, deepseek, pi, opencode, copilot, qodercli), the executor, the spec's CAP-5. Prevents: a per-provider steer capability flag that lets a provider silently opt out; a side interface only some providers implement. Rule: a provider that cannot deliver a steer AT ALL (even by queue-and-flush) is not a qualified Archon provider. This DELIBERATELY REVERSES AGENTS.md's ISP guidance ('do not add unrelated methods to an existing interface — define a new one') and the spec's CAP-5 'defaults to unsupported'; the product owner ruled steering a core provider responsibility. Spec + provider-steering-matrix must be updated via bmad-spec to match.
- (decision) AD (per-Impl transport strategy, ratified): the steering CONTRACT is uniform on IAgentProvider; the MECHANISM is per-Impl. claude = streaming input + interrupt() (immediate or tool-boundary, echoes uuid); omp = RPC steer between tool calls; deepseek = queue-and-flush in DeepseekAgentProviderImpl (concurrent prompt is refused -32602, so the Impl holds the message and flushes when the in-flight prompt returns); codex = turn/steer if the pinned TS SDK exposes it, else queue-and-flush; grok = hooks pre_tool_use, else queue-and-flush. Binds: each provider package. Prevents: modelling steering as interrupt-first (matrix convergence: 'deliver at the next boundary' is the baseline, immediate interrupt is the special case). Rule: queue-and-flush is the UNIVERSAL FLOOR — every provider can send a follow-up prompt, so no provider is disqualified by transport today. deepseek is NOT dropped.
- (decision) AD (steer granularity, honest, ratified): boundary granularity varies and queueing does not erase it. Intra-turn boundary providers (claude, omp) inject between tool calls WITHIN the running turn; queue-and-flush providers whose transport refuses concurrent input (deepseek, and codex/grok when they fall back) flush only at TURN-END — the agent finishes its current (possibly wrong) work, then reads the correction. Both are non-interrupting mid-turn steer. To cut wrong work IMMEDIATELY on a coarse-boundary provider, the operator uses Stop (CAP-2). The two paths are complementary; neither pretends to be the other.
- (decision) AD (delivery confirmation is a declared best-effort axis, ratified): queue-and-flush solves 'the message arrives'; it cannot manufacture an echo id. Only claude echoes the caller-set uuid, so CAP-6 'delivered' is reachable on claude alone today. Binds: CAP-6, the UI status chip. Prevents: making delivery-confirmation a mandatory interface function (which would disqualify every provider except claude). Rule: confirmation is a declared capability, best-effort — the UI shows 'sent' for every provider and 'delivered' only when the provider echoes the stamped id. This REPORTS arrival; it is not a delivery mechanism and is never a disqualifier. The spec already carries CAP-6 this way; no change there.
- (note) Spec follow-up owed: provider-steering-matrix.md currently reads deepseek as 'No' for steering — that measured the raw concurrent-prompt acceptance, not the contract. Under the ratified model deepseek is 'queue-and-flush, boundary = turn-end'. The matrix's 'two yes / one no / two unknown' framing and CAP-5's 'defaults to unsupported' both need rewriting via bmad-spec so spec and architecture do not diverge.
- (decision) AD (fork 4, Kevin): a mid-turn Send reaches the running provider through an in-process live-handle registry keyed (runId, nodeId) -> steer handle; the server looks up the active session and calls Impl.steer() directly. Binds: CAP-5, the web-dispatch executor (api.ts:3200, in the server process), the operator-row writer. Prevents: routing mid-turn through the 10s DB poll (would make it no faster than the stop path, defeating its purpose). Rule: low-latency mid-turn is in-process only; a detached (--detach) run has no in-process handle and FALLS BACK to the stop-and-resume path (the DB-durable marker still reaches it). The operator transcript row is still appended through the store (single-writer, fork 2), not written by the server. This settles open-question #2 for now: mid-turn does not reach detached runs; stop does.
- (direction) Kevin correction, SUPERSEDES the 'steer method on IAgentProvider' framing in the three fork-3 decision entries above: there is NO 'steer' primitive. Sending to a running agent IS a normal prompt send (CAP-1: 'like chatting with an agent normally'). IAgentProvider does not grow a steer() method.
- (decision) AD (REVISED steering model, ratified): the operator message is a NORMAL PROMPT directed at the node's running session — not a distinct steer primitive, and IAgentProvider gains no steer() method. Binds: IAgentProvider, the executor, every provider Impl. Prevents: inventing a parallel steer channel; a capability that lets a provider opt out of receiving a message. Rule: (1) UNIVERSAL FLOOR — the executor holds a per-running-node inbound-prompt queue; when the current turn ends, the queued message(s) are the next sendQuery (an ordinary prompt send). Every provider satisfies this, so none is disqualified; deepseek is simply 'flush at turn-end'. (2) MID-TURN ACCELERATION — where a provider's sendQuery runs in a streaming-input mode that accepts additional user messages mid-turn (claude; omp via RPC), the same queued message arrives before turn-end. Same prompt, earlier — not a different operation.
- (decision) AD (REVISED interface delta, ratified): the ONLY IAgentProvider change is that sendQuery's INPUT may be a stream (async iterable) instead of a one-shot string, for providers that inject mid-turn (claude: 'string -> async iterable' per research). This is still sending prompts on an open channel, NOT a steer method. Providers that do not inject mid-turn keep the existing one-shot sendQuery signature and are simply re-called by the executor at turn-end. Binds: providers/src/types.ts IAgentProvider.sendQuery. Prevents: a new named primitive; a fat interface method only some providers implement.
- (decision) AD (REVISED fork 4, ratified): the in-process registry keyed (runId, nodeId) holds the node's INBOUND-PROMPT QUEUE and session handle, not a 'steer handle'. Server Send enqueues the operator message(s) there; the executor (same process, web dispatch) delivers them — mid-turn if the provider's open sendQuery stream accepts them, else as the next sendQuery at turn-end. Detached runs have no in-process registry entry and fall back to the stop-and-resume path (DB marker). The operator transcript row is still appended through the store by the executor (single writer, fork 2).
- (note) Reconcile vs spec: 12/14 constraints have a spine home; 2 UX-owned constraints correctly absent (finished-node dock, stop-is-not-undo). Two GAPS to close in triage: (a) 'stop and resume are two gated steps — wait for runtime ready before resuming' (aion race) — fold into AD-4; (b) 'a steer delivery must not emit a turn-start event' (a stray one opens a phantom turn boundary and corrupts the transcript record) — new AD. One superseded constraint: 'provider support defaults to unsupported' is replaced by the universal-queue-and-flush model; owed to bmad-spec.
- (decision) AD-4 extended (reconcile gap): stop and resume are two gated steps — Send waits for runtime-ready before resuming (aion race). AD-11 added (reconcile gap): a delivered message emits NO turn-start event — a stray one opens a phantom turn boundary that corrupts the transcript record the read half projects from; adapters that cannot inject without signalling a new turn fall back to turn-end flush. Capability map: CAP-3/4/5 now cite AD-11; fixed a duplicate AD-10 in the CAP-4 row (now AD-10 + Track A/AD-10).
- (event) Reviewer Gate returned: 3 lenses, all DONE_WITH_CONCERNS. Verified the load-bearing findings against source before acting (not taken on faith).
- (note) CRITICAL/HIGH findings, source-confirmed: (1) AD-4 targets the wrong site — the poll does abort();break; (dag-executor.ts:2325) and falls to the INLINE check at :3123 which records node_failed unconditionally; it never reaches the classifier :3386 or pauseOnAskHuman :3339 (those are in the catch, for a THROW). Fix: the poll sets an operator-stop flag on a marker match; the inline :3123 site AND the :3386 classifier both branch on it to the pause ending, never node_failed. (2) A marker-stopped node has NO pending_interactions row, so the Ask re-entry predicate (:1892: kind=ask AND status=answered AND node_id match AND epoch) never fires — steering needs its OWN re-entry. (3) A stopped run is 'paused' = DURABLE, so AD-9's 'server death kills the run, memory is safe' premise is FALSE for the stop path; the queued messages must live in the durable marker, not memory. (4) resumeWorkflowRun (workflows.ts:932) does not clear a stop marker and retry_epoch does not bump on resume, so the marker-aware poll re-fires -> stop loop; steering-resume must clear the marker atomically. (5) The stopping/stopped state has no core producer; shells reading metadata violates TrackA/AD-1 — needs a projector.
- (note) MEDIUM findings: (6) steer authorization silent — restore HITL/AD-7 'only workflow_runs.user_id may mutate' for Stop/Send. (7) no AD makes the executor write the 'interrupted' lifecycle row Track A renders. (8) CAP-4 ship-ordering: operator rows need the transcript spec's render support first. (9) mid-turn message stranded by a Stop — ownership/transfer rule between browser draft box and in-process registry. (10) retry_epoch is run-scoped not attempt-scoped -> AD-8 can't tell loop iteration N from N+1.
- (note) WEB-VERIFY settled two open questions: codex TS SDK turn()/steer() REFUTED across 0.144.5/0.154.0/alpha0.155 (steer is app-server protocol only) -> codex is firm queue-and-flush, close the open question. claude streaming-input + interrupt() ALREADY in pinned 0.3.209 (interrupt since 0.3.205, AsyncIterable input <=0.3.142); only CAP-6 uuid pre-result echo needs >=0.3.246 -> Stack table over-broadened the claude upgrade, reword. codex-sdk is a caret ^0.144.5 (floats to 0.154.0), not an exact pin. omp RPC steer + ACP-implicitly-cancels CONFIRMED against source.
- (event) Triage complete. All reviewer findings resolved in the spine: AD-3 (marker now carries durable messages), AD-4 (rewritten to the correct inline :3123 + classifier :3386 sites via operatorStopRequested flag), AD-5 (plain prompt not resumeInteractions), AD-6 (registry = mid-turn/in-process only), AD-8 (attempt-scoped guard), AD-9 (stop-path durable, premise corrected), +AD-12 steering re-entry, +AD-13 stopped-state projector, +AD-14 interrupted lifecycle row, +AD-15 steer authorization; HITL/AD-6 inherited row corrected; Conventions gained the one-message-one-owner transfer rule + authorization; Stack reworded per web-verify (codex queue-and-flush firm, claude upgrade scoped to CAP-6 >=0.3.246); codex open question closed NO. Lint 0, 15 ADs sequential, tables + mermaid valid.
- (event) spine finalized. 15 ADs, reviewer gate passed (3 lenses, all findings resolved), walkthrough.html built (Kevin chose spine+walkthrough). Owed follow-up: bmad-spec update so SPEC.md + provider-steering-matrix.md match the reversed model (steering is a normal prompt on IAgentProvider / queue-and-flush universal / codex settled NO / CAP-5 no longer 'defaults to unsupported').
- (event) Advisor post-finalize review applied: AD-3 (stop detection must be timer-driven via withIdleTimeout precedent :2298, not the chunk-bound poll — silent long tool call otherwise defers the stop; Stop route writes only the marker; natural completion clears a stale phase:stop marker), AD-4 (the EXECUTOR pauses at the inline check like ask-human, not the Stop route), AD-13 (stopping/stopped are PROJECTED on the projector output type, not persisted enum additions — persisted status unchanged), AD-2 (claude AsyncIterable x HITL/AD-6 host-abort resume is UNVERIFIED -> Deferred + spike), AD-15 (Stop/Send are NEW routes /nodes/:nodeId/stop|send, resume not extended; stale attempt key -> 409 per HITL/AD-7). Lint 0.
- (decision) AD-12 marker lifecycle corrected (advisor, post-final): the executor CONSUMES the steering marker at re-entry (read->deliver->clear as first act, before opening the sendQuery stream), never at the resume-flip. Rationale: re-entry runs only after status=running; on a detached run the durable marker is the SOLE channel to the child process, so clearing at the flip would erase the messages before the executor reads them (message-loss bug). Re-fire still prevented: clear precedes the stream+timer, and the timer abort is scoped to phase:'stop' only (phase:'send' = deliver, not stop). Also amended AD-3 cross-ref clause.
- (event) walkthrough.html refreshed to match final spine: SVG timer-label, AD-3 (stop-route-only + timer + natural clear), AD-12 (executor-consumes-at-re-entry fix), AD-15 (routes + 409), AD-2 (host-abort spike note), AD-4 (executor-pauses), AD-13 (projected-not-persisted), AD-9 card restored (was missing; 15/15), Deferred gained the claude-streaming spike row. Lede count reconciled to 15 decisions / 6 gate-fix.
- (event) RE-GATE (post-AD-12-fix) ran 3 lenses; owner (Kevin) chose FULL PLAN triage. 1 CRITICAL + 5 HIGH all source-verified; 6 blockers collapse to 3 AD amendments + one-line clauses; H3/A8/A11 to Deferred.
- (decision) AD-3 re-keyed per node (closes A1 CRIT / A2 / A3). Binds CAP-2,CAP-3,storage. Prevents: A1 one run-level marker with a single node_id clobbered when two concurrent siblings (HITL/AD-2 keeps siblings streaming) are stopped, or a siblings natural node_completed wipes anothers stop; A2 re-entry with no session home (mapAnsweredAskResume:1915 reads session only from a pending row; persist_session opt-in:9305) resumes a FRESH session, CAP-3 dead; A3 jsonMerge diverges by dialect (postgres.ts:327 shallow || vs sqlite.ts:1155 deep json_patch) and drops arrays. Rule: metadata.steering becomes a per-node map keyed by node_id, each record {attempt,phase,messages,provider_session_id}; each node record written WHOLE (replace, never merge); clear is a KEY DELETE (jsonb minus / json_remove), never merge-null (no phantom key); Stop writes {phase:stop}; executor enriches provider_session_id at the inline abort; concurrent Send CAS on (attempt,phase) else 409; natural completion deletes only its own key. Still marker-in-workflow_runs.metadata (Fork 1) — per-node refinement, NOT a reversal.
- (decision) AD-12 lifecycle corrected again (closes H1,H2,A5,A9). Prevents: H2/A9 clear-before-provider-accepts loses the correction on a crash and can write a false CAP-4 row (phase-scoping alone already prevents re-fire, so the early clear only costs durability); H1/A5 the steering consume ran only off the named steering-resume caller, so generic /workflow resume and the Ask CAS bypassed it, re-firing or orphaning a phase:stop node. Rule: the marker consume is part of EVERY executor hydration of a paused->running run, for every node bearing a marker, regardless of which trigger flipped status; ordering is read marker -> hand off to provider (sendQuery invoked without throwing, resuming provider_session_id) -> ONE transaction appends operator rows AND deletes the nodes marker key. Crash before the transaction leaves the marker, re-entry re-delivers: AT-LEAST-ONCE to the provider, stated as the accepted trade-off (SPEC never-discarded forbids at-most-once; exactly-once needs a provider ack that does not exist).
- (decision) AD-9 discriminator changes from RUN liveness to NODE liveness (fixes A4 conflation): a node can stream while its run is co-Ask-paused, so live-vs-durable turns on whether the NODE is still streaming in-process, not the run status. Stop-path carriage now also carries provider_session_id (AD-3).
- (decision) AD-15 Send gate (closes A4): Send requires the marker be phase:stop AND the node be pending (the stop has actually landed); otherwise 409. The UI stopping state maps onto that 409. Prevents a Send flipping phase to send before the <=10s timer reads stop, which would scope the abort out and lose the stop on a detached or co-Ask-paused run.
- (decision) Small clauses (owner-ratified full plan): AD-7 delivered is UNREACHABLE at the inherited pin 0.3.209, claude-only after >=0.3.246 (web-verify corroborated) -> new Deferred row H3. AD-6/AD-9 executor drains the registry inbound queue into the durable marker at the inline pause (names the owner for A7). AD-10/AD-5 sequencing: no operator row is written to a live transcript until the reader recognizes origin=operator, else it renders as agent text (A10) — reader change lands first. AD-13 both shells (Legacy+Console) host the dock and render stopping/stopped from the one projected state (cross-shell parity, M1); refetch reuses HITL/AD-7 triggers only if they fire on a workflow_runs metadata/status change, else steering adds one marker-write trigger since Stop/Send create no pending row (M2). AD-8 re-validate the attempt key AT DELIVERY, mismatch -> failed-send returns the message to the FRONT of the queue per EXPERIENCE (A6). AD-11 reworded: the STEERING LAYER emits no turn-start; precise turn-end-flush wiring vs newTranscriptAttempt:2286 deferred (A11).
- (decision) Stack/matrix WV-1: codex-sdk lockfile PINS 0.144.5 (caret has not floated); npm latest is 0.153.4 not 0.154.0. Surface conclusion (run/runStreamed only -> queue-and-flush, settled NO) unchanged. WV-2 (:2298/:3123 +-1-3 line seed drift) ignored as tolerable.
- (decision) Deferred rows added: H3 SDK bump (delivered needs >=0.3.246, revisit when HITL pin moves); A8 the interrupted glyph depends on transcript spec deriveOutcome (agent-history.ts:120) reading the tool card not the trailing status row — a dependency alongside AD-10; A11 turn-end-flush vs newTranscriptAttempt:2286 wants a source read before build.
- (event) begin spine amend-in-place (Update): AD-3,7,8,9,11,12,13,14,15 + conventions + stack + deferred + diagrams
- (event) spine amend-in-place complete; lint clean; AD ids stable 1-15; re-derive spec next
- (event) walkthrough.html refreshed to amended model (AD-3 per-node, AD-9 node-liveness, AD-12 deliver-then-clear, AD-15 Send gate, AD-7 delivered-at-pin, AD-8 revalidate, AD-13 M1/M2, AD-14 A8, AD-11 A11; SVG + Deferred + provider table). 15 cards, 8 gate-fix, tags balanced. Full-plan amendment complete; handing to advisor per owner.
- (decision) BLOCKER1 fix (advisor, post-amend): the Send gate must BRANCH, not apply unconditionally, or it 409s the CAP-5 send-without-stop path (same /send route enqueues the registry per AD-6). Prevents: an unconditional phase:stop+pending gate contradicting CAP-5/AD-6. Rule: /send dispatches by (this nodes marker phase, node liveness): (a) phase:stop AND node pending -> accept stop-path Send, CAS to phase:send; (b) phase:stop AND node still streaming -> 409 (stop not landed, UI stopping); (c) phase:send -> 409 (already sent, CAS); (d) no marker AND node running in-process -> registry enqueue (CAP-5/AD-6); (e) no marker AND not running in-process -> 409 stop-first (AD-6 detached fallback). Also control-states retry wording corrected: dock never Sends while stopping (control reads Queue); 409 guards a stale/racing client, not a retry loop.
- (decision) BLOCKER2 fix (advisor, post-amend): H1 still open — hydration consume handled only phase:send, so a phase:stop entry on a plain /workflow resume re-enters, timer reads stop, aborts, pauses, forever. Prevents: the re-fire loop on every resume of a stopped-but-not-sent node. Rule: on hydration, phase:send -> deliver then delete key; phase:stop (nothing to deliver) -> DELETE the key (a resume without a Send spends the stop; dock reverts to Queue, draft stays in browser). CONSEQUENCE to surface to owner: an unrelated resume (a teammate answering a siblings Ask) also spends an un-sent stop; the alternative (stopped node stays stopped across other resumes) needs a cancel-stop affordance control-states lacks, and reintroduces the per-node-suspend Kevin already rejected — so spend-on-resume is the consistent choice, flagged not silently chosen.
- (decision) SHOULD-FIX (advisor): AD-3 Send must READ-MODIFY-WRITE not whole-replace-drop — registry-drained messages (AD-9) already sit in the entry when the operator Sends, so Send reads the entry, APPENDS after the drained messages, and rewrites the whole entry; the CAS on phase:stop makes the read-modify-write safe. NIT: SPEC open-question Settled-NO line still said 0.154.0/not-an-exact-pin — correct to lockfile pins 0.144.5, latest 0.153.4.
- (event) Advisor post-amend check found 2 blockers (B1 unscoped Send-gate killing CAP-5 = advisor's own gate wording; B2 H1 still open on phase:stop hydration) + should-fix + nit. All fixed across spine + 3 companions + walkthrough; lint 0; 5 gate sites branch; both phases handled; residuals clean. Re-gate proposed to owner.
- (decision) FUNDAMENTAL REFRAME (owner Kevin, aion-verified) — SUPERSEDES the entire durable stop-and-resume design (AD-3 marker, AD-4 pause, AD-9, AD-12 re-entry, refuse-resume, phase/CAS, one-stop-per-run, timer-detection, at-least-once ALL superseded). Two DISTINCT operations must never be conflated: (1) Cancel node = the EXISTING teardown/abort path, stops whole node+agent, keep as-is; (2) Steer = interrupt only the agents CURRENT generation via the providers own primitive (claude query.interrupt() / codex turn/interrupt; or soft-inject without interrupting via codex turn/steer / claude streaming-input) — provider SESSION STAYS ALIVE, operator message continues the SAME session, NODE NEVER pauses/resumes, stays running throughout. aion source verified: interrupt = control frame to the persistent CLI subprocess ending only the turn, not the session; cancel-for-good = separate teardown. My old model wrongly cast steering-stop as a run pause (= conflated with Cancel), which forced all the durable machinery; every HIGH finding from the 3 re-gate rounds lived in that path and dissolves here.
- (decision) Clean-model invariants: steering is IN-PROCESS/IN-SESSION only for v1 (the registry holds the live handle; the process receiving the operator message is the one holding the live session). Detached-run steering + surviving-restart are DEFERRED (server crash mid-steer loses that steer, run resumes normally — like aion). NEW engine behavior: a steered node runs MULTIPLE provider turns on one live session until the agent finishes with nothing queued — rides the existing session-resume seam (structured-output re-ask already re-invokes sendQuery with attemptResumeId on the same session, dag-executor.ts:2290). Steering never uses the abort->node_failed path (that is Cancel). Kevins earlier one-stop-per-run answer is now MOOT (no durable pending-stop state exists to limit).
- (event) spine REWRITTEN to clean in-session model: 11 ADs (AD-1 acts-on-live-agent-not-node-lifecycle, AD-2 interrupt=provider-primitive-or-stream-abort-session-alive, AD-3 send-ordinary-prompt-soft-inject, AD-4 multi-turn-one-session, AD-5 in-process-only, AD-6 operator-row, AD-7 no-turn-start, AD-8 delivered-gated, AD-9 projected-generating-state, AD-10 cross-spec, AD-11 send/interrupt-routes-no-durable-gate). Durable design fully superseded. lint next.
- (decision) AD-2 amend: steering sets operatorInterrupt flag BEFORE abort(); flag does double duty — routes the inline :3123 abort check back into the turn loop (not node_failed) AND marks turn-end as interrupted so the loop waits for the operator. Interrupt racing natural turn-end = no-op, spent. Prevents: a steering abort falling through to node_failed (verified :3123 if(signal.aborted && !nodeIdleTimedOut)->node_failed). codex re-run verified: codex.resumeThread(sessionId) providers/src/codex/provider.ts:1006.
- (decision) AD-3 amend (fixes advisor BLOCKER): delivery TIMING is the operator's choice; only soft-inject is provider-gated. Three paths, interrupt orthogonal: Queue (default, any provider, delivered turn N+1 at natural turn-end, non-interrupting); Send now (any provider, only when idle-after-interrupt, ordinary prompt->turn N+1 now); soft-inject (CAP-5, claude streaming at pin / codex turn-steer deferred, mid-turn no interrupt). Prevents: a branch that holds a message 'because the provider can't do better' (conflating transport limit with operator intent) — interrupt-then-deliver is reachable on EVERY provider. Reconciled with advisor: withdrew 'never wait'; kept Kevin's ratified Queue default (control-states.md).
- (decision) AD-4 amend: turn-end has TWO causes, discriminated by operatorInterrupt flag. Natural end: queue pending->turn N+1, empty->node completes. Interrupted end: partial result (no schema-valid output_format) must NOT be validated / node_completed / DAG-advanced; enter idle-await -> queue pending->turn N+1, empty->wait bounded. Re-ask precedent (:2290) imperfect: re-ask fires on validation miss from a recoverable state, steering on interrupt from a state the executor treats as terminal (:3123) — operatorInterrupt branch is the new code. Prevents: interrupt-with-empty-queue completing the node immediately; validating partial output into downstream garbage.
- (assumption) AD-4 idle-await bound [ASSUMPTION, product call for Kevin]: while idle-after-interrupt with empty queue, the node's EXISTING idle timeout runs from the interrupt moment; on expiry node FAILS ('interrupted by operator, no redirect received'), operator resumes to retry. Not completes (partial output), not indefinite (AD-5 in-process only, unbounded in-memory wait fragile across restart). Alternative = approval-gate-style wait-forever. Ask Kevin: Stop-and-walk-away -> fail-after-timeout, or wait-forever?
- (decision) AD-9 amend: name the sub-state inside node=running: generating | idle-after-interrupt (the ghost of rejected stopping/stopped, relocated INSIDE running as a projected typed field, no persisted enum). idle-after-interrupt is where Send now is offered (AD-3). AD-11 amend: the registry queue absorbs races (Send-during-interrupt, Send-to-dead-turn) -> NO 409; only a NOT-running node refuses — node finished->409 (draft stays, matches control-states finished row), no live handle (detached)->not steerable here. Prevents: a 409 where the queue already absorbs the race; a phase/marker gate this model does not have.
- (decision) CORRECTION (advisor round 2): AD-4 no longer auto-fires the queue on interrupt (that erased the ratified Send now moment + contradicted the spine's own in-session mermaid). Interrupted end ALWAYS enters idle-await whatever the queue holds; drains ONLY on operator Send now (flush queued+typed in order) or the bound. Natural end keeps auto-drain (Queue's contract). AD-11 race: a Send during an in-flight interrupt WAITS for Send now (not auto-fire); teardown is the last queue check -> 409 finished so nothing reads sent-but-never-delivered. AD-1 softened: a send/interrupt never fails the node; the only failure is operator abandonment past the idle bound.
- (decision) CORRECTION (advisor round 2): AD-4 idle-await bound mechanism fixed. It does NOT reuse withIdleTimeout/nodeIdleTimedOut — that path COMPLETES the node (:3118, guarded out of fail at :3123), which is complete-with-partial garbage. Instead: fresh timer, configured duration only, explicit FAIL branch. Three outcomes named for Kevin: fail (chosen) / wait-forever (approval-gate style) / complete-with-partial (do-nothing bug, rejected).
- (decision) OWNER DECISION (Kevin): idle-await bound = FAIL after 30 minutes. Resolves the AD-4 [ASSUMPTION]. When operator interrupts then sends nothing, a fresh 30-min timer expires and the node fails ('interrupted by operator, no redirect received'); operator resumes to retry. /workflow cancel during idle-await still ends the node, racing the timer. Rejected: wait-forever, complete-with-partial.
- (decision) CORRECTION (advisor round 3, source-verified): AD-2 abort seam corrected. nodeAbortController (:2209) is ONE-SHOT and belongs to Cancel (feeds sendQuery :2221, re-ask loop guards on it :3031, idle/poll abort it :2298/:2325, :3123 fails on it). Steering CANNOT reuse it (one-shot would kill turn N+1's sendQuery + block re-ask). Steering interrupts via a FRESH per-turn signal; sendQuery gets AbortSignal.any([nodeAbortController.signal, perTurnSignal]); each new turn gets a fresh per-turn signal while node-level persists -> this is what enables multi-turn. :3123 is NEVER tripped by steering (Cancel-only, byte-for-byte). operatorInterrupt = which-signal-fired discriminator. idle-await watches node-level signal so /workflow cancel ends node from idle (chunk poll :2312 dormant, no stream). Also fixed SPEC self-contradiction: 'all of steering needs executor in-process' (was 'only mid-turn'). Polish: control-states 'waits until Send now'; matrix omp interrupt downgraded to stream-abort; spine mermaid Q->IDLE edge added.
- (event) RE-GATE round (3 lenses on clean spine): web-verify PASS (all source anchors + SDK behavioral claims verified; only codex npm-latest 0.153.4->0.154.0 + 1-2 line drift). rubric PASS-WITH-CONCERNS (2 HIGH). adversarial 9 pairs (3 severe). Consolidated 6 real gaps; advisor validated triage + corrected the fix approach (placement, not turn-id machinery).
- (decision) GATE-FIX (round 4, advisor-guided): (1) canReask (:3032) must also stop on operatorInterrupt else interrupted partial auto-re-asks [adv-H1]; (2) validation skipped on interrupted turn; (3) operatorInterrupt branch placed immediately AFTER :3124 Cancel check -> Cancel dominates by position [adv-H5]; (4) flag is per-turn, reset at turn N+1 -> no stale-flag mis-route [adv-H2]; (5) discriminator = 'turn emitted a result', not the flag alone -> a naturally-completed turn is never mis-routed to idle-await [rubric-M1]; interrupt aborts per-turn signal synchronously (single-threaded, no dead-controller). idle-await Cancel: NOT 'watch the signal' (nothing fires it, no stream) -> idle-await runs OWN timer-driven getWorkflowRunStatus poll at CANCEL_CHECK_INTERVAL_MS (:682) reusing shouldContinueStreamingForStatus (:700-702); idle-await RESOLVES EXACTLY ONCE (Send now | cancel-poll | 30-min timer) [rubric-H2, adv-H6]. 30-min fail=node_failed, resume=fresh session (context gone) - stated so builder doesn't preserve. Operator row +operator_user_id +message_id (3 additive metadata fields, no migration) [adv-H3]; on ANY terminal client reconciles sent-ids vs message_id -> unmatched 'Never sent' [adv-H7]; global order = registry receipt order, no per-node lock [adv-H3]. soft-inject spike-gated (claude AsyncIterable+resume seam), v1 floor = interrupt+Queue [rubric-H1]. AD-9 interrupting = UI transient not projected [adv-H4]. AD-5 detached: no 'resume' while running [adv-H8]. omp ordering = Consistency Convention row [rubric-M3]. Line anchors :3124/:3032/:2223/:3387; codex latest 0.154.0. Lint 0.
- (decision) GATE-FIX round 4 micro (advisor verify of AD-2/AD-4 seam): (a) mermaids relabeled — idle-await cancel exit is 'via idle-await status poll' not 'node-level signal' (that signal is dead in idle-await); added the cancel-poll exit edge to the spine in-session diagram. (b) undelivered-queue reconciliation runs ONLY on the node terminal event (node_completed/node_failed, HITL/AD-7 refetch after last write), NEVER a live refetch — else a Cancel mid-flight mis-marks a delivered message 'Never sent' and duplicates on resend. (c) result discriminator cites provider-normalized msg.type==='result' (:2533); stream-abort yields none. (d) cite :3030 comment as canReask design intent. (e) note: idle-await 30-min timer keeps running through a sibling Ask-pause (shouldContinueStreamingForStatus true for paused) — Kevin's 30-min choice covers it. Lint 0.
- (decision) AD-4 amended in place (no renumber): added one clause distinguishing the 30-min idle-await fail — the in-process owner failing its OWN node on a bounded timer it actively holds, same class as the existing idle timeout — from AGENTS.md 'No Autonomous Lifecycle Mutation' (which governs a process judging non-terminal work started by an unknowable OTHER party). Pre-empts a reviewer objection; lint 0 findings after.
- (decision) AD-4 amended (no renumber): the 30-min idle-await timer is an INACTIVITY timer (SC 2.2.1, owner-chosen reset-on-activity) — a debounced authorized composing-keepalive over the Send/Interrupt route family (AD-11) re-arms it WITHOUT resolving idle-await; still resolves exactly once (Send now/cancel-poll/timer). Adds a re-arm input, not a resolution. Driven by the bmad-ux accessibility Reviewer Gate.
