---
scope: Workflow run view HITL (spec-workflow-run-view-hitl): mid-turn AskHuman + node-centric run view on legacy + console
purpose: build-substrate
altitude: feature
paradigm: ports-and-adapters
updated: 2026-09-05T21:52
---

- (constraint) INHERITED spec: ONE pending_interaction envelope for all HITL; unified render; responses SPLIT typed - ask.answer(answers[]|decline) keyed request_id vs permission.confirm(intent) keyed call_id; separate endpoints, never conflated
- (constraint) INHERITED spec: build BOTH surfaces (legacy WorkflowExecution.tsx + /console experiment); HITL engine+contract surface-agnostic in backend; front-ends are thin renderers. Console isolation rule stands (no production web imports)
- (constraint) INHERITED spec: Ask form built FULLY; Permission v1 = envelope/type-contract ONLY (dormant under skip-permission posture); per-variant cards deferred until live activation source
- (constraint) INHERITED spec: AskHuman tool is the MANDATED ask channel (system-prompt + tool-description); NEVER prose-sniffing (NL is not a wire format); prose-ask residual risk accepted
- (constraint) INHERITED spec (spike-verified): awaiting trigger = intercepting the AskHuman TOOL INVOCATION, not SDK requires_action (never fires v0.3.209). Claude durable = END turn on ask, resume delivering answer as NEW USER MESSAGE (no tool re-issue; synthetic result needs no scrubbing). Pi durable = Agent.continue() + appended ToolResultMessage. Same-process seamless = internal optimization only
- (constraint) INHERITED spec: durable teardown CANONICAL - persist pending atomically before/with turn teardown, never pin an in-memory promise; purge pending on run cancel/fail; recover half-committed on restart; resume failure FAILS node loudly with answer retained
- (constraint) INHERITED spec: ONLY run starter may answer; no resolvable starter identity (nullable workflow_runs.user_id) => fail fast, never unanswerable awaiting; idempotent keyed (sessionId, tool_use_id); first answer wins; late submit for resolved key = no-op
- (constraint) INHERITED spec: blocked node WAITS INDEFINITELY (no timeout/auto-default - no autonomous lifecycle mutation); concurrency: card keyed (nodeId, tool_use_id), node awaiting until ALL its asks answered, run-level awaiting clears when LAST node resolves; decline delivers declined to agent, AGENT decides outcome
- (constraint) INHERITED spec: run-start capability gate - ask-capable workflow on provider without in-process ask channel (Codex/Grok/OpenCode/Copilot v1) REJECTED at run start, fail loud; only Claude + Pi produce Ask in v1
- (constraint) INHERITED spec: node-type taxonomy - agent room + ad-hoc HITL eligible = command/prompt/loop ONLY; route_loop = deterministic controller; loop_group = container; bash/script = stdout only; approval/plannotator_gate = declared gate IS the interaction; workflow = child-run link
- (constraint) INHERITED spec: KEEP the Logs tab; graph is an ADDITIONAL per-node log-history entry point (both surfaces); no workflow YAML authoring-language changes; Claude SDK version pinned/guarded as regression guard (spike basis v0.3.209)
- (constraint) INHERITED spec OQ#4: per-node agent-stream history does NOT exist today - streamed assistant prose is node-blind (per-run conversation rows, no nodeId; SSE no replay); per-node tool_called/tool_completed events + final node_output DO exist. CAP-1/CAP-2 require NEW node-tagging or per-node transcript store
- (direction) PARADIGM ratified from spec + AGENTS.md package rules: ports-and-adapters - surface-agnostic HITL core (envelope, store, durable resume) behind narrow ports (IWorkflowStore/WorkflowDeps/providers-types); providers + web surfaces are adapters. Coaching scope = the forks the spec left open
- (decision) AD candidate (user Q1=A): node lifecycle gains an explicit awaiting status; pending interactions are a NEW mutable table (working name pending_interactions) keyed (run_id, node_id, tool_use_id) with lifecycle pending->answered/purged; engine reaches it ONLY via IWorkflowStore/WorkflowDeps extension (package boundary); run-level reuses existing paused-run machinery
- (constraint) CODE-GROUNDED: no per-node pause exists - run-level status enum pending/running/completed/failed/cancelled/paused; node state is EVENT-SOURCED (node_started/node_completed/tool_called keyed step_name), no status column; pending gate state = metadata.approval ApprovalContext SINGLE-SLOT per run
- (constraint) CODE-GROUNDED: approval-gate resume template - pausing node returns completed WITHOUT writing node_completed so resume re-enters it (executeApprovalNode); interactive_loop/child_workflow keep node incomplete + carry sessionId; between-layer halt when status != running
- (constraint) CODE-GROUNDED: in-flight sibling nodes KEEP STREAMING when run paused (shouldContinueStreamingForStatus) - pause never tears down healthy turns; nativeTools injection is CHAT-ONLY today, workflow nodes get none - dag-executor must inject AskHuman per-node; Pi provider is one-shot prompt+dispose, no continue() path yet
- (decision) RECONCILE Q1=A vs code: metadata.approval single-slot cannot represent N concurrent pending interactions => dedicated pending_interactions table CONFIRMED; ApprovalContext remains for declared gates, AskHuman does NOT ride it
- (decision) AD candidate (user Q2=A): awaiting uses RUN-LEVEL pause reuse (approval-gate style) - asking node tears down its turn + persists pending_interactions row(s), run -> paused; in-flight siblings finish streaming, next layer halts; run resumes ONLY when the LAST pending interaction resolves; resume = hydrate skip-completed + re-enter the awaiting node (no node_completed, interactive_loop precedent) with session restore + answer injected
- (direction) DEFERRED candidate: true per-node independent scheduling (branch continues past a sibling's ask) - revisit when a real use-case shows long parallel branches with frequent asks
- (decision) AD candidate (user Q3=B): per-node agent-room history lives in a NEW dedicated table (working name workflow_node_messages: run_id, node_id, seq, kind[text|tool|status], payload, created_at), written by the executor as it streams, cascade-delete with the run (usage_ledger precedent); node room reads ONE store with ONE ordering; existing per-run prose writes to messages stay untouched
- (decision) AD candidate (user Q4=A): run-graph renderer = ONE shared pure-TS module (layout + edge routing + taken-path; no React/DOM/API imports) under packages/web/src/lib/run-graph/, explicitly sanctioned as the ONE allowed cross-surface import (console isolation exception, frozen API + own tests); each surface wraps its own React/SVG shell
- (decision) AD candidate (user Q5 ratify AD-5..8): AskHuman NativeTool owned by @archon/workflows, dag-executor injects per agent node; Claude resume = new user message, Pi = continue() (new provider path); dedicated interaction events+REST (never reuse approval_pending / metadata.approval); two new tables additive-only mirrored SQLite+Postgres; awaiting is NOT a new run status (UI projection of paused + pending>0); nodeStateSchema gains awaiting
- (event) coaching complete; distill spine
- (decision) GATE fix from spec-reconcile: AD-9 AskHuman invocation is the only ask channel (no prose-sniff, residual prose-ask accepted, no AskUserQuestion, console Reply is not HITL); AD-5 CAP-7 ratified as allowed_tools-only (not reject-all-Codex); AD-1/2 sessionId in CAS key + node stays awaiting until ALL its asks resolve; AD-7 Ask questions[]/Other/one-POST; conventions for Logs IA, awaiting tone, unified-render=envelope-not-React
- (decision) GATE fix from version-reality: Claude stack pin lockfile exact 0.3.209 until AD-6 spike; Pi continue is session.agent.continue() on pi-agent-core 0.80.6 not AgentSession.continue(); AD-6 Archon-visible contract kept (new user message / ToolResultMessage) with SDK-how (tool_deferred vs host abort; reopen-after-dispose) deferred as required implementation spikes
- (decision) GATE fix from incompatible-units + rubric: one pendingInteractionSchema (tool_use_id IS request_id/call_id, GET run embeds pending_interactions); pauseWorkflowRun approvalContext optional and Ask never writes metadata.approval; Ask pause idempotent; operations CAS is the only Ask resume owner; AskHumanAwaitingError in providers/types wrappers reject sendQuery; converters accept questions array; one node-state projector including awaiting; re-enter ALL awaiting nodes; resumeInteractions provider-owned; provider_session_id is session SoT; fail starter identity only when persisting an Ask; child Ask lives on child run
- (event) spine finalized
