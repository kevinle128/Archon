---
topic: Archon Source Control — legacy UI
updated: 2026-09-05T21:23
---

- (decision) Target surface: LEGACY web UI — the existing workflow-run screen (/legacy/workflows/runs/:id, beside Graph/Logs/Chat); explicitly NOT the /console experiment
- (event) fresh Create run; prior final run ux-Archon-source-control-2026-08-31 retained as reference only, not resumed
- (decision) Sources confirmed by user: SPEC bundle ONLY (_bmad-output/specs/spec-archon-source-control/: SPEC.md, brownfield.md, viewer-rules.md, architecture-diagrams.md, roadmap.md). Prior UX run ux-Archon-source-control-2026-08-31 deliberately excluded — fresh take.
- (decision) Working mode: FAST PATH — user delegated vision ('tuy ban' / up to you). Spines will be drafted with [ASSUMPTION] tags; user reacts on the draft. Creative tools skipped unless user names a surface needing a visual.
- (event) fast-path draft v1 of DESIGN.md + EXPERIENCE.md written from the SPEC extraction brief
- (assumption) A1 form-factor desktop-first (mobile out of v1); A2 commit rows expand/collapse inline; A3 history default = current branch recent-first + Load more
- (assumption) A4 CAP-6 copy variants drafted (container vs no-checkout); A5 empty-Changes copy, no auto-collapse in v1; A6 Reload CTA only when a retry could help
- (assumption) A7 badge hues M=amber(warning)/A=green(success)/D=red(error), letter-carried; A8 diff tints = low-alpha success/error bound at build; ligatures off in mono
- (event) user asked for a reviewable website mock; key-screens renderer dispatched to .working/
- (event) user reported diff view rendering unified (single-column); investigation: file on disk renders correct two-pane side-by-side (verified via live browser capture); user's tab was a stale render from an earlier build iteration — advised hard refresh
- (change) mock fix: panel divider was visual-only (col-resize cursor, no behavior) — implemented drag-to-resize (pointer capture, clamp 20-70%) + ArrowLeft/Right keyboard resize + role=separator, mirroring the real screen's ResizablePanelGroup; verified via synthetic-drag test (40% -> 60% exact) and live screenshot
- (decision) panel-split-default narrowed 40/60 -> 30/70 per user review ('left-panel hoi bi to'); panel is for scanning, viewer for reading. Token updated in DESIGN.md; mock updated + verified.
- (change) responsive fix: two-pane diff no longer uses a max-content table that overflowed the window. Each pane is now its own independently-scrolling column (overflow-x: scroll) so a long line cannot clip the opposite pane or the browser edge. Below 900px the layout stacks (lists above, viewer below; before-pane over after-pane).
- (decision) User accepted the key-screen mock (ok roi do). Locked from review: default split 30/70; divider drag-resize (20-70%) + ArrowLeft/Right; two-pane M-diff with independently scrolling columns (never overflow the window); below 900px stack lists-above-viewer and before-over-after.
- (event) Discovery visual review closed; ready to offer Finalize / Reviewer Gate
- (event) spines finalized — rubric walker findings resolved; mock promoted; status: final
