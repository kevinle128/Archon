---
topic: Archon Source Control tab — UX (DESIGN.md + EXPERIENCE.md), formalizing UX previously inline in PRD/architecture
status: complete
updated: 2026-09-01T00:36
---

- (event) Sources confirmed (all 4 core): prd.md, architecture.md, addendum.md, epics.md; IR report as supporting context. Working mode = Coaching path.
- (decision) Inherited from sources — Form-factor: web, desktop-first; a 4th tab in the workflow-run screen beside Graph/Logs/Chat.
- (decision) Inherited — UI system: existing Archon web (React 19 + Tailwind v4 + shadcn/Radix). DESIGN.md references/extends existing tokens; EXPERIENCE.md specifies behavioral delta only.
- (decision) Inherited — Stakes: internal tool, trusted operators, strictly read-only.
- (note) Inherited spec (to formalize): two regions (Changes above commit history), status-keyed viewer M=2-pane red/green diff, A/D=1-pane content; direction Now=HEAD→worktree, commit=parent→commit; manual Reload no polling; Empty state for no readable checkout / container run; large=Load-more stream, binary=inline image / hex / download.
- (note) Inherited security contract: checkout root server-derived from runId; client passes only a server-returned repo-relative path, realpath-validated; never the checkout root or an absolute/filesystem path.
- (event) Discovery started (Coaching path).
- (event) Subagent extraction complete over ALL 7 confirmed sources ('dùng cả' = 4 core + IR report + reconcile + review). Cited digest saved to .working/source-extract.md (supersedes the earlier memory-seeded inherited notes with sourced citations).
- (note) SOURCED decided — Viewer: single shared, status-keyed; M=2-pane red/green diff, A/D=1-pane uncolored; Now=HEAD→worktree, commit=parent→commit; M diff-only v1 [prd.md:115-119]. Stack D3: react-diff-view + highlight.js + react-resizable-panels + @tanstack/react-virtual + react-query; no Shiki/Monaco [architecture.md:35].
- (note) SOURCED decided — States: Empty 'no worktree / not available' by directory existence at read time (null path / dir absent / not-git / container), not run status [prd FR-8:172-181, arch:41]; loading skeleton+Cancel, metadata-immediate; large text Load-more ~256KB/2000 lines, >1MB stream+Cancel, >50MB download-only; binary NUL-in-8KB → images inline / else download+hex ~4KB; nothing blocks [addendum:52-55]. Interactions: click file/commit→shared viewer; manual Reload no polling; 'changed—Reload' affordance [ASSUMPTION] [prd FR-2/FR-6,:85].
- (note) SOURCED open gaps (coaching agenda): (1) viewer spatial placement/layout; (2) what resizable-panels splits + sizes; (3) cross-region selection model (commit file-list location, return-to-Now, breadcrumb); (4) history presentation (cols/order/virtualization); (5) file-list presentation (flat vs grouped/sort/truncate/counts); (6) empty-Changes vs FR-8 Empty; (7) Reload scope/placement; (8) changed—Reload form + divergence detection; (9) error/refusal states beyond Empty; (10) empty-state copy/icon/CTA; (11) loading skeleton/post-Cancel/Load-more increment; (12) download+hex-peek presentation; (13) snapshot-fallback vs live indication; (14) tokens/dark-mode/density (delegated to Archon system); (15) accessibility (kbd/focus/ARIA/color-blind-safe diff); (16) motion; (17) i18n/UI-copy.
- (event) Coaching agenda derived from sourced gaps; beginning Decision #1 = viewer spatial placement/layout.
- (decision) CORRECTION (authoritative = current source, supersedes extractor's stale reconcile:83): Empty state has TWO layered triggers, NOT directory-only. (a) D5 container gate — isolation-env provider==='container' via conversation→isolation_env→provider (status-agnostic getById) routes to Empty BEFORE any git read [architecture.md:41; epics.md:104-110 Story 1.1 AC]. (b) Host/worktree runs reach Empty by directory existence at read time (null working_path / dir absent / not-a-git-checkout) [prd.md FR-8:172-181]. reconcile-source-control.md:83 ('directory-existence only; do NOT gate on isolation-env') is SUPERSEDED by D5 for the container case. Run _status_ is still never the detector. UX implication: the two Empty entry-paths may warrant distinct copy (container-run vs no-checkout) — flag for empty-state copy decision.
- (direction) Kevin chose the wireframe route to settle layout. Per skill, dropping the anchoring A/B/C archetypes; eliciting his real inspection session (named-protagonist journey) + spatial mental model first, then rendering 2-3 Excalidraw wireframes DERIVED from his narration.
- (note) JOURNEY (Kevin, real inspection session): on opening the tab he looks at TWO things, together — (1) uncommitted Changes = which files the agent is currently modifying / adding; (2) commit history = what the previous workflow NODES did (which files each node updated/created). Implication: both regions visible simultaneously (not one-at-a-time); commits read as per-node work. Viewer placement / commit drill-down / return-to-Now not yet stated — to be surfaced via the wireframes.
- (event) Rendered 3-variant layout wireframe to .working/flow-source-control-layout-2026-08-31.excalidraw (141 elements, validated: valid JSON, all indices two-char). Variants derived from Kevin's journey (Changes + Commit-history-per-node visible together): V1 right-pane (VS Code), V2 bottom-pane (max diff width), V3 overlay/drawer. Open beats flagged in-canvas: selected-commit file-list location, return-to-Now.
- (note) RETRACTION (capture accuracy, supersedes entry-15 'both regions visible simultaneously; commits read as per-node work'): Kevin's wording was a SEQUENCE — 'đầu tiên' uncommitted Changes (which files the agent edits/adds), 'sau đấy' commit history — used to understand what PRIOR NODES did. He did NOT assert (H1) simultaneous on-screen visibility, nor (H2) one-commit-per-node / node-grouped history. H1 and H2 are unvalidated HYPOTHESES, not decisions. Wireframe patched: 'per NODE' labels -> plain commits + 'group per node? TBD'; added a HYPOTHESES banner flagging H1/H2 to validate separately from viewer placement.
- (decision) Kevin pointed at VS Code's Source Control UI (screenshot) as the target — RESOLVES Decision #1 = LAYOUT V1 (master-detail). Left 'Source Control' panel stacks, both visible + scroll (H1=simultaneous, now Kevin-confirmed via the image): (top) Changes = uncommitted files with status badges; (bottom) 'Graph' = the run branch's commit history AS A GRAPH (branch/merge topology lines, author + relative time). Right = an editor pane that opens the clicked file as a tab (breadcrumb path + content/diff).
- (decision) H2 RESOLVED (rejects per-node grouping): commit history = chronological commit GRAPH (VS Code/GitLens style topology), NOT grouped one-per-node. A commit row EXPANDS INLINE to its changed files (M/A/D). Resolves the two open beats: (a) a selected commit's file list renders INLINE beneath the commit row in the Graph; (b) return-to-Now = the Changes section persists at the top of the same panel.
- (assumption) ARCHON ADAPTATIONS vs the raw VS Code screenshot (read-only tool) — PENDING Kevin confirm: (1) DROP the Commit button/box and the inline stage/unstage/discard (+, curved-arrow, -) row actions — strictly read-only, no write surface; (2) status badges are M/A/D only (screenshot's 'U' untracked maps to A in Archon); (3) the Graph refresh control = the manual Reload (no 'Auto' refresh, no polling); the VS Code activity bar/chrome is not part of the tab — this renders as the run-screen tab content.
- (event) Decision #1 (layout) settled = V1 per Kevin's reference screenshot; pending adaptation confirm.
- (note) import: Kevin's VS Code Source Control screenshot (user-supplied visual reference). Raw image pasted in chat, binary not extractable to disk; described in imports/vscode-source-control-reference.md.
- (note) Refine per review — separate ESTABLISHED vs INFERRED from the screenshot. ESTABLISHED: VS Code-style left SCM panel (Changes over Graph, both visible) + right editor pane opening a file; Graph shows commit topology with the current commit expanded to its files inline; Changes sits above Graph. INFERRED (pending Kevin confirm, NOT proven by a static image): click-to-expand/collapse a commit, the exact return-to-Now interaction, and the read-only adaptations. Earlier 'H2 RESOLVED / beats resolved' entries: the spatial arrangement is established, but the interaction behaviors remain inferred until confirmed.
- (decision) Badge set = Cách 1 (Kevin): M/A/D only; new files fold to A (untracked-U and staged-A both display A); read-only has no staging so the U-vs-A distinction is dropped as non-actionable noise; confirms spec 'untracked→A'. Verified A/U: U=untracked/unstaged-new, A=staged-new, difference is git add [web: bobbyhadz, dev.to].
- (decision) Kevin explicitly: 'bỏ nút commit' → DROP the Commit button and the inline stage/unstage/discard (+, ↩, -) actions. Changes is a read-only list (click a file to view its diff). Consistent with PRD §5 (no edit/commit/write surface) + strictly read-only. #3 manual Reload (no auto/polling) follows FR-2. VS Code activity-bar chrome is not part of the tab.
- (event) Refined V1 wireframe rendered + indices normalized (a0..cZ, 108 elements, valid/ascending/unique): tab strip (Source Control active); LEFT read-only Changes (M/A/D badges, manual Reload, NO commit/stage/discard) over Graph (topology dots+lines, current commit expanded inline to its files); resize handle; RIGHT editor pane (file tab + breadcrumb + M two-pane red/green diff, 'A/D single pane no color'); Empty-state callout; 3 interactions marked 'PROPOSED — confirm'. File: .working/flow-source-control-v1-refined-2026-08-31.excalidraw. Supersedes the 3-variant sketch.
- (change) SCOPE CHANGE (per review): Kevin's reference is a commit-TOPOLOGY GRAPH (branch/merge lanes), a NEW UX delta beyond current specs — NOT merely formalizing the existing 'commit history region'. Current PRD FR-6 requires only a commit-history LIST (click a commit to inspect its M/A/D + diffs); architecture D3 names the diff stack (react-diff-view etc.) but NO commit-graph renderer / lane layout and no graph dependency. Choosing the graph REOPENS: PRD (FR-6 history as GRAPH not list), architecture (graph/lane renderer — custom SVG lanes vs a lib; new dep + feasibility/perf on long histories + virtualization), epics (Epic 2 stories). MUST go through PRD/architecture/epics reconciliation + dependency/implementation-feasibility review before READY. Open at reconciliation: full topology lanes vs simpler chronological commit LIST (cheaper, matches current specs).
- (note) Kevin said 'continue' → the 3 PROPOSED interactions provisionally accepted as the working model, pending his visual confirm on the exported PNG: (1) click file (Changes row or expanded-commit file) → opens in right editor pane; (2) click commit → expand/collapse its files inline in the Graph; (3) return-to-Now = Changes stays pinned at top (no separate back control). Not an item-by-item explicit yes; revisit if the PNG prompts changes.
- (event) Exported PNG of the refined V1 wireframe (offline SVG->canvas render; excalidraw CDN export failed headless). Visually verified faithful: tab strip, read-only Changes (M/A/D + Reload, no Commit), Graph (topology + current commit expanded), right editor M diff (red/green), Empty callout, 3 PROPOSED flags. File: .working/flow-source-control-v1-refined-2026-08-31.png (+ render.svg).
- (decision) Kevin chose: commit history = FULL TOPOLOGY GRAPH (branch/merge lanes, like the VS Code/GitLens screenshot), NOT a plain list.
- (decision) Kevin confirmed ('đúng') the 3 interactions — LOCKED: (1) click a file (Changes row or expanded-commit file) → opens in the right editor pane; (2) click a commit → its changed files expand INLINE beneath it in the Graph; (3) return-to-Now needs NO Back control — Changes stays pinned at the top.
- (note) Before finalizing the graph scope-change: scouting web deps + the existing 'Graph' (workflow DAG) tab to VERIFY real implementation cost/reuse — earlier 'needs new lib / reopens readiness' was UNVERIFIED inference (only 'specs omit topology' is verified).
- (change) COST VERIFIED (supersedes the earlier overstated cost in the prior 'change' entry): commit-topology graph = LOW cost, NOT expensive. Web already ships @xyflow/react ^12.10.1 + @dagrejs/dagre ^2.0.4 + @tanstack/react-virtual ^3.0.0 [archon/packages/web/package.json]; the run screen's own 'Graph' tab already uses React Flow [WorkflowExecution.tsx ~:861-877 → WorkflowDagViewer.tsx]. React Flow is layout-agnostic → commit-lane view reuses it with a ~50-150 line lane layout, or plain virtualized SVG, with NO new dependency and NO readiness reopen. Only real delta: the specced-but-unbuilt helper must return parent OIDs (git log %H %P). Minimal spec updates: PRD FR-6/§4.4 (list→graph, keep click+parent→commit) + epics FR6 mirror; architecture Stack note 'reuse @xyflow/react, no new dep' + log wire includes parents; epics Story 2.1 AC (lane graph incl. off-base commits + merge rendering), Story 2.2 unchanged; addendum log row +%H %P. Detail: .working/graph-cost-scout.md.
- (change) COST REFINEMENT (do not overstate 'LOW'): scout verified ONLY (a) NO new dependency (React Flow already present + reused by the run Graph tab) and (b) parent OIDs = the data delta. The commit lane-assignment algorithm through merges + keeping lane continuity across paged/virtualized history are CUSTOM and UNPROTOTYPED -> cost = TBD / medium-risk until a spike. Recommend a spike (lane layout + one paged/windowed boundary on real history) before locking effort. Disproven: need for a new library or a readiness reopen. See .working/graph-cost-scout.md.
- (decision) Empty-state copy = Kevin chose TWO distinct messages (option 2): (a) container-run case and (b) no-readable-checkout case get different wording; both are whole-panel empty states (no Changes/Graph/viewer). Draft copy pending Kevin's approval; open sub-question: whether either carries a Reload CTA.
- (decision) Empty-state Reload CTA (CORRECTED, supersedes my earlier 'no CTA on either' lean): (b) no-readable-checkout INCLUDES a Reload CTA — PRD UJ-1 covers RUNNING workflows where working_path/checkout may not be ready yet (transient), and FR-2 Reload re-fetches the current region/file, so Reload lets the operator pick up the checkout once ready. (a) container case = NO CTA (structural: files are not on the host, Reload cannot help). My earlier reasoning ('checkout never reappears') was wrong for the running/transient case.
- (note) Empty-state (b) copy wording fix (accuracy): drop 'the run has no git worktree' — folder/--no-worktree runs still HAVE a working_path (addendum), so that phrase misleads. FR-8 real triggers = not-yet-ready/null-legacy, directory absent/cleaned, or path is not a git checkout. Use neutral 'checkout is not available or readable right now'. Copy still pending Kevin approval.
- (decision) Empty-state copy LOCKED (Kevin ok). (a) container: title 'No files to show' / body 'This run executed inside a container — its working files aren'\''t on the host to read.' / NO CTA. (b) no readable checkout: title 'No worktree available' / body 'This run'\''s checkout isn'\''t available or readable right now — it may not be ready yet, or it may have been cleaned up.' / Reload CTA. Both replace the whole panel.
- (decision) Accessibility floor for the diff (WCAG 1.4.1) LOCKED: diff must not rely on color alone. Kevin 'ok' → standard treatment applied (stated with easy override): per-line +/- gutter markers (+ added, - removed, GitHub/unified-diff convention, supported by react-diff-view) PLUS a subtle red/green line background; the +/- marker is the non-color cue that satisfies the floor. M/A/D badges already use letters (not color-only) — accessible. Override available: minimal '+/- only, no background' if Kevin prefers.
- (event) Entering Finalize: distilling DESIGN.md + EXPERIENCE.md from memlog + .working + imports + sources; then key-screen mock, Reviewer Gate (opt-in), and the PRD/architecture/epics reconcile list.
- (event) Spines distilled + verified by parent read: DESIGN.md (canonical design.md order, inherit-by-reference token maps, SC-specific visual deltas) and EXPERIENCE.md (Foundation/IA/Voice/Components/States/Interactions/Commit Graph/Accessibility/Open Questions/Key Flow, cross-refs DESIGN tokens). All locked decisions encoded, corrections honored, Open Questions flagged. Promoted the refined V1 wireframe (.excalidraw + .png) to wireframes/.
- (note) A11y CORRECTION (supersedes entry-39 'standard treatment applied'): the +/- gutter markers are the LOCKED WCAG-1.4.1 floor (justified, confirmed). The red/green line TINT is [ASSUMPTION - default conventional], NOT user-confirmed - Kevin's 'ok' did not disambiguate 'markers only' vs 'markers + tint'. Promote the tint to a decision only on explicit selection.
- (note) SPINE DEMOTION (before reviewer/status:final): demote distill-introduced items from contract to [ASSUMPTION]/[OPEN] because the user/specs never confirmed them - panel split ratio ~33/67, commit-row height ~24px, per-region INDEPENDENT scrolling, the commit-graph RENDERER choice (React Flow reuse vs virtualized SVG; only 'no new dependency' is verified), focus-management specifics, and the diff tint. Only user- or spec-confirmed items remain contract. Both spines edited accordingly.
- (change) GRAPH readiness CORRECTION (supersedes my 'no readiness reopen' claim): the commit graph is a USER-ADDED NORMATIVE REQUIREMENT (history = graph, not the specced list). Implementation cost/dependency is low (verified), BUT the requirement change reopens PRD FR-6/section 4.4 + epics Story 2.1 + addendum log-parents. Per our own rule this REQUIRES bmad-correct-course (update PRD+architecture+epics) THEN a rerun of bmad-check-implementation-readiness. Prior READY is NOT valid for the graph until reconciled + re-checked.
- (event) Reviewer Gate ran (rubric + accessibility, PASS, 0 critical/0 high). Findings resolved/addressed/deferred in-spine (3 medium, 6 low) with honest buckets; fresh overclaims corrected (theme contrast 'must be verified' not 'met'; region-load skeleton = ASSUMPTION not cited decision; single focus invariant; empty state role=status not dialog; live-region = concise metadata only). Consolidated validation-report.md + .html written. spines finalized: DESIGN.md + EXPERIENCE.md status:final.
- (note) Post-final a11y tightening: focus invariant hardened - NO focus steal on initial/first-paint render (incl. a run opening straight into an Empty state); move focus only if a replacement removed the element the user had focused, else retain + announce concise metadata via role=status; empty state aligned (never on first paint, never unconditionally). No decision changed; spines remain status:final.
- (note) Provenance fix (both final spines): frontmatter now records the full source set Kevin approved ('dùng cả') — sources: 4 core specs; supplementalSources: IR report + reconcile + review; references: imports/vscode-source-control-reference.md. YAML structure verified valid.
- (note) Date-provenance fix: this UX run happened 2026-09-01 (system today), not 2026-08-31 (a mis-set slug copied from the IR report). Corrected created/updated in both spines and Run-at in validation-report.{md,html} to 2026-09-01. Left the workspace folder name 'ux-Archon-source-control-2026-08-31' and asset filename suffixes '-2026-08-31' as a LEGACY slug (renaming would cascade into cross-references/links); authoritative dates are the frontmatter fields.
- (event) Correct Course applied (graph normative change): reconciled prd.md (§4.1/§4.4/FR-6 graph + new Cross-Cutting Accessibility NFR), architecture.md (commit-graph renderer spike-selected no-new-dep + log parents[] + Spike 3), epics.md (list->lane graph, NFR7 + testable a11y ACs on Stories 1.1/1.2/2.1, coverage NFR1-7, UX-contract sections updated), addendum.md (log/snapshot parents[] + fixed L9 D5-gate contradiction). Synced the final UX spines (renderer=@xyflow/react OR bespoke SVG spike-selected; log record adds parents[] retaining message/author/time). Sprint Change Proposal at prds/prd-source-control/sprint-change-proposal-2026-09-01.md. Next: rerun IR.
- (event) IR rerun 2026-09-01 READY. Soft-wording aligned (UJ-1/glossary graph; Epic1 NFR1-7; EXPERIENCE renderer spike-selected either-or; DESIGN Do's either-or; architecture/addendum bespoke SVG). Residual hunt a-f none. Report: prds/prd-source-control/implementation-readiness-report-2026-09-01.md. Next: sprint-planning; Spike 3 before Story 2.1.
