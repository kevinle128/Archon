---
topic: Readable agent transcript inside the node room, Legacy + Console — Track A UX
updated: 2026-09-13T19:54
---

- (event) Create mode. Both prior UX runs (2026-08-31, 2026-09-05) are status final and cover source control, not the node room transcript. New workspace.
- (event) imports: superpowers-transcript-v1.html — brainstorm mockup, current-vs-proposed split, density options A/B/C. Real OMP payloads from the local DB.
- (event) imports: superpowers-transcript-v2.html — brainstorm mockup applying option B to the full transcript plus four hard cases (long path, Codex command-as-name, MCP name, unknown tool) and the status-glyph options 1/2/3.
- (decision) Inherited from brainstorm (user-settled 2026-09-09): one collapsed row per tool call; successes collapsed, failures auto-expand; density option B = tool name as chip; status glyph option 1 = characters ✓ ✕ ◐ plus colour, never colour alone; long paths elide in the MIDDLE so the filename survives; unknown tools fall back to up to three key:value pairs, never JSON.
- (decision) Inherited from spec-readable-agent-transcript: nine tool families (shell, file, search, glob, code, todo, task, web, generic); chip is the tool name when a single token of at most 24 chars else the family name; todo renders as folded state anchored at the last todo call; task renders context plus one card per subtask; attempts and loop iterations grouped by occurrence_id with a header only when more than one; every card keeps a Raw toggle, closed by default.
- (direction) User asked for detailed mockups to review, inheriting the superpowers brainstorm. Layout is already firm, so working mode is Fast path with key-screen mocks rendered during late Discovery; gaps carried as [ASSUMPTION] and settled by the user's review of the mock, not by a questionnaire.
- (decision) Foundation: web, two surfaces. Console is the default route and inherits packages/web/src/experiments/console/theme.css (Geist Mono, cool charcoal hue 265, brand magenta/violet/teal, status running/success/warning/error). Legacy inherits packages/web/src/index.css (Inter + JetBrains Mono, hue 260). Brainstorm GitHub-dark hexes are NOT carried; every colour in the mock resolves to a named product token. Constraint from AGENTS.md UI rules.
- (decision) Mocks nest inside the prior HITL contract's room chrome (spec-workflow-run-view-hitl/ux-mockup/{console,index}.html) so the transcript is shown where it actually lives, not as a floating panel. That contract is final; this run changes only the agent-room transcript inside it.
- (assumption) Occurrence group header wording is open. Brainstorm wrote 'Attempt 1 / Attempt 2 · sau retry' but the spec forbids the word Attempt (attempt_id is finer than occurrence_id). Mock will render 'Run 1' / 'Run 2 · retry' / 'Iteration 3' and the user picks at review.
- (assumption) Raw toggle placement: a small text button at the far right of the EXPANDED body's first line, not on the collapsed row, so the collapsed row stays one scan-line. User confirms at review.
- (assumption) Keyboard: each row is a disclosure (button + aria-expanded), Enter/Space toggles, arrow keys move between rows. Matches the existing details/summary behaviour today.
- (decision) User pick: tool chips are coloured per family (not neutral). Hues must trace to existing product tokens; the exact family-to-hue mapping is rendered in the mock with a legend and is an [ASSUMPTION] until the user reviews it.
- (assumption) Family-to-hue mapping, every hue an existing --node-* token so both surfaces already have it: shell=node-bash amber(75) · file+web=node-command blue(250) · search+glob=node-prompt violet(290) · code=node-script green(120) · todo+task=node-approval orange(40) · generic=neutral text-secondary. Six treatments, not nine: siblings share a hue so the palette stays below the status colours (success/error/running) in salience. Rendered with a legend for review.
- (assumption) Codex names arrive as /bin/zsh -lc '<script>' across the corpus. The headline unwraps that shell wrapper and shows the script's first line; the full wrapped text stays in the terminal body and behind Raw. Without this every Codex row would headline the same /bin/zsh -lc prefix.
- (event) Rendering three key screens to .working/: key-console-node-room.html (Console chrome, running node, failure expanded), key-legacy-node-room.html (Legacy chrome, completed node, two occurrences), key-transcript-states.html (legend, all nine families collapsed and expanded, four hard cases, occurrence header wording options, Raw toggle, glyph row).
- (event) Three key screens written to .working/. Row anatomy is byte-identical between the Console and Legacy files; only the :root tokens differ, which demonstrates the one-presenter-two-renderers constraint visually.
- (event) Screenshot pass on all three key screens: row anatomy, chips, glyphs, middle elision, failure auto-expand, occurrence headers and Raw toggle all render as intended on both token sets. Two layout fixes applied: path head must not flex-grow (tail was pushed away on short paths); grep line numbers must flow inline, not take the diff body's fixed column.
- (decision) User confirmed at mock review: family chips keep the six-treatment mapping (shell=node-bash · file+web=node-command · search+glob=node-prompt · code=node-script · todo+task=node-approval · generic=text-secondary). No new tokens.
- (decision) User confirmed: occurrence header reads 'Run N' for retry-driven occurrences and 'Iteration N' for loop-driven ones, matching the panel header's existing runs/iterations vocabulary; suffix '· retry' or '· failed' only when known. 'Pass' rejected (collides with test pass/fail); reason-only rejected (loses the ordinal).
- (decision) User confirmed: Codex tool names that begin with the fixed prefix /bin/zsh -lc ' or /bin/bash -lc ' have exactly that prefix and the closing quote stripped for the headline; the script is never parsed; the full wrapped name stays in the terminal body and behind Raw.
- (change) Glyph correction: earlier todo calls collapsed to a 'todo updated' row showed – (unknown outcome). Those calls succeeded; they are merely collapsed. They now show a muted ✓. The – glyph is reserved for a genuinely unknown outcome or missing output.
- (decision) Badge priority when the row is short of space: exit code and counts (matches, files, +n −m) are kept; duration is dropped first. Chip cap stays at 24 characters per the spec; the headline absorbs remaining pressure via elision.
- (event) Finalize started. Reviewer Gate: user opted in; lenses = rubric walker + accessibility.
- (change) CORRECTION to the family-hue claim (every hue exists on both surfaces, no new tokens): false for the code family on Legacy. --node-script exists only in console/theme.css:88 (oklch 0.7 0.14 140, console-scoped by its own comment); index.css never had it. The mocks hardcoded oklch(0.68 0.17 120), copied from the HITL mock's styles.css, which invented it. Console mock corrected to the real value; Legacy needs a user decision (see question).
- (question) code family hue on Legacy: (a) promote --node-script into packages/web/src/index.css as a new production token, which per AGENTS.md means updating the token source AND the brand guide; or (b) fold code into the shell family's amber (both execute), five treatments, no new token. Pending user.
- (decision) Chip has no min-width. The loss of a fixed scan column was weighed when the user chose density B over A at brainstorm; B's own caption stated that trade. Not reopened.
- (decision) Default-open is decided by outcome only: failed rows open, every other row collapsed. Mocks show a few successful rows expanded purely to illustrate their bodies; the spines govern, and no per-family default-open rule exists.
- (decision) Middle elision keeps the filename always and the immediate parent directory when width allows, matching the v2 import's example. The mocks' two-span CSS is an approximation that drops the parent first; the real presenter does it in code.
- (decision) MCP tools render as a neutral generic chip labelled server · tool. The v2 import's blue MCP chip is superseded by the contract's Tier 1 rule; confirmed at mock review (states sheet, hard case 3).
- (assumption) Success glyph ✓ stays full-strength per the user's glyph option 1 (colour reinforces). The single dimmed ✓ is on collapsed earlier-todo rows, marking collapsed-not-primary. A silent-success hybrid (option 3's rationale, dim every ✓) was never offered; listed as an open item, not applied.
- (decision) User pick (b): the code family shares the shell family's amber (--node-bash) on both surfaces. Five chip treatments: shell+code amber · file+web blue · search+glob violet · todo+task orange · generic neutral. No new production token; --node-script is not introduced into index.css. Chip text (eval) still distinguishes code from shell.
- (event) Spines distilled by subagent, then patched by the parent for decisions the subagent could not have seen: the code family now shares --node-bash on both surfaces (pick b), its Open Question is closed and the rest renumbered 1-4, code-body string literals rebound to the success token. Token reference check: 89 refs, 236 defined paths, zero unresolved.
- (change) DESIGN.md Colors now attributes the amber code chip to the user decision and records why the earlier six-treatment mapping was superseded, so a reader of the spine alone sees the reasoning, not just the outcome. Raised by the distill subagent, which otherwise mis-read memlog order: L36 (pick b) supersedes L23 (six treatments) under the append-only later-wins rule.
- (decision) Legacy runs/iterations chips FILTER, they do not scroll. selectNodeRoomMessages (packages/web/src/components/workflows/NodeRoom.tsx:90-119) slices the message list to one iteration window between iteration_started and iteration_completed/failed for selection.kind loop_iteration, and returns the full ordered list for occurrence and node. Neither room has scroll-to-occurrence code: the only scroll logic is Console's stick-to-bottom and scrollTop restore (ConsoleNodeRoom.tsx:550-572). The spine's scroll assumption was wrong; the HITL README's scoped is right, but only for loop iterations.
- (decision) interrupted takes a warning triangle in --warning, not an error cross. It is a stopped tool, not a failed one: Claude alone writes it, from the PostToolUseFailure hook when is_interrupt is true (packages/providers/src/claude/provider.ts:952-959), and that same hook already prefixes the output text with a warning triangle. Codex cannot produce it at all — its union is success, error, unknown (codex/provider.ts:644-650). --warning exists on BOTH surfaces (index.css:24, console/theme.css:79), so the glyph inherits cleanly and adds no token; the failure that hit code and --node-script does not repeat here.
- (decision) The full-output control keeps the position it has today: a text button under the output block, shown only when canLoadFullOutput is true (ConsoleAgentHistoryList.tsx:199-208). It fetches more bytes; it does not change presentation, so it does not belong in the body bar beside Raw, which redraws the SAME bytes.
- (assumption) Row-to-row keyboard movement stays on Tab. Native details and summary give Enter and Space to toggle the focused summary, and Tab to move between summaries; arrow keys need custom JavaScript and a roving tabindex. A transcript is a list of disclosures, not a composite widget, so the native semantics are correct and cost no code.
- (assumption) Small viewports inherit the node room unchanged. The transcript declares no breakpoint of its own; the room already owns the responsive behaviour (commit 56c61f72).
- (change) States sheet section B now shows five glyphs, not four: the interrupted warning triangle joins the legend with a .gl-warn rule on --warning, and the caption says why it is a fifth character rather than a recoloured cross. Both spines, the legend, and the state table now agree on the same five outcomes.
- (event) Accessibility lens returned. It reproduced every published ratio within rounding, then found the shortfall wider and one new critical fault. Applied without a user decision, because each is a spine bug or an addition that reverses nothing: focus-console renamed from --accent-ring (30% alpha, 1.35:1 over surface) to --accent-bright (opaque, 4.85:1), which is what all three mocks always drew; the folded-todo 55% opacity dropped (2.69:1 Legacy, 3.56:1 Console) — the user's own glyph choice was full-strength colour reinforcement, so the dimming was an unratified assumption anyway; chevron hidden from assistive technology and the glyph given a required accessible name; the resolved family added to the chip's accessible name; scroll anchoring and focus-on-append specified from Console's shipped behaviour; scroll-margin-top against the sticky header; the occurrence chips bound to the shipped select and button semantics rather than the mock's non-focusable spans; dropped badges required to reappear in the body bar; and the two spines reconciled on elision, with DESIGN.md's CSS two-span rule authoritative over EXPERIENCE.md's produced-string wording, which would have destroyed the path for assistive technology and copy.
- (question) Three questions go to the user, each a real trade-off rather than a fact: the text tier that carries every number (--text-secondary on both surfaces, or accept), the family hue for a sighted colour-blind reader (tooltip plus body bar, or a family prefix that costs row width), and target size (raise Raw and record the row's 2px shortfall, or reopen density). The interrupted glyph, the focus token, the folded-todo opacity and the exit-badge question left the list.
- (event) Rubric lens returned and independently confirmed the two faults the accessibility lens found, then added four the accessibility pass missed. Applied: the chip fallback now matches its own contract — a name over 24 characters chips the FAMILY, so get_command_or_subagent_output reads generic, and the ellipsis-in-pill state the spines and the states sheet all taught was unreachable under tool-presentation-contract.md:159; both routes corrected against live code (/legacy/workflows/runs/:runId, /console/p/:projectId/r/:runId) with the note that node selection IS deep-linkable through ?node=, which the IA had denied; panel widths stopped citing a README that states neither number — 460px is the room contract at ux-mockup/styles.css:1111 and the 520px is this run's own mock; contrast table extended with surface-hover, surface-inset and both focus rows; Flow 1 now opens the task row, so CAP-4 is demonstrated rather than only claimed; the invented persona replaced by its role.
- (decision) The transcript row's focus ring is --accent-bright, and this is recorded as a DELTA from the shipped console rather than an inheritance. Console ships outline: 2px solid var(--accent-ring) (theme.css:153-156) and --accent-ring is magenta at 30% alpha (theme.css:68), which composites to 1.4:1 over --surface — under the 3:1 floor of SC 1.4.11, so a keyboard reader cannot see their position. All three mocks had quietly drawn the opaque token; the spine now states it deliberately. It uses an existing token, so the no-new-token constraint holds, and it is scoped to the transcript row: the same 1.4:1 ring is on every other focusable element in the console, which is room chrome and out of scope, recorded so the next person finds it.
- (decision) USER DECISION: every transcript fact moves from --text-tertiary to --text-secondary, on BOTH surfaces. Chosen from three options with the measurements shown: tertiary carried every badge, body bar, occurrence header and key name at 10-11.5px and measured 2.53:1 Legacy / 4.07:1 Console resting, worse on hover, while the headline beside it passes at 15.3:1 — so a reader with low contrast sensitivity got the command and not the result. Secondary clears the floor everywhere (5.82:1 / 8.37:1) and is an existing token, so no token is added. --text-tertiary now has exactly one use, the chevron, which is decoration hidden from assistive technology and carries no fact.
- (change) Collapsing the third tier flattened two pairs that had used it for contrast, so the ramp was rebuilt upward rather than downward: the key-value list now reads secondary key against a PRIMARY value, and the body box reads primary content with secondary annotations (line numbers, code comments), where before both pairs were secondary against tertiary. The folded todo row keeps its subordination the same way — its headline drops from primary to secondary while every other headline stays primary. Nothing was made quieter to restore a difference; the content was made louder.
- (decision) USER DECISION on the family hue for a sighted colour-blind reader: tooltip plus the family named in the body bar, not a family prefix inside the chip. The prefix was rejected because it costs row width on the one line the design says must never wrap, and at 460px the path headline pays for it first. So the family now travels on three channels — the chip's accessible name, the chip's title, and the first word of the body bar. The body bar is the one that always works, needing neither hover nor assistive technology, so that word is a requirement and is never dropped under width pressure. The title only duplicates it, which is why it does not break the hover-only ban. Where the tool name already IS the family word (glob, todo, task, Codex shell) it is said once.
- (decision) USER DECISION on target size: the Raw toggle grows to min-height 24px through padding, so its painted box is unchanged, and the 22px tool row keeps its 2px shortfall against SC 2.5.8 as a recorded decision. Raw was the target that genuinely missed — about 15-17px, at the far right of the body bar and near the panel's drag-to-resize edge. Row density is the feature the transcript exists to deliver and the surface is reached with a pointer, so the row is not grown; the shortfall is written down instead, to be revisited if the surface is ever targeted at touch.
- (decision) Raw SWAPS the body rather than appending. Appending would put two renderings of the same bytes on screen at once and double the body height, which is the problem the transcript exists to remove; the states sheet the user reviewed already showed the swap.
- (decision) A reader's manual open or close outranks every automatic rule, including a row the transcript auto-opened because it failed. New rows arriving never reset it: an explicit reader action is the strongest signal on a row.
- (decision) New rows are announced through ONE polite role=status region scoped to node-level transitions and failures, never by blanketing the transcript in aria-live — announcing every row of a fifty-row streaming node is worse than silence. The earlier plan to announce nothing rested on the panel's status badge carrying the state, and that fallback does not exist: the shipped badge is a plain span at NodeRoomHeader.tsx:78 with no role and no aria-live, so without this region a blind reader never hears the one event the transcript exists to surface.
- (event) spines finalized
- (change) Two corrections found by re-checking rather than by a reviewer. Flow 1 claimed each subtask card carries its own outcome; TaskSubtask is {name, agent, prompt} at tool-presentation-contract.md:49 and has no outcome field, so the flow now says the cards show what was dispatched and the row's own glyph is the only status. And two of the four contrast cells added for surface-hover and surface-inset had been written from estimate, not arithmetic: recomputed through oklch to sRGB to relative luminance they are 5.1:1 and 7.4:1 on hover and 6.3:1 and 8.7:1 on inset, not 5.4/7.7 and 6.3/8.9. The warning glyph row was measured too (8.3:1 Legacy, 9.9:1 Console) and added. Also recorded: on Legacy --warning and --node-bash are the same value, so an interrupted shell row's triangle matches its own chip.
- (change) Contrast cells recomputed after the converter was calibrated against the ten cells the rubric had already verified — it reproduced all ten, so the new numbers are trustworthy. surface-hover is 5.1:1 Legacy and 7.5:1 Console, not the 5.4/7.7 first written; surface-inset was right at 6.3/8.9; the warning glyph measures 8.3:1 and 10.1:1 and is now in the table. The header no longer says the cells are an approximation, because they are computed.
- (change) Second reviewer pair landed after the first was already resolved, and it overturned one decision of mine and one of its own. REVERSED (mine): the claim that an occurrence chip returns the list unfiltered. It is filtered — by the SERVER. lib/api.ts:717-719 puts occurrenceId and attemptId on the request, and select-node-room-messages.ts:12 says so in its own comment; the client only slices loop iterations. Both surfaces behave identically, and Console calls the same filter at ConsoleNodeRoom.tsx:603 — my 'Console does not filter at all' cited lines 379-400, which render a loop-group's node list, not a transcript. The earlier check read the filter function alone without following the fetch: new evidence, not a counter-argument. WITHDRAWN (theirs): the sticky-header finding I had adopted. LegacyNodeRoom.tsx:250-253 renders header and body as siblings and the overflow-y-auto scroller sits inside the body, so the header cannot cover a focused row; the bullet is now a forward guard instead of a fix.
- (question) One question is genuinely still open and was wrongly marked resolved during a renumbering: --node-prompt chip text measures 3.7:1 Legacy and 3.9:1 Console against surface-elevated, and chip text at 11px needs 4.5:1. It is the only family hue that fails — command 4.8/4.9, approval 6.5/6.8, bash 7.6/7.9, generic 5.8/8.4 — so it is the search and glob chips, not the palette. The tier decision does not cover it, because a chip's colour IS its family and the hue system is a user decision. Either brighten those two with color-mix toward text-primary while keeping the pure hue on the border, or accept it on the grounds that the chip is a redundant label. Restored as open rather than quietly accepted.
- (change) Closed the last substantive gaps the consolidated report found. Flow 2 no longer clicks a run chip and then reads both runs — a chip scopes to one execution and the occurrence headers go with it, so the flow now stays on the node, which is also what the Legacy mock draws and what CAP-6 needs. The resolved-questions table row that still summarised the pre-reversal answer was rewritten to match the section it points at. Added: a transcript load-error state, so a failed fetch never looks like an empty transcript; an awaiting-output state for a row the reader opened before the result arrived; the subtask card's own disclosure spec, reusing the row chevron rather than inventing a second affordance; the occurrence header as a heading rather than a bare div, and a note that it cannot appear under an occurrence or iteration chip; and a citation of states-sheet section D from the body-arm row. The Console mock's --error had drifted from theme.css and is realigned — a full token diff of all three mocks against index.css and theme.css now shows zero drift across 19 shared tokens and 14 node hues. DESIGN.md's own Console hexes were correct.
- (change) Verified how the panel is actually scoped, because the reversal left a rule that was true but imprecise and a mock that contradicted it. occurrence_id is OPTIONAL on a node execution (node-execution.ts:50), and occurrenceSelection branches on it (build-log-rows.ts:108-118): no occurrence id yields kind node, an occurrence id yields kind occurrence. Only the second reaches the server as a filter. A graph click prefers a node-kind row and otherwise falls back to the node's LAST row (resolve-graph-room-row.ts:19-27). So the two-occurrence view is the node-scoped one and it is genuinely reachable — it is also the ONLY state in which the occurrence header renders, which is what CAP-6 depends on. Both spines and the Flow 2 precondition now say this.
- (change) key-legacy-node-room drew run chips with chip 2 marked viewing beside a transcript showing BOTH runs — a state the verified rule makes impossible, and markup less accessible than what ships. Replaced with the shipped control, a native select labelled Execution, showing the node-scoped option. This fixes the contradiction and the accessibility regression in the same edit, and it is a correction to match verified behaviour, not a design change: the user's mock review was of the layout, not of the chip state. Both room mocks also gained the focus-visible rule the spine specifies and only the states sheet carried.
- (event) Provenance note: two reviewer pairs ran and both wrote to review-rubric.md and review-accessibility.md, so the second pair overwrote the first. The committed files are the second pair's. The first pair's findings survive as the actions taken on them in this log — the focus-ring token, the folded-todo dimming, the chip fallback, the routes and the withdrawn sticky-header finding.
- (event) Mock coverage confirmed. All five IA surfaces — node panel, transcript, tool row, tool body, Raw — appear in a key screen: the two room screens carry the panel and the transcript in their real chrome, and the states sheet carries every row, every body arm, every glyph and the open Raw toggle. Nothing is spine-only, so there was nothing to put to the user.
- (note) The open --node-prompt question now carries a recommendation, which it lacked: accept and record, because the chip is the most redundant element on the row and it would join three shortfalls already accepted on the same footing. The counter is stated with it — color-mix is already how every chip border is drawn, five times in the states sheet, so the brand-rule argument against brightening is the weakest of the three reasons and the user should see that before choosing.
- (change) Structure and prose lens found six self-contradictions, every one the same shape: a fact written in two places, and the copies drifted. Fixed: the sentence claiming all questions were answered, which sat directly under the open one; the claim that every bold contrast cell is listed under Open Questions, when the 4.4:1 pair was not; the node-prompt chip cell, which the table rounded 3.749 up to 3.8 while the open question said 3.7 — recomputed, 3.7 is right; the claim that nothing in the exit badge sits under the floor, when the digits keep --error at 4.3:1, which is an accepted shortfall rather than a cleared one; Flow 1 counting nine collapsed rows while enumerating seven and calling the failure the tenth; and a Provider-normalization bullet still saying the family colour is the only sign of resolution, three sections after the family gained three non-colour channels. The open question also widened: --node-prompt fails on BOTH backgrounds it is drawn against, as chip text and as code-body keywords, so it is one token in two places rather than two chips.
- (change) Two duplications removed rather than re-stated. Responsive and Platform no longer repeats the pressure order — it points at Elision and badge priority, which owns it. Provider normalization no longer paraphrases four rules that live elsewhere in the same file; it keeps the framing and the one reader-visible consequence, and cites the contracts for the mappings. The reviewer argued to PRESERVE the Narrow panel state row, which restates the same rule in fourteen words: it sits in a random-access table where a pointer would hollow it. Accepted that argument.
- (change) The target-size decision was written in full in both spines — a rule in EXPERIENCE.md Accessibility Floor and a record in DESIGN.md — which is the same defect the prose lens had just found six of. DESIGN.md owns dimensions, so the Accessibility Floor bullet now points at it instead of repeating it. The status: final trailing comment also still described the open question as the chip pair; it is the token on both backgrounds it carries text.
- (event) validation-report.html and .md are pinned to revision 76853497 and two material commits followed, so they under-report: they say two open wording items, both since fixed, and they predate the six self-contradictions the structure-and-prose lens found. Deliberately not re-rendered — a fourth synthesis pass is diminishing returns and this log is canonical. plans/reports/docstandards-260910-0127-ux-spine-structure-prose.md covers the later round.
- (event) Spec-side scope confirmed before handing off: only tool-presentation-contract.md:156 names the glyph set, and it says four. test-plan.md:67 states the colour-independence guarantee without enumerating glyphs, so it needs no change. The bmad-spec update therefore carries three inputs — adopt DESIGN.md and EXPERIENCE.md as companions, correct that one line to five glyphs, and expect stories.yaml drift.
- (change) EXPERIENCE.md:90 corrected. It said the chip shows the NORMALIZED tool name, contradicting its own :79 ('exactly as the provider sent it', with read_file, Edit, Grep, eval as the examples) and :208 ('the tool name as sent'), and contradicting DESIGN.md:377 which agrees with them. Normalization is defined in tool-presentation-contract.md:68 as case-folding plus stripping underscores and hyphens, so the losing reading would render read_file as readfile — a spelling no example in this run ever shows. :90 was the lone outlier in the whole run. The cell now says 'as the provider sent it' and adds that normalization is how the resolver MATCHES a family, never a display transform. The single-token and 24-character test and the family-name fallback are untouched, having been deliberately settled earlier; ellipsis truncation was not reintroduced.
- (change) EXPERIENCE.md:91 corrected. The cell gave the status glyph two incompatible derivations at once: 'Derived from AgentHistoryItem.outcome via deriveOutcome(), unchanged' AND '– unknown or output missing'. Those diverge on a reachable row — a result row with outcome success and no output, where deriveOutcome returns succeeded and deriveOutputState returns missing — so one rule yields a tick and the other a dash. This is the only channel carrying status without colour, so a fork here breaks this run's own accessibility guarantee. Resolved the way :131 and :132 already render it and the way architecture spine AD-13 pins it: the glyph is a function of outcome ALONE, '–' means unknown and nothing else, and output state is carried by a badge.
- (change) mockups/key-transcript-states.html and its .working copy: the legend read '– unknown / output missing', teaching the losing side of the :91 correction. Changed to '– unknown' in both copies, which remain byte-identical. Verified first that NO rendered transcript row in the mockup uses that glyph — gl-na appears only in the CSS rule at :49 and in the legend at :111 — so no row was ever drawing the wrong thing, and the style block and span tags are balanced after the edit.
- (note) Whole-run sweep after both corrections: no file teaches either losing side. Zero occurrences of a case-folded chip spelling, zero remaining uses of 'normalized/normalised tool name' as a display rule, and no mockup row rendering the dash on anything but an unknown outcome. DESIGN.md was not touched and needed no change.
- (event) EXPERIENCE.md updated 2026-09-12, status stays final. Surgical update only — Discovery, mock re-rendering and the Reviewer Gate were all deliberately skipped.
- (event) UPDATE run opened 2026-09-12: adopting spec-live-agent-steering into this spine. The node room is now specified by two specs - readable transcript (the read half) and live steering (the write half). User settled the update-vs-new-run choice explicitly.
- (change) Scope of the delta: composer, Stop control, draft box panel, message status chips, and the operator's own transcript row - visual in DESIGN.md, behavioural in EXPERIENCE.md.
- (override) CONFLICT WITH A PRIOR DECISION IN THIS RUN. Foundation currently states the room chrome 'is the shipped HITL contract and does not change'. Steering adds write affordances to that chrome, so the sentence can no longer stand as written. Surfaced to the user rather than silently rewritten.
- (assumption) Panel anatomy today is header plus a single scrolling body - there is no footer slot on either shell (ConsoleNodeRoom.tsx:867-875; NodeRoom.tsx:142). A composer therefore introduces new chrome whichever way it is placed.
- (assumption) The shipped Legacy header is a flex-wrap row already carrying eight items at a 460px panel width (NodeRoomHeader.tsx:58-95): node label, execution label, Execution select, status pill, started, duration, provider, model, Close. It wraps today, so a destructive action placed there can wrap too.
- (decision) The shipped HITL ask card is the precedent for an input surface in this panel, and it renders INLINE in the transcript anchored to its tool row, never in header or footer (ConsoleNodeRoom.tsx:656-759). Legacy uses shadcn Textarea plus Button (AskCard.tsx:354-384). The steering composer is a peer of that card, not a clone: the ask card answers a question the agent asked, the composer is operator-initiated and present while the node runs.
- (decision) An ask and a steer cannot be answered by the same control: resume refuses to run while any pending interaction row is still pending ('Answer or decline the Ask before resuming run', dag-executor.ts:10367-10370). So Send now must be blocked, with a reason, whenever the run holds a pending ask.
- (decision) USER DECISION: every write affordance lives in ONE composer dock pinned to the bottom of the node panel - draft box, text field, send control, and the stop control together. The shipped header is not touched. This resolves the Foundation conflict with a one-sentence amendment rather than an override, and it keeps the chrome change in a single place on each shell.
- (decision) Upstream divergence to repair: spec-live-agent-steering says 'a stop control in the node header' in both SPEC.md and control-states.md. That wording predates the header measurement and is now wrong. Offer to correct it so spec and spine do not disagree.
- (decision) Dock layout: stop control at the LEFT edge of the control row, send control at the RIGHT. The separation is deliberate - a destructive control and the primary control must not sit under the same thumb.
- (decision) Control labels follow node state, never a remembered mode: stop control reads 'Stop node' then 'Stopping…' disabled; send control reads 'Queue' while running and 'Send now' while stopped. Both revert the instant the node resumes.
- (decision) The draft box sits ABOVE the text field inside the dock and renders only when it holds something. Header reads 'Queued' while running and 'Will send' while stopped; that one word is the signal the control below has changed meaning.
- (decision) Stop-is-not-undo is disclosed as TEXT, not as a tooltip alone. Once the node is stopped the dock states it in one line - stopped after the last completed tool call, files already written stay written. The stop control's title duplicates those words, which is the same allowance the family chip title already takes: nothing is reachable by hover alone.
- (decision) The operator's transcript row reuses the assistant-text pattern - sans face, prose, in seq order - and is separated from it by an explicit role label plus its own rail colour. The label is the non-colour channel; the rail is the fast path.
- (decision) Message status is text, never colour: 'sent' and 'delivered' render as badges with words. On a provider that echoes nothing a message stays 'sent' and the interface adds no explanation, because it has nothing it can honestly say.
- (decision) Queue depth rides the send control's accessible name, e.g. 'Queue message, 2 waiting'. The draft box's ABSENCE carries meaning that absence cannot announce, so the count has to live on a control that is always present.
- (decision) The polite role=status region this run already owns gains two node-level events: node stopping, and node resumed. It already announces node transitions and failures, so steering adds events rather than a second live region.
- (decision) Send now is blocked while the run holds a pending ask, with the reason in text - resume itself refuses in that state (dag-executor.ts:10367-10370). A blocked control that does not say why would read as a bug.
- (assumption) [ASSUMPTION] The dock is absent on a node that has finished, because there is nothing to steer. The steering spec lists the finished-node composer as an open question, so this is the safe default and not a settled answer - flag it at triage rather than pinning it here.
- (event) Spines distilled: DESIGN.md gained 8 steering component token blocks plus control typography and dock spacing, all resolving to shipped variables; EXPERIENCE.md gained dock rows in IA, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, a dedicated Accessibility Floor subsection, a Responsive paragraph, and Key Flow 4.
- (event) Key screen rendered and promoted: mockups/key-steering-dock.html - the four dock states including the stopping state no prior mock had, plus the operator row in the transcript and a refusals table. No raw hex, tags and braces balanced.
- (event) Input reconciled: reconcile-live-steering.md. One divergence from the approved mock (stop control moved from header to dock, on header measurement), one addition (the stopping state), three deliberate drops, two aion ideas recorded as rejected rather than lost.
- (event) Reviewer Gate: user picked accessibility plus the rubric walker. Both dispatched as parallel subagents, scoped to the steering delta only - the transcript half was reviewed by both lenses in September and those findings are already applied. Reports will land at review-rubric-steering.md and review-accessibility-steering.md.
- (event) Reviewer Gate returned. Rubric: 1 critical, 4 distinct high, 15 medium, 11 low. Accessibility: 3 critical, 5 high, 7 medium, 5 low. Both lenses independently computed the same contrast figures and both flagged the same critical.
- (override) C-1 REVERSED A RECORDED DECISION. The send control no longer inherits Legacy's shipped filled ask-card Button: that pairing is --primary-foreground on --primary at 3.06:1 on an 11.5px label, a clean SC 1.4.3 miss. Verified independently in the parent session with a converter calibrated against the four hexes the spine publishes - all four reproduced exactly. The control is bordered on both shells now, 14.1:1 / 16.7:1, and primacy is carried by right-edge position instead of a fill.
- (change) C-2: the focus-ring fix was scoped to the transcript row and excluded the dock by omission. All four focusable dock elements now carry the fixed ring token explicitly (6.8:1 / 4.6:1), and the scoping sentence was narrowed to elements outside this panel.
- (change) C-3: Stopping… now uses aria-disabled rather than the disabled attribute, which blurs the focused element to body - the operator who presses Stop by keyboard would have landed at the top of the document for up to ten seconds. The dim floor moved from text-tertiary (2.31:1) to text-secondary (5.33:1), since the SC 1.4.3 inactive-component exemption no longer applies to a focusable control.
- (change) H-3 was my own regression: the new mockup set 'this tab only' in --text-tertiary at 2.71:1, the exact tier this run had already removed from every fact it carries. Fixed in both mockup copies.
- (change) Applied besides: H-1 live-region coverage for four dock status changes, H-2 aria-describedby on the pending-ask reason, H-4 focus and unsent text preserved when a node finishes on its own, H-5 conveyance of the Queue to Send now change, M-1 composer label, M-2 the shared polite region collision at Flow 4 step 5, M-3 send control width held, M-4 dock growth preserves scroll, M-5 draft box list semantics, M-6 a bounded end for Stopping…, M-7 border exemption dispositioned against the Raw toggle precedent, plus L-3 and L-5.
- (change) Rubric fixes: capability-coverage line qualified by spec (both specs number CAP-n, so every id was ambiguous); steering CAP-5 given a Send control clause instead of a rule contradicting it; a State Patterns row for a steer aimed at a superseded attempt; and the five DESIGN.md sections the dock falsified - sans exception, bold rule, surface stack, Shapes, Layout and Spacing.
- (change) Advisor review closed three seams the gate had opened. (1) The CAP-5 clause specified the bare word Send, which Voice and Tone bans in the Don't column - changed to 'Send now', told apart from the stopped reading by whether Stop node sits beside it. (2) The operator role label was 'you', which labels one person's message 'you' when a second person reads it; AGENTS.md supports multi-user per install and the transcript row carries no user_id, so the label is now 'operator', matching the spec's own vocabulary. (3) Foundation's 'no schema, migration or backend change' claim was true of the transcript half only and is now scoped to it.
- (change) Rubric mediums applied after reading them rather than skimming: a cold-load dock row, the queue's scope stated as per-node as well as per-tab (previously undefined - switching nodes had no specified behaviour), a Cross-spec dependency section so CAP-4's dependency on spec-readable-agent-transcript is visible from the pair, and the State Patterns Surface column renamed to match Component Patterns.
- (question) Deferred with CAP-5: control-states.md:49 says mid-turn delivery adds 'an extra option' while running (queue AND send), where the spine's Send control clause has it REPLACE the Queue reading. Both are defensible and neither is reachable until a provider declares the capability. Logged so it does not surface later as two truths.
- (event) doc_standards polish dispatched: bmad-review structure and prose lenses, in parallel, scoped to the steering delta only. Both were told explicitly NOT to cut rationale clauses, measurements, citations or recorded reversals - the spine records why a rule exists on purpose, and a minimising lens is input to the decision rather than an instruction. The structural question put to them: after eight scripted insertions, does the pair still read as one contract or as two documents stapled together.
- (event) Structure lens returned: Reference/Database model, 24 recommendations (4 CUT, 2 MOVE, 8 CONDENSE, 4 QUESTION, 6 PRESERVE). Verdict on the question that mattered: one coherent contract with visible seams, not two documents stapled together - the delta was integrated as rows inside the pair's existing two-axis tables rather than bolted on beside them.
- (change) Four structure findings applied. The cross-spec prerequisite moved from after the Key Flows into Foundation, beside the other fact of its class - it blocks CAP-4 and was sitting 340 lines below the component it blocks, which defeated the reason it was written. The Stopping… dim bullet still justified itself with SC 1.4.3's disabled-control exemption, a survivor the C-3 fix did not reach; a builder extracting that bullet alone could have dimmed to 2.3:1 against the floor DESIGN pins. A dangling 'listed in Open Questions' pointer now resolves to the accepted-shortfalls list, where the shipped ask-card Submit finding is recorded as out of this spine's scope. And the bold rule stated itself twice in one sentence pair, insertion residue from my own prepend.
- (event) Prose lens returned: 24 rows. Dominant defect was stale copies of the pre-C-3 disabled reasoning surviving in three spine locations plus the mockup's stale 'you' caption - the fix had reached the rules but not every place that explained them.
- (change) Prose fixes applied. Flow 4 used masculine pronouns for Kevin 13 times where Flows 1 and 2 use they/them 11 times and project-context declares no pronouns; Flow 4 now matches, verb agreement included. Three more 'disables' survivors corrected to aria-disabled. Terminology collapsed to one name per thing: Composer dock (not 'steering dock'), Operator text (not 'Operator row'), Claude capitalised. The Voice and Tone claim that the dock speaks lowercase was falsified by its own control labels, so the rule now says controls are sentence-case because a label is a name while the lowercase rule governs reported facts. DESIGN's 'uppercase marks exactly two things' was a sixth section the dock falsified - now four.
- (change) Structure gap the prose lens routed rather than swallowed: the stop disclosure line had microcopy, a state row, a flow beat and a rendered mockup slot but NO anatomy slot and no token block, so it existed everywhere except in the component list a builder assembles the dock from. It is now a named component in DESIGN.md with its own token block, and it appears in the dock anatomy in both DESIGN Components and the IA table.
- (change) Mockup caption in both byte-identical copies said 'the you label' while every rendered label said operator; also lowercased claude. Copies verified identical after the edit.
- (event) Mock coverage confirmed: every IA surface is mocked, including all four dock states, the draft box, the stop disclosure and the operator row. No surface is spine-only.
- (decision) Five prose rows phrased 'Consider:' were NOT applied - each turns on intent rather than wording, and one of them (Foundation:36) is the shape the product owner chose in this run, with the confirmation trail stated in the row itself. A minimising lens is input to that decision, never an instruction to reverse it.
- (event) spines finalized
- (decision) Finished-node dock settled (owner): field and both controls absent. Settling it exposed a live contradiction between two State Patterns rows written at different points in this run — 'Node finishes on its own, dock removed' promised the draft and typed text are preserved, while 'Node finished while a stop was in flight' said the queued messages stay in the box; the box lives INSIDE the dock (IA row, Component Patterns row), so removing the dock removed the only surface that could keep the promise. Resolved by keeping the draft box rendered READ-ONLY when non-empty (header states the node finished and nothing was sent; the half-typed field line folds in as the last item), with no field and no controls. Queue empty means no dock at all, consistent with the existing 'Queue empty' row. Same tokens, no new colour — read-only is carried by the absent controls and the header word, never by a dimmed fill.
- (change) mockups/key-steering-dock.html gained states 5 and 6 — finished with something undelivered (read-only box, no field, no controls) and finished with nothing undelivered (no dock at all). New .pill.done built the same way as .pill.run/.pill.paused from existing tokens. 0 raw hex, tags balanced, mockups/ and .working/ byte-identical.
- (change) Follow-up sweep after the finished-node settle found three more places still enumerating the draft box header as exactly two modes, all missed because the phrase sweep looked for the losing side's words rather than for two-item lists: Voice and Tone microcopy row, the Component Patterns draft-box defining row, and control-states.md's draft box paragraph. All three now carry NEVER SENT alongside QUEUED and WILL SEND. Two remaining WILL SEND mentions are state-specific (the Node stopped row and the Flow 4 climax) and are correct. Column validator re-run file-wide across both spines and all three spec companions: consistent.
- (change) REFRAME propagation (owner-ratified; from spec+arch memlogs this session). Live agent steering was re-scoped from durable stop-and-resume to IN-SESSION INTERRUPT. Two engine facts these spines were built on are now FALSE: (F1) 'Stop is not instant — a poll of up to 10 seconds (dag-executor.ts:2304-2334)'; (F2) 'Stop pauses the whole run — no later layer starts'. Authoritative behaviour is now control-states.md (already updated). Both spines status:final; this is a consequence-propagation, not re-elicitation.
- (change) F1 FIX: interrupting the agent is a provider primitive (claude interrupt()) or a stream-abort — NEAR-INSTANT (sub-second), not a 10s DB poll. The 'Stopping…'/'interrupting' state shrinks to a sub-second UI-LOCAL OPTIMISTIC TRANSIENT (the brief window while the interrupt request is in flight); whether to render it at all is a product-owner call (control-states.md). Remove the 'ten seconds / dag-executor.ts:2304-2334' claim and the 'two poll intervals' timeout copy from the State Pattern. It is still not instant enough to fake-flip optimistically to done, but it is not a 10s wait.
- (change) F2 FIX (inverts prior guidance): Stop interrupts ONLY this agent's current generation; the node stays running and the REST OF THE RUN KEEPS ADVANCING (independent siblings, later layers). The old State Pattern 'Node stopped, run therefore paused' is wrong — rename to reflect: the node does not pause, the run is not paused; the interface must NOT imply the run stopped. Only this one agent's current thought was interrupted and this node is waiting for the operator.
- (change) CONTROL MODEL: the dock's stop control is 'Stop' = interrupt the AGENT's thinking (node keeps running), DISTINCT from the existing 'Cancel' button = stop the whole node+agent (existing teardown, untouched). Relabel 'Stop node' -> 'Stop' across the spine (Voice/Tone vocab row, Component Patterns Stop control, State Patterns). Node sub-states are the AGENT's: generating | idle-after-interrupt (node stays 'running'); 'interrupting' is the UI transient only. 'the node resumes' -> 'the agent continues on the same live session' (it never paused). Keep the honesty rationale for the explicit Stop; DROP the old mechanism rationale ('no provider has an inbound channel').
- (change) ROUND-4 deltas to propagate: (a) durable attempt-key {node_id,retry_epoch,iteration} is GONE — a steer to a not-live/other node is refused as 'not steerable here' via the in-memory registry, not a rejected attempt-key (fix the 'Viewing an execution that is not the live one' row's rationale). (b) Operator row now carries operator_user_id (MULTI-USER attribution: show WHICH operator sent it, two docks may steer one node, order = server receipt order) and message_id (reconciliation). (c) NEW State Pattern: interrupt-then-abandon -> node FAILS after 30 minutes ('agent stopped, waiting for you'; abandoned past 30 min -> node_failed, resume = fresh session). (d) 'Never sent' reconciliation now fires on ANY terminal (finish/Cancel/30-min fail), only on the node terminal event. (e) soft-inject 'Send now while running' (CAP-5) is SPIKE-GATED (claude only, unverified AsyncIterable+resume seam) — floor is Stop+Queue, not v1-ready. (f) a11y announcements: 'node stopping/stopped/resumed' -> agent sub-state 'interrupting/idle/generating' (node stays running).
- (change) Pending-ask rationale reframed (subagent flagged as stale): the Send block STAYS and its copy 'answer the agent's question first' is unchanged, but the stale 'resume refuses (dag-executor.ts:10367-10370)' rationale is replaced with the clean-model ground: a node parked at an AskHuman gate is pending/awaiting (executor returned via pauseOnAskHuman), not running, so it has no live steering handle -> the AD-11 'not steerable here' family; answering the ask brings it back live. Applied to State Pattern row + Flow 4 failure line. Also accepted subagent's 2 consistency edits (Foundation 'resume path'->'send/interrupt routes bound to in-process live handle'; CAP-5 intro spike-gate) + the duration-only fix inside the focus-ring bullet.
- (event) Reframe fully propagated into DESIGN.md + EXPERIENCE.md (10 EXPERIENCE items + DESIGN + pending-ask reframe). Both status:final, updated 2026-09-13. No new colour token / no raw hex. bmad-ux Update edits complete; reconcile pass clean.
- (change) Advisor re-check caught 3 semantic contradictions a grep sweep cannot see. Fix1 (scope): 'Run holds a pending ask' row retitled 'This node is parked at an ask' + Flow 4 failure line re-scoped to THIS node — a SIBLING's Ask-pause leaves this node running+steerable (verified: spine AD-4 + shouldContinueStreamingForStatus dag-executor.ts:700-702 returns true for run status 'paused'). Prior wording would disable Send on a steerable node whenever any other node asked a question.
- (change) Fix2 (queue branch): 'Node finished while a stop was in flight' row (retitled 'Turn ends on its own while a stop is in flight') + Flow 4 race line now branch on queue state. Per AD-4, a turn that emits its result before the interrupt lands is a NATURAL end (interrupt spent): queue non-empty auto-drains to turn N+1 (node keeps running), only an empty queue finishes. Prior rows asserted unconditional finish, stranding queued messages AD-4 says deliver.
- (change) Fix3 (universal interrupted glyph): 'Interrupted' tool-row now states steering makes the interrupted status row universal — executor writes it on EVERY provider at delivery, folded into the tool card via AD-10 (engine-integration build-list item 4); the Claude PostToolUseFailure hook stays the pre-existing source, not the only one. Prior row said Claude-only while SPEC/CAP-2 promises universal interrupt.
- (event) Reviewer Gate: accessibility lens (WCAG 2.2 AA) ran -> review-accessibility-steering.md. Verdict DONE_WITH_CONCERNS, 2 HIGH / 5 MED / 2 LOW / 0 critical. Steering half strong (dock a11y subsection + measured contrast table pre-answer most); gaps are what the reframe ADDED.
- (change) Applied 8 clear a11y-floor fixes (none reverses a decision). HIGH SC4.1.3-A: EXPERIENCE 232 now states a SERIALIZATION mechanism not a priority — coincident live-region writes coalesced into one composite announcement per transition (steering state last), sub-second interrupt = up to 3 writes/sec; delivery-failed-back-to-queue moved to its own ASSERTIVE role=alert (must-not-miss, no order among steering msgs otherwise); 'agent interrupting' suppressed when Stopping not shown. MED 4.1.3-B: EXPERIENCE 241 enumerates the 30-min-fail as a 5th announced status with distinct copy 'node failed - interrupted with no redirect - these never left this tab' + inherits finished-node focus/read-only-box. 4.1.3-C: 242 sub-state announcements carry consequence ('agent idle - Send now delivers'). MED contrast: DESIGN send-control gains blocked-legacy/console dim floor = text-secondary (mirrors stop-control stopping-*, aria-disabled forfeits SC1.4.3 exemption). MED keyboard: EXPERIENCE 202 Cmd/Ctrl+Enter gated by same aria-disabled condition as click. MED operator label: 247 resolves operator_user_id to users display name (short-id fallback), never a raw id. LOW: dock reduced-motion (231) + text-transform/SC2.5.3 build-notes (new bullet). Lint/sweep clean.
- (decision) SC 2.2.1 (HIGH) resolved — Kevin chose RESET-ON-ACTIVITY (of warn+extend / reset / both). EXPERIENCE row 174: the 30-min timer is an INACTIVITY timer, not a countdown from interrupt — any composer activity (keystroke/focus/Send now) re-arms it; disclosure states the limit up front ('no redirect ends this node after 30 min of inactivity - typing keeps it open') so it is never silent; the ratified 30-min value is unchanged. Also carried 175's focus rule into 174 (focus -> last transcript row if operator present at fail). Matches Kevin's 'bỏ đi'=walk-away intent: typing = not walked away.
- (event) spines finalized
