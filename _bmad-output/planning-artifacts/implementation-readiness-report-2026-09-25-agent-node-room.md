---
stepsCompleted:
  - step-01-document-discovery
  - step-02-prd-analysis
  - step-03-epic-coverage-validation
  - step-04-ux-alignment
  - step-05-epic-quality-review
  - step-06-final-assessment
targetSlug: agent-node-room
inputDocuments:
  - ../specs/spec-agent-node-room/SPEC.md
  - ../planning-artifacts/epics-agent-node-room/epics.md
  - ../planning-artifacts/architecture/architecture-Archon-readable-agent-transcript-2026-09-12/ARCHITECTURE-SPINE.md
  - ../planning-artifacts/architecture/architecture-Archon-live-agent-steering-2026-09-12/ARCHITECTURE-SPINE.md
  - ../planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09/DESIGN.md
  - ../planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09/EXPERIENCE.md
mockupManifest: mockup-manifests/agent-node-room.json
---

# Implementation Readiness Assessment Report

**Date:** 2026-09-25
**Project:** Archon — Agent Node Room

## Document Discovery

The user requested a fresh assessment of the explicit `agent-node-room` target after the approved Epic 4 and G3 planning corrections.
The selected set is the canonical `spec-agent-node-room` folder and its companion contracts, the target Epic, two named Architecture spines, and the target UX `DESIGN.md` and `EXPERIENCE.md`.
The Epic frontmatter names these inputs and the approved mockup handoff.
No same-lineage whole/sharded duplicate was found.
Other PRD, Epic, Architecture, and UX folders belong to other targets or are historical reviews.

| Type              | Selected document                                                                             | Size (bytes) | Modified (local) |
| ----------------- | --------------------------------------------------------------------------------------------- | -----------: | ---------------- |
| Requirements      | `../specs/spec-agent-node-room/SPEC.md` plus its frontmatter companion contracts              |       38,033 | 2026-09-24 23:31 |
| Epics and Stories | `epics-agent-node-room/epics.md`                                                              |       71,006 | 2026-09-24 23:32 |
| Architecture      | `architecture/architecture-Archon-readable-agent-transcript-2026-09-12/ARCHITECTURE-SPINE.md` |       42,285 | 2026-09-24 23:32 |
| Architecture      | `architecture/architecture-Archon-live-agent-steering-2026-09-12/ARCHITECTURE-SPINE.md`       |       50,646 | 2026-09-24 23:32 |
| UX                | `ux-designs/ux-Archon-agent-node-room-2026-09-09/DESIGN.md`                                   |       69,055 | 2026-09-24 23:32 |
| UX                | `ux-designs/ux-Archon-agent-node-room-2026-09-09/EXPERIENCE.md`                               |      172,665 | 2026-09-24 23:32 |

## Manifest preflight

The selected Epic frontmatter points to the approved Claude Design handoff, and its `inputDocuments` point to the same target lineage as the canonical SPEC, two Architecture spines, and two UX files.
The handoff README names four approved `.dc.html` sheets and the required `support.js`; the manifest records exactly those five sources.
The manifest also records current implementation or test sources and scoped completed-Epic evidence for its 70 classified product items.
The command `python3 .agents/skills/claude-design-feature-extractor/scripts/validate-mockup-manifest.py validate --root "$PWD" --expected-target agent-node-room _bmad-output/planning-artifacts/mockup-manifests/agent-node-room.json` exited 0 with `Manifest is valid and source hashes are current.`
The updated validator checks only unchanged historical Epics 1–3 for its completed-Epic comparison, so the revised Epic 4 text did not require re-extraction.
The mockup behavior values below are copied from that validated manifest and remain unchanged.

## Frozen mockup change boundary

The validated manifest records 70 current product items, 9 changed features, and 61 unchanged context items.
Its change boundary is: Current-scope changes are the TODO strip position on both rooms, the full-room shared queue disclosure and truthful accepted-queue label, per-item mid-turn Send now on true soft-inject providers, universal Run N occurrence headers, sender-free operator rows produced by accepted per-item Send now, and delivered status after evidence that the agent consumed the selected message.
The approved HTML conflicts with user decisions on the queue label, operator name, and delivered gate; the static refusal of per-item send applies only to queue-only providers.
Stored operator attribution remains intact.
The mockup behavior below is copied from the manifest before planning-document mapping.
No planning document supplies a mockup behavior field.

### Mockup Behavior Evidence Ledger

#### M001 — Console TODO strip placement and disclosure

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: `running prompt node`; inventory: I007; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                  | Mockup or user evidence                                                                                                                                                                                                                |
| --------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | One collapsible TODO strip on the Console node room directly below the transcript and above the steering dock | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip` — The TODO strip sits below the transcript and directly above the queue or dock.                  |
| precondition          | The selected agent node has todo calls                                                                        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip` — The selected running agent node has todo calls and a visible current todo item.                 |
| trigger               | The selected node room renders or the operator toggles the strip                                              | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#showTodoStrip` — The mockup enables the strip in the selected agent room.                                                      |
| target_identity       | The selected node todo strip                                                                                  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip` — The strip belongs to the selected implement node.                                               |
| target_cardinality    | One strip for the selected node                                                                               | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip` — One strip appears for the selected node.                                                        |
| timing                | Pinned and visible while the transcript scrolls                                                               | annotation: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;todo` — The todo checklist is pinned while the transcript scrolls.                                                         |
| effect_on_active_work | None; expanding the strip changes only its display                                                            | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo` — The toggle only changes the strip open state; agent work is not changed.                                       |
| collection_mutation   | None                                                                                                          | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo` — The toggle updates local disclosure state and does not alter a queue or transcript collection.                 |
| expected_result       | The current todo item and progress remain visible; expansion shows the checklist                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip` — The collapsed strip shows the current item and 4/6 progress; expansion exposes checklist items. |
| next_state            | The strip is collapsed or expanded while the node keeps its agent sub-state                                   | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#onToggleTodo` — The strip changes between collapsed and expanded while the selected node stays running.                        |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/experiments/console/components/ConsoleNodeRoom.tsx#RoomRegion` — The current room mounts TodoStrip before the transcript scroller, above the transcript.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5` — The completed Story describes the strip as pinned above the transcript; the mockup renders it below.

#### M002 — Legacy TODO strip placement and disclosure

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: `running prompt node`; inventory: I070; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                 | Mockup or user evidence                                                                                                                                                                                                               |
| --------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | One collapsible TODO strip on the Legacy node room directly below the transcript and above the steering dock | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip` — The TODO strip sits below the transcript and directly above the queue or dock.                  |
| precondition          | The selected agent node has todo calls                                                                       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip` — The selected running agent node has todo calls and a visible current todo item.                 |
| trigger               | The selected node room renders or the operator toggles the strip                                             | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#showTodoStrip` — The mockup enables the strip in the selected agent room.                                                      |
| target_identity       | The selected node todo strip                                                                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip` — The strip belongs to the selected implement node.                                               |
| target_cardinality    | One strip for the selected node                                                                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip` — One strip appears for the selected node.                                                        |
| timing                | Pinned and visible while the transcript scrolls                                                              | annotation: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;todo` — The todo checklist is pinned while the transcript scrolls.                                                        |
| effect_on_active_work | None; expanding the strip changes only its display                                                           | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo` — The toggle only changes the strip open state; agent work is not changed.                                       |
| collection_mutation   | None                                                                                                         | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo` — The toggle updates local disclosure state and does not alter a queue or transcript collection.                 |
| expected_result       | The current todo item and progress remain visible; expansion shows the checklist                             | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip` — The collapsed strip shows the current item and 4/6 progress; expansion exposes checklist items. |
| next_state            | The strip is collapsed or expanded while the node keeps its agent sub-state                                  | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#onToggleTodo` — The strip changes between collapsed and expanded while the selected node stays running.                        |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/NodeTranscriptPane.tsx#RoomRegion` — The current room mounts TodoStrip before the transcript scroller, above the transcript.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5` — The completed Story describes the strip as pinned above the transcript; the mockup renders it below.

#### M003 — Per-item Send now during generation

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: `agent generating on soft-inject transport`; inventory: I009; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                                                                                   | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | A Send now button on each queued item                                                                                                                                                          | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-send-now` — Every queued item has a Send now button.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| precondition          | An agent is generating on a soft-inject transport with a queued item in the live iteration                                                                                                     | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#canSendNow` — The code shows the button only with softInject, Queue mode, no Stop request in flight, and the live iteration selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| trigger               | The operator selects that queued item’s Send now button                                                                                                                                        | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow` — The per-item button calls that item’s sendNow handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| target_identity       | The queued message beside the selected button                                                                                                                                                  | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#draftItems.sendNow` — The handler is attached to one mapped queued item whose accessible label contains its text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| target_cardinality    | Exactly one selected queued item.                                                                                                                                                              | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| timing                | Immediately upon provider acceptance during the running turn, without waiting for turn end.                                                                                                    | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| effect_on_active_work | The active turn and tool call continue without interruption or a new turn.                                                                                                                     | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#stateNote=soft-inject` — The mockup note says this path does not interrupt the turn or current tool call.                                                                                                                                                                                                              |
| collection_mutation   | Exactly the selected accepted item leaves the shared queue; other queued items remain.                                                                                                         | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                         |
| expected_result       | The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name.                                                               | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| next_state            | That item is absent from the queue; its transcript row reads sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then delivered. | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa                                                                                                                                                                |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current queued-item JSX renders message text, sent, and Delete, but has no per-item Send now button.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.2` — The completed queue and withdraw stories define Queue and Delete, but no per-item soft-inject action.

#### M004 — Per-item Send now during generation

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: `agent generating on soft-inject transport`; inventory: I029; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                                                                                   | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | A Send now button on each queued item                                                                                                                                                          | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-send-now` — Every queued item has a Send now button.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| precondition          | An agent is generating on a soft-inject transport with a queued item in the live iteration                                                                                                     | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#canSendNow` — The code shows the button only with softInject, Queue mode, no Stop request in flight, and the live iteration selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| trigger               | The operator selects that queued item’s Send now button                                                                                                                                        | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow` — The per-item button calls that item’s sendNow handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| target_identity       | The queued message beside the selected button                                                                                                                                                  | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#draftItems.sendNow` — The handler is attached to one mapped queued item whose accessible label contains its text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| target_cardinality    | Exactly one selected queued item.                                                                                                                                                              | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| timing                | Immediately upon provider acceptance during the running turn, without waiting for turn end.                                                                                                    | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| effect_on_active_work | The active turn and tool call continue without interruption or a new turn.                                                                                                                     | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#stateNote=soft-inject` — The mockup note says this path does not interrupt the turn or current tool call.                                                                                                                                                                                                               |
| collection_mutation   | Exactly the selected accepted item leaves the shared queue; other queued items remain.                                                                                                         | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                         |
| expected_result       | The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name.                                                               | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| next_state            | That item is absent from the queue; its transcript row reads sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then delivered. | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa                                                                                                                                                                |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current queued-item JSX renders message text, sent, and Delete, but has no per-item Send now button.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.2` — The completed queue and withdraw stories define Queue and Delete, but no per-item soft-inject action.

#### M005 — Shared queued-message band and disclosure

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html`; rendered state: `agent generating with accepted queued guidance`; inventory: I008; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                                        | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label. | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list` — The full room shows the QUEUED count, ordered rows, and a misleading this-tab-only label.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                             |
| precondition          | At least one accepted message is queued for the selected live node.                                                                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list` — Two queued items are visible.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                                                                                         |
| trigger               | The operator selects the queue disclosure header.                                                                                                   | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The queue header invokes onToggleDraft.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| target_identity       | The selected node's shared accepted-message queue band.                                                                                             | user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The handler changes the draftOpen state used by the queued list.                                                                                                                                                             |
| target_cardinality    | One band for the selected node.                                                                                                                     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list` — One QUEUED band contains the accepted items.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| timing                | The band expands or collapses immediately when selected.                                                                                            | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The handler updates local draftOpen state directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| effect_on_active_work | None; disclosure changes only the queue display.                                                                                                    | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The handler only toggles draftOpen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| collection_mutation   | None; opening or closing the band does not add, send, or delete a queued item.                                                                      | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The handler only toggles draftOpen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| expected_result       | The operator can show or hide the accepted rows while their shared count and order remain available.                                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list` — The expanded band shows its accepted rows.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The disclosure toggles row visibility.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue. |
| next_state            | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                          | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — The state changes only draftOpen.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                                                                                            |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/experiments/console/components/ConsoleComposerDock.tsx#queueBandHeader` — The current room shows a flat accepted-message band with sent and Delete, without mockup ordinals, next-out mark, or disclosure.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1` — The completed story covers a shared QUEUED list and local unsent draft; it does not define the mockup disclosure and row treatment.

#### M006 — Shared queued-message band and disclosure

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html`; rendered state: `agent generating with accepted queued guidance`; inventory: I028; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                                        | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| visible_presentation  | A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label. | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list` — The full room shows the QUEUED count, ordered rows, and a misleading this-tab-only label.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                            |
| precondition          | At least one accepted message is queued for the selected live node.                                                                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list` — Two queued items are visible.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                                                                                        |
| trigger               | The operator selects the queue disclosure header.                                                                                                   | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The queue header invokes onToggleDraft.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| target_identity       | The selected node's shared accepted-message queue band.                                                                                             | user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The handler changes the draftOpen state used by the queued list.                                                                                                                                                            |
| target_cardinality    | One band for the selected node.                                                                                                                     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list` — One QUEUED band contains the accepted items.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| timing                | The band expands or collapses immediately when selected.                                                                                            | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The handler updates local draftOpen state directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| effect_on_active_work | None; disclosure changes only the queue display.                                                                                                    | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The handler only toggles draftOpen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| collection_mutation   | None; opening or closing the band does not add, send, or delete a queued item.                                                                      | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The handler only toggles draftOpen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| expected_result       | The operator can show or hide the accepted rows while their shared count and order remain available.                                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list` — The expanded band shows its accepted rows.<br>interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The disclosure toggles row visibility.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue. |
| next_state            | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                          | interaction_code: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — The state changes only draftOpen.<br>user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.                                                                                                                                                                                           |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/ComposerDock.tsx#queueBandHeader` — The current room shows a flat accepted-message band with sent and Delete, without mockup ordinals, next-out mark, or disclosure.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1` — The completed story covers a shared QUEUED list and local unsent draft; it does not define the mockup disclosure and row treatment.

#### M007 — Run N occurrence separator

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html`; rendered state: `multi-occurrence node`; inventory: I052; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                   | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | Run 1, Run 2, and later Run N separators, with a contextual suffix such as retry or iteration when applicable. | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The first displayed variant shows Run 1, Run 2 · retry, and Run 3 · iteration 2.<br>user_answer: `user_answer:coordinator-260924-occurrence` — For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator. |
| precondition          | The selected node has more than one occurrence.                                                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note` — The sheet says separators appear only for more than one occurrence.<br>user_answer: `user_answer:coordinator-260924-occurrence` — For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.          |
| trigger               | The transcript renders occurrence groups.                                                                      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The mockup renders separators between occurrence groups.                                                                                                                                                                                                                                                                                                                                                                       |
| target_identity       | The selected node's transcript occurrence groups.                                                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The separators are shown in the occurrence-header section.                                                                                                                                                                                                                                                                                                                                                                     |
| target_cardinality    | One separator per occurrence when there are multiple occurrences.                                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The first variant renders a separator for each of three occurrences.<br>user_answer: `user_answer:coordinator-260924-occurrence` — For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.             |
| timing                | When the multi-occurrence transcript is rendered.                                                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note` — The sheet states the condition for rendering the headers.                                                                                                                                                                                                                                                                                                                                                                  |
| effect_on_active_work | None; the separator labels existing transcript history.                                                        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The section presents read-only transcript headers.                                                                                                                                                                                                                                                                                                                                                                             |
| collection_mutation   | None; displaying separators does not alter transcript rows.                                                    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The section presents read-only transcript headers.                                                                                                                                                                                                                                                                                                                                                                             |
| expected_result       | Each occurrence has a Run N header with the relevant suffix; Pass N and reason-only are not primary headers.   | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options` — The first variant shows numbered Run headers with retry or iteration suffixes.<br>user_answer: `user_answer:coordinator-260924-occurrence` — For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.   |
| next_state            | The multi-occurrence transcript remains grouped under Run N headers; a single occurrence has no separator.     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note` — The sheet omits the header for one occurrence.<br>user_answer: `user_answer:coordinator-260924-occurrence` — For multi-occurrence transcript separators, use the first mockup variant universally: Run 1, Run 2, etc., with a contextual suffix such as retry or iteration when applicable; do not choose Pass N or reason-only as the primary header. A single occurrence has no separator.                               |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/lib/occurrence-groups.ts#factsOf` — The current grouping labels loop occurrences Iteration N instead of a universal Run N header.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed story selects Run, Iteration, Pass, or reason-only by context; the user selected universal Run N with suffixes.

#### M008 — Operator transcript row without sender name

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html`; rendered state: `operator exchange`; inventory: I063; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                                            | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| visible_presentation  | An operator message row with full-strength text and a sent badge, without the sender's name.                                                            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=operator-record;element=operator-rows` — The approved static example shows operator · kevin and sent; the user changed only the name display.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| precondition          | One selected message is accepted by the provider and recorded in the transcript.                                                                        | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| trigger               | That accepted operator message renders in the transcript.                                                                                               | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| target_identity       | The transcript row for that selected stamped operator message.                                                                                          | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| target_cardinality    | Exactly one transcript row for the selected item.                                                                                                       | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| timing                | Immediately on provider acceptance.                                                                                                                     | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| effect_on_active_work | None from the name display; stored attribution remains intact.                                                                                          | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| collection_mutation   | None from the visual name treatment; provider acceptance still removes the selected queue item and creates its operator transcript row.                 | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| expected_result       | The row shows the operator message and sent badge without the sender's name.                                                                            | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                             |
| next_state            | The row remains sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then reads delivered. | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa                                                                    |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory` — The current operator row includes operatorDisplayName in its label and a sent badge.
- **implementation:** `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory` — The Console row also includes operatorDisplayName in its label.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8` — The completed story requires operator · <display name> and a sent badge; the user removed the visible name only.

#### M009 — Delivered operator-message status

Mockup: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html`; rendered state: `message status after provider confirmation`; inventory: I068; scope: CURRENT; manifest status: PROVEN.

| Behavior field        | Frozen value                                                                                                                        | Mockup or user evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| visible_presentation  | A delivered badge on the matching operator message row.                                                                             | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered` — The status examples render a delivered badge.<br>user_answer: `user_answer:coordinator-260924-delivery` — Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| precondition          | The selected operator transcript row is sent and agent consumption can be established for that message.                             | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-delivery` — Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa |
| trigger               | A matching provider lifecycle event or a response stream causally linked to the selected message proves that the agent consumed it. | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa                                                                                                                                                                               |
| target_identity       | The operator transcript row with that matching stamped message_id.                                                                  | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                                        |
| target_cardinality    | Exactly one matching operator message row.                                                                                          | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                                        |
| timing                | After evidence that the agent consumed the selected message, not merely after transport acceptance or unrelated stream output.      | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-stream-consumption` — khi agent nhận message, agent có stream lại mà, nếu harness không có thì phải dựa vào đó để biết delivery hay chưa                                                                                                                                                                               |
| effect_on_active_work | None; confirmation updates the displayed delivery status.                                                                           | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                                        |
| collection_mutation   | None; the row stays in the transcript and only its status changes.                                                                  | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.                                                                                                                                                                                                                                                                                                                                                                        |
| expected_result       | The matching row changes from sent to delivered.                                                                                    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered` — The sheet shows the delivered status example.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-delivery` — Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.  |
| next_state            | The matching operator row reads delivered; unconfirmed rows continue to read sent.                                                  | user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered.<br>user_answer: `user_answer:coordinator-260924-delivery` — Delivered is current scope, not future; any SDK update needed for confirmation is an implementation prerequisite.                                                                                                                                                                                          |

Classification evidence from the current implementation and completed Epic:

- **implementation:** `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory` — The current row always renders sent and has no confirmation transition.
- **implementation:** `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory` — The Console row always renders sent and has no confirmation transition.
- **epic:** `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8` — The completed story leaves every v1 provider row at sent; its later delivered transition is not part of that completed baseline.

### Unchanged Context Inventory

Every row below is `CURRENT` product context with role `UNCHANGED_CONTEXT`, not newly requested Epic 4 work.
Each row copies its exact mockup evidence and current-implementation plus completed-Epic classification evidence.

| ID   | Mockup and state                                                                                                                   | Visible control or state                                | Mockup evidence                                                                                                                                                                                                                                                | Inventory ID | Scope and role              | Why unchanged                                                                                                                                                                                             | Current implementation evidence                                                                                                                                                                                                 | Completed Epic evidence                                                                                                                                                                         |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C001 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Run shell, Running status, and separate Cancel control  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=run-header` — The run header shows Running and Cancel above the node panel.                                                           | I001         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/routes/RunDetailPage.tsx#RunDetailPage` — The current product implements run shell, running status, and separate cancel control on this surface.                          | epic: `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5` — The completed Epic records the existing run shell, running status, and separate cancel control contract. |
| C002 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Log, Graph, Artifacts, Tool calls, and System controls  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=run-tabs` — Log, Graph, Artifacts 2, Tool calls, and System appear above the log.                                                     | I002         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/routes/RunDetailPage.tsx#RunDetailPage` — The current product implements log, graph, artifacts, tool calls, and system controls on this surface.                          | epic: `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5` — The completed Epic records the existing log, graph, artifacts, tool calls, and system controls contract. |
| C003 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Node log with completed, running, and pending steps     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=step-list` — Completed command and bash steps, the running implement prompt, and pending speckit-verify are visible.                  | I003         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/components/ConsoleNodeRoom.tsx#ConsoleNodeRoom` — The current product implements node log with completed, running, and pending steps on this surface.                     | epic: `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5` — The completed Epic records the existing node log with completed, running, and pending steps contract.    |
| C004 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Selected implement node transcript and close control    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=node-panel` — The selected PROMPT implement panel shows Running, transcript rows, and a close button.                                 | I004         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/components/inspect/ConsoleRoomHeader.tsx#ConsoleRoomHeader` — The current product implements selected implement node transcript and close control on this surface.        | epic: `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5` — The completed Epic records the existing selected implement node transcript and close control contract.   |
| C005 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Assistant text and tool calls grouped by run            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=transcript` — RUN 1, RUN 2 · RETRY, assistant text, and tool calls appear in order.                                                   | I005         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#ConsoleAgentHistoryList` — The current product implements assistant text and tool calls grouped by run on this surface.    | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing assistant text and tool calls grouped by run contract.                  |
| C006 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node                       | Expandable tool row and Raw control                     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=tool-row` — A failed terminal command is expanded with a Raw button and terminal output.                                              | I006         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#ToolHistory` — The current product implements expandable tool row and raw control on this surface.                         | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing expandable tool row and raw control contract.                           |
| C010 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating                          | Per-item Delete controls                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-delete` — Each queued item has a separate delete button.                                                                       | I010         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current product implements per-item delete controls on this surface.                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.2` — The completed Epic records the existing per-item delete controls contract.                                   |
| C011 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating                          | Message composer and Queue control                      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=composer` — Message the agent and Queue appear while the agent generates.                                                             | I011         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#composer` — The current product implements message composer and queue control on this surface.                                                          | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing message composer and queue control contract.                   |
| C012 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating                          | Stop current agent turn                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=stop` — Stop appears in the dock while Cancel remains in the run header.                                                              | I012         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#stop` — The current product implements stop current agent turn on this surface.                                                                         | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing stop current agent turn contract.                              |
| C013 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent interrupting                        | Stopping transient while node remains running           | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=interrupting;element=stopping` — The dock shows Stopping… and Queue; the node still shows Running.                                                       | I013         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#interrupting` — The current product implements stopping transient while node remains running on this surface.                                           | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing stopping transient while node remains running contract.        |
| C014 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent idle after interrupt                | Will send queue and Send now control                    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=idle;element=will-send` — The dock shows WILL SEND 2, Send now, and no Stop control.                                                                     | I014         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#idle` — The current product implements will send queue and send now control on this surface.                                                            | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing will send queue and send now control contract.                 |
| C015 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent idle after interrupt                | Interrupted tool glyph and retained-file disclosure     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=idle;element=interrupt-warning` — The tool row shows ⚠ interrupted and text says files already written stay written.                                     | I015         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#STEERING_INTERRUPT_DISCLOSURE` — The current product implements interrupted tool glyph and retained-file disclosure on this surface.                                     | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing interrupted tool glyph and retained-file disclosure contract.  |
| C016 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating again                    | Operator messages in transcript and active work resumes | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=again;element=operator-rows` — Two OPERATOR · KEVIN sent rows precede the assistant response and running command.                                        | I016         | CURRENT · UNCHANGED_CONTEXT | The Console transcript already records operator messages in sequence and resumes generation; M008 separately records the user-selected removal of the visible sender name.                                | implementation: `packages/web/src/experiments/console/components/inspect/ConsoleAgentHistoryList.tsx#OperatorHistory` — The current product implements operator messages in transcript and active work resumes on this surface. | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8` — The completed Epic records the existing operator messages in transcript and active work resumes contract.    |
| C017 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · completed node with undelivered messages  | Read-only Never sent draft list                         | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=finishedUndelivered;element=never-sent` — NEVER SENT 2 appears without a composer or send controls.                                                      | I017         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished` — The current product implements read-only never sent draft list on this surface.                                                             | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing read-only never sent draft list contract.                    |
| C018 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · completed clean node                      | No steering dock                                        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=finishedClean;element=no-dock` — The completed node has no draft block or composer.                                                                      | I018         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#steeringDockMode` — The current product implements no steering dock on this surface.                                                                    | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing no steering dock contract.                                   |
| C019 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · failed after idle interrupt               | Failed 30-minute state and Never sent drafts            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=failed;element=idle-failure` — The node shows failed after 30m idle with an interrupted row and NEVER SENT 2.                                            | I019         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#neverSentDisclosure` — The current product implements failed 30-minute state and never sent drafts on this surface.                                                      | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing failed 30-minute state and never sent drafts contract.       |
| C020 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · detached running node                     | Not steerable disclosure without composer               | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=detached;element=not-steerable` — The running node says not steerable here and has no composer.                                                          | I020         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE` — The current product implements not steerable disclosure without composer on this surface.                                                | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing not steerable disclosure without composer contract.          |
| C021 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running loop node                         | Per-iteration log entries and Execution selector        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=loop-live;element=execution-select` — Implement ×1, ×2, ×3 appear and Execution selects Iteration 3 · running.                                           | I021         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/experiments/console/components/inspect/ConsoleRoomHeader.tsx#ConsoleRoomHeader` — The current product implements per-iteration log entries and execution selector on this surface.            | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing per-iteration log entries and execution selector contract.           |
| C022 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · finished loop iteration selected          | Read-only old iteration and Go to live iteration        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=loop-iter-1;element=go-live` — Iteration 1 transcript is shown with a Go to iteration 3 button instead of steering controls.                             | I022         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished-iteration` — The current product implements read-only old iteration and go to live iteration on this surface.                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing read-only old iteration and go to live iteration contract.           |
| C023 | `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running queue-only provider               | Per-item Send now absent                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=codex-queue-only;element=queued-list` — QUEUED items retain delete buttons but no per-item Send now buttons.                                             | I023         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current product implements per-item send now absent on this surface.                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing per-item send now absent contract.                             |
| C024 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running prompt node                        | Legacy run shell, status, Cancel, and tabs              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=run-header` — Legacy shows Running, Cancel, Logs, Graph, Chat, Files changed, and Artifacts 4.                                         | I024         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/WorkflowExecution.tsx#WorkflowExecution` — The current product implements legacy run shell, status, cancel, and tabs on this surface.                                    | epic: `_bmad-output/planning-artifacts/epics-workflow-run-view-hitl/epics.md#Epic-5` — The completed Epic records the existing legacy run shell, status, cancel, and tabs contract.             |
| C025 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running prompt node                        | Legacy node list with two implement runs                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=node-list` — The list shows completed steps, a failed first implement run, a running second run, and pending verify.                   | I025         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/WorkflowExecution.tsx#buildLogRows` — The current product implements legacy node list with two implement runs on this surface.                                           | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing legacy node list with two implement runs contract.                   |
| C026 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running prompt node                        | Selected implement transcript and Execution selector    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=node-panel` — The selected implement panel contains an Execution selector and grouped RUN 1/RUN 2 transcript.                          | I026         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoomHeader.tsx#NodeRoomHeader` — The current product implements selected implement transcript and execution selector on this surface.                                | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing selected implement transcript and execution selector contract.       |
| C027 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running prompt node                        | Expandable tool rows and Raw output                     | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=tool-rows` — The Legacy transcript has collapsed and expanded tool rows; the open row has a Raw button.                                | I027         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#ToolHistory` — The current product implements expandable tool rows and raw output on this surface.                                                          | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing expandable tool rows and raw output contract.                           |
| C030 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent interrupting                         | Stopping transient                                      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=interrupting;element=stopping` — Stopping… and Queue appear while the node remains Running.                                                               | I030         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#interrupting` — The current product implements stopping transient on this surface.                                                                      | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing stopping transient contract.                                   |
| C031 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent idle after interrupt                 | Will send and Send now with interrupted tool            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=idle;element=will-send` — WILL SEND 2 and Send now appear with ⚠ interrupted tool output.                                                                 | I031         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#idle` — The current product implements will send and send now with interrupted tool on this surface.                                                    | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing will send and send now with interrupted tool contract.         |
| C032 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent generating again                     | Operator correction in transcript                       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=again;element=operator-rows` — Operator rows appear between interrupted and redirected shell commands.                                                    | I032         | CURRENT · UNCHANGED_CONTEXT | The Legacy transcript already records operator correction in sequence; M008 separately records the user-selected removal of the visible sender name.                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory` — The current product implements operator correction in transcript on this surface.                                                        | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8` — The completed Epic records the existing operator correction in transcript contract.                          |
| C033 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · completed with undelivered messages        | Read-only Never sent list                               | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=finishedUndelivered;element=never-sent` — NEVER SENT 2 remains visible without field or controls.                                                         | I033         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished` — The current product implements read-only never sent list on this surface.                                                                   | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing read-only never sent list contract.                          |
| C034 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · completed clean node                       | No steering dock                                        | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=finishedClean;element=no-dock` — The completed node transcript reaches the panel bottom without a dock.                                                   | I034         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#steeringDockMode` — The current product implements no steering dock on this surface.                                                                    | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing no steering dock contract.                                   |
| C035 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · failed after idle interrupt                | Failure and Never sent list                             | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=failed;element=idle-failure` — The node is Failed after 30m idle and shows NEVER SENT 2.                                                                  | I035         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#neverSentDisclosure` — The current product implements failure and never sent list on this surface.                                                                       | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing failure and never sent list contract.                        |
| C036 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · detached running node                      | Not steerable disclosure                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=detached;element=not-steerable` — The panel says not steerable here and offers no composer.                                                               | I036         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE` — The current product implements not steerable disclosure on this surface.                                                                 | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing not steerable disclosure contract.                           |
| C037 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running loop node                          | Iteration selector and per-iteration entries            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=loop-live;element=execution-select` — The node list shows implement ×1, ×2, ×3 and Execution selects Iteration 3.                                         | I037         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoomHeader.tsx#NodeRoomHeader` — The current product implements iteration selector and per-iteration entries on this surface.                                        | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing iteration selector and per-iteration entries contract.               |
| C038 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · finished loop iteration selected           | Read-only earlier iteration and live navigation         | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=loop-iter-1;element=go-live` — The old iteration transcript is shown without steering controls and offers live navigation.                                | I038         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished-iteration` — The current product implements read-only earlier iteration and live navigation on this surface.                                   | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing read-only earlier iteration and live navigation contract.            |
| C039 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running queue-only provider                | Per-item Send now absent                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=codex-queue-only;element=queued-items` — The queue-only transport shows Delete but no per-item Send now.                                                  | I039         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current product implements per-item send now absent on this surface.                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing per-item send now absent contract.                             |
| C040 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · tool family palette                       | Tool name chips use family colour                       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=A;element=tool-chips` — Shell, file, web, search, todo, task, and generic chips use distinct existing node token colours.                              | I040         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#CHIP_TONE` — The current product implements tool name chips use family colour on this surface.                                                              | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing tool name chips use family colour contract.                             |
| C041 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · tool status key                           | Five status glyphs                                      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=B;element=status-glyphs` — ✓, ✕, ◐, ⚠, and – label succeeded, failed, running, interrupted, and unknown.                                               | I041         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/tool-presentation.ts#OUTCOME_GLYPH` — The current product implements five status glyphs on this surface.                                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing five status glyphs contract.                                            |
| C042 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · collapsed tool rows                       | Family-specific one-line summaries                      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=C;element=family-rows` — Shell, file, code, search, glob, web, todo, task, and generic rows show name, summary, and result badge.                      | I042         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/tool-presentation.ts#toolRowPresentation` — The current product implements family-specific one-line summaries on this surface.                                                            | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing family-specific one-line summaries contract.                            |
| C043 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · collapsed long path                       | Middle-elided path preserves filename                   | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=C;element=long-path` — The narrow row retains ConsoleAgentHistoryList.tsx while shortening its parent path.                                            | I043         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#ToolHeadline` — The current product implements middle-elided path preserves filename on this surface.                                                       | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing middle-elided path preserves filename contract.                         |
| C044 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · collapsed Codex shell row                 | Multiline script headline and shell fallback chip       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=C;element=codex-shell` — The seven-line shell name becomes a first-line headline with ellipsis and a shell chip.                                       | I044         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/tool-presentation.ts#shellHeadline` — The current product implements multiline script headline and shell fallback chip on this surface.                                                   | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing multiline script headline and shell fallback chip contract.             |
| C045 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · collapsed MCP row                         | Server and tool headline                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=C;element=mcp-tool` — The row shows gitnexus · query and a result count.                                                                               | I045         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/tool-presentation.ts#mcpLabel` — The current product implements server and tool headline on this surface.                                                                                 | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing server and tool headline contract.                                      |
| C046 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · collapsed unknown tool row                | Generic scalar summary                                  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=C;element=unknown-tool` — The row shows command_id and tail as scalar key-value snippets under a generic chip.                                         | I046         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/tool-presentation.ts#genericFacts` — The current product implements generic scalar summary on this surface.                                                                               | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing generic scalar summary contract.                                        |
| C047 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded shell row                        | Terminal output body and Raw control                    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;element=shell-body` — The failed shell row is expanded with command, failure output, and Raw.                                                        | I047         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#ToolBodySwitch` — The current product implements terminal output body and raw control on this surface.                                                      | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing terminal output body and raw control contract.                          |
| C048 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded file row                         | Inline edit diff body                                   | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;element=file-body` — The expanded Edit row shows red removed and green added lines.                                                                  | I048         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#InlineDiff` — The current product implements inline edit diff body on this surface.                                                                         | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing inline edit diff body contract.                                         |
| C049 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded web, search, glob, and code rows | Family-specific bodies                                  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;element=web-search-glob-code` — Expanded examples show a web document, path:line search hits, glob paths, and highlighted code.                      | I049         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#ToolBodySwitch` — The current product implements family-specific bodies on this surface.                                                                    | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing family-specific bodies contract.                                        |
| C050 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded todo row                         | Folded latest checklist and earlier call summaries      | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;element=todo-body` — The latest todo row shows a checklist; earlier todo calls say todo updated.                                                     | I050         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeTranscriptPane.tsx#TodoStrip` — The current product implements folded latest checklist and earlier call summaries on this surface.                                   | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.5` — The completed Epic records the existing folded latest checklist and earlier call summaries contract.         |
| C051 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded task row                         | Batch and single-agent dispatch bodies                  | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=D;element=task-body` — The batch row lists two subtask cards and the Agent row shows one dispatch.                                                     | I051         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#TaskBody` — The current product implements batch and single-agent dispatch bodies on this surface.                                                          | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing batch and single-agent dispatch bodies contract.                        |
| C053 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · single-occurrence node                    | No occurrence header                                    | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=single-occurrence-note` — The sheet states that a one-occurrence node has no header.                                                         | I053         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/occurrence-groups.ts#groupByOccurrence` — The current product implements no occurrence header on this surface.                                                                            | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-1.7` — The completed Epic records the existing no occurrence header contract.                                       |
| C054 | `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · expanded tool row                         | Raw JSON toggle                                         | rendered: `claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=F;element=raw-toggle` — The expanded read_file row shows Raw ▾ and provider JSON.                                                                      | I054         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#ToolHistory` — The current product implements raw json toggle on this surface.                                                                              | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Epic-1` — The completed Epic records the existing raw json toggle contract.                                               |
| C055 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · agent generating                       | Composer, Queue, and Stop beneath transcript            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=generating;element=dock` — The dock shows queued messages, Message the agent, Stop, and Queue.                                                        | I055         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#composer` — The current product implements composer, queue, and stop beneath transcript on this surface.                                                | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing composer, queue, and stop beneath transcript contract.         |
| C056 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · agent interrupting                     | Stopping transient and Queue                            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=interrupting;element=dock` — Stopping… is shown while Queue stays available and node remains running.                                                 | I056         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#interrupting` — The current product implements stopping transient and queue on this surface.                                                            | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing stopping transient and queue contract.                         |
| C057 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · agent idle after interrupt             | Will send, Send now, and retained-file disclosure       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=idle;element=dock` — WILL SEND 2, Send now, and a files-already-written warning appear.                                                               | I057         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#idle` — The current product implements will send, send now, and retained-file disclosure on this surface.                                               | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing will send, send now, and retained-file disclosure contract.    |
| C058 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · agent generating again                 | Resume with Stop and Queue                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=again;element=dock` — The dock returns to Stop and Queue without an empty draft box.                                                                  | I058         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#composer` — The current product implements resume with stop and queue on this surface.                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing resume with stop and queue contract.                           |
| C059 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · completed with undelivered messages    | Read-only Never sent state                              | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=finishedUndelivered;element=dock` — NEVER SENT 2 appears without field, Stop, or send.                                                                | I059         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished` — The current product implements read-only never sent state on this surface.                                                                  | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing read-only never sent state contract.                         |
| C060 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · completed clean node                   | No dock                                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=finishedClean;element=no-dock` — The sheet shows a completed node with no dock.                                                                       | I060         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#steeringDockMode` — The current product implements no dock on this surface.                                                                                              | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing no dock contract.                                            |
| C061 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · failed after 30-minute idle            | Failure and Never sent state                            | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=failed;element=dock` — The failed node shows NEVER SENT 2 and the 30-minute timeout text.                                                             | I061         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#neverSentDisclosure` — The current product implements failure and never sent state on this surface.                                                                      | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing failure and never sent state contract.                       |
| C062 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · detached running node                  | Not steerable disclosure                                | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#state=detached;element=dock` — The detached node says not steerable here and has no field.                                                                  | I062         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#STEERING_DETACHED_DISCLOSURE` — The current product implements not steerable disclosure on this surface.                                                                 | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing not steerable disclosure contract.                           |
| C064 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · message statuses                       | Sent message status example                             | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=sent` — An operator message card shows the sent badge.                                                                               | I064         | CURRENT · UNCHANGED_CONTEXT | The sent badge remains the unconfirmed operator-message state; M009 separately records the delivered transition after evidence that the agent consumed that message.                                      | implementation: `packages/web/src/components/workflows/NodeRoom.tsx#OperatorHistory` — The current product implements sent message status example on this surface.                                                              | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.8` — The completed Epic records the existing sent message status example contract.                                |
| C066 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · running agent                          | No Enter-to-send shortcut                               | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=refused;element=enter-to-send` — The refused-actions table says Enter must not send to a running agent.                                             | I066         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/lib/steering-dock.ts#isQueueShortcut` — The current product implements no enter-to-send shortcut on this surface.                                                                             | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.1-2.7` — The completed Epic records the existing no enter-to-send shortcut contract.                            |
| C067 | `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent generating                           | Per-item Delete control                                 | rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-delete` — Each queued item has its own Delete button.                                                                           | I067         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current product implements per-item delete control on this surface.                                                                   | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.2` — The completed Epic records the existing per-item delete control contract.                                    |
| C069 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · message status example                 | Never sent message status example                       | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=never-sent` — A read-only NEVER SENT 1 card holds an undelivered message.                                                            | I069         | CURRENT · UNCHANGED_CONTEXT | The completed product already presents this control or state with the same operator-visible purpose.                                                                                                      | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#finished` — The current product implements never sent message status example on this surface.                                                           | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Stories-2.11-2.12` — The completed Epic records the existing never sent message status example contract.                  |
| C065 | `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · refused-actions table                  | Per-item send absent for a queue-only provider          | rendered: `claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=refused;element=per-item-send` — The refused-actions table states that per-item send while generating is unavailable, without a provider qualifier. | I065         | CURRENT · UNCHANGED_CONTEXT | The current queue-only path has no per-item send. The static sheet refuses this action generally, but the user limits the refusal to queue-only providers and approves it for true soft-inject providers. | implementation: `packages/web/src/components/workflows/ComposerDock.tsx#queue-list` — The current queued list offers Delete but no per-item Send now, including queue-only operation.                                           | epic: `_bmad-output/planning-artifacts/epics-agent-node-room/epics.md#Story-2.1` — The completed queue story provides Queue and a queued list, without a per-item action.                       |

### Frozen Action Identity Audit

| Shared label or intent | Feature IDs | Manifest outcome | Recorded differences | Decision evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------- | ----------- | ---------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| send-queued-item-now   | M003, M004  | SAME_ACTION      | []                   | user_answer: `user_answer:coordinator-260924-send` — Per-item Send now during generation is current for a provider that can truly soft-inject mid-turn. It sends exactly the selected queued item immediately into the active turn, without waiting for the turn to end or interrupting work; queue-only providers do not show this per-item action.<br>user_answer: `user_answer:coordinator-260924-receipt` — On provider acceptance, exactly that selected item leaves the queue and appears immediately in the transcript as an operator message. Do NOT show the sender's name on that transcript row. This is a visual decision only; the user did not authorize removing stored attribution. Show sent until the provider confirms the same stamped message_id, then delivered. |
| toggle-shared-queue    | M005, M006  | SAME_ACTION      | []                   | user_answer: `user_answer:coordinator-260924-queue` — Once Queue accepts a message, the queue is shared across tabs/operators. Only text still typed but not submitted is this-tab-only. The this-tab-only label beside QUEUED 2 in the mockup is misleading; record the conflict, do not infer a tab-local accepted queue.<br>rendered: `claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#handler=onToggleDraft` — Console disclosure toggles its accepted-list display.<br>rendered: `claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#handler=onToggleDraft` — Legacy disclosure toggles its accepted-list display.                                                                                          |

The manifest records no unknown behavior field, unclear feature, unclear action identity group, or open behavior question.

## PRD Analysis

The canonical `SPEC.md` is the target requirements document.
Its eight local companions refine the CAP contracts and define tests, provider behavior, API types, and UI states.

### Functional Requirements

#### FR1 — CAP-1

- **CAP-1** — Scan a node's work without expanding anything
  - **intent:** A reader can scan a node's tool calls one line each and tell what ran, what it ran on, and whether it worked.
  - **success:** A node with forty tool calls renders forty single-line rows, each carrying a family chip, a status glyph, a headline naming the salient argument, and right-aligned badges.
    Successful calls are collapsed on first render and failed calls are expanded.
    No collapsed row contains serialized-data punctuation.

#### FR2 — CAP-2

- **CAP-2** — Expand a call into a body shaped for that kind of tool
  - **intent:** A reader can open any tool call and see its input and output rendered for the kind of tool it is.
  - **success:** Every family renders its declared body arm per `tool-presentation-contract.md`.
    A tool matching no family renders at most three scalar `key: value` pairs, with objects and arrays collapsed to `{…}` / `[n]`, and never a JSON dump.
    Measured against the production corpus, the generic fallback claims under 2% of rows.

#### FR3 — CAP-3

- **CAP-3** — Follow the agent's checklist as state, not as mutations
  - **intent:** A reader can see the agent's current todo list, with phases and per-item status, instead of a sequence of opaque updates.
  - **success:** Both provider shapes normalize to the same `TodoPhase[]` per `todo-fold-contract.md`.
    Every todo call in the transcript collapses to a one-line row; the current checklist lives only in the pinned todo strip, not inline in the transcript.
    A phase emptied by `rm` disappears rather than rendering an empty header.
    The **pinned todo strip** is after the transcript and immediately above the queue or dock, stays visible while transcript rows scroll, and is absent when the node has no todos.
    Its collapsed header shows the current item, completed count, and segmented meter.
    Its expanded state shows phases and items and exposes Raw.
    Terminal presentation marks remaining items done when the node completes and resets the current item to todo after the 30-minute interruption failure, without changing stored provider rows.

#### FR4 — CAP-4

- **CAP-4** — See what a subagent dispatch asked for
  - **intent:** A reader can see the brief a task dispatch carried and which subtasks it spawned.
  - **success:** Both provider shapes normalize to `TaskSubtask[]`.
    The card renders batch context as markdown when the provider sends any, then one collapsible card per subtask naming the subtask and its agent.

#### FR5 — CAP-5

- **CAP-5** — See what a file edit changed
  - **intent:** A reader can see the actual change a file edit made, inline, without leaving the transcript.
  - **success:** When the payload carries both before and after content, a line diff renders through `react-diff-view`.
    Claude always qualifies — `FileEditInput` declares `old_string` and `new_string` as required.
    Any persisted file tool row without both before/after string sides falls back to path plus preview, and a diff is never fabricated from one side.
    Scope is presentation over persisted rows only: current Codex `file_change` events are emitted as `system` chunks (`codex/provider.ts:709`) that the executor debug-logs as `dag.system_message_unhandled` (`dag-executor.ts:2691-2748`) instead of appending them to the node transcript, so they never become persisted file rows and are explicitly excluded here.
    Making successful Codex file changes visible in the transcript is separately tracked work.
  - **data contract:** `packages/web/src/lib/diff-hunks.ts` is the only `structuredPatch` caller, on `diff@9.0.0` — deterministic bounds (65,536-byte and 2,000-line side caps, `context: 4`, `maxEditLength: 2000`, no wall-clock timeout) and dual-bounded memoization (256 entries capped at 1,048,576 source code units).
    `tool-presentation.ts` qualifies a pair only after the family resolves to `file`, only as own-property strings (`''` valid; inherited keys, throwing accessors, one-sided, and wrong types never qualify), and caches the result per input record in a `WeakMap` so summary and body compute once.
    Collapsed rows carry `+n`/`−m` badges; expanded rows carry `N hunk(s)` or `no changes` plus `replace_all` when the input sends it as an own boolean.
    Line content is display-sanitised — ANSI/C0/C1 stripped, `Cf`/`U+2028`/`U+2029` escaped as `\u{HEX}`, 1,024-code-unit ceiling — while diffing runs on raw strings and Raw keeps the original payload.
    Refused, identical, and non-qualifying pairs degrade honestly; a failed edit still shows its attempted diff alongside the normalized failure output.

#### FR6 — CAP-6

- **CAP-6** — Tell attempts and loop iterations apart
  - **intent:** A reader can tell which attempt or loop iteration produced a given tool call.
  - **success:** A node whose rows span more than one `occurrence_id` renders a header per group; a single-occurrence node renders none.
    Grouping keys on `occurrence_id`, never on `attempt_id`.
    Every group header uses primary `Run N` in occurrence order; loop iteration, provider pass, retry, and interruption context follows as a suffix when relevant.
    A **loop-iteration selector** lets the reader navigate directly between occurrence groups; the per-group headers remain its targets, and it is absent on a single-occurrence node.
    On a live loop node, selecting a **finished** iteration through the **`Execution` selection controls** (header select, Logs row, or graph occurrence; `Jump to` stays scroll-only) renders a read-only dock, a `Go to iteration N` control, and a read-only band that mirrors the node-scoped shared pending queue across operators and tabs.
    The composer and steering actions are absent in this view.
    Every steering mutation carries the selected `retry_epoch`, and the server fails closed when that epoch is stale, so a delayed or replayed mutation from a finished iteration cannot affect the live iteration.
    The authenticated node-scoped `GET …/queue` read is allowed so the band stays current.

#### FR7 — CAP-7

- **CAP-7** — Keep the raw payload reachable
  - **intent:** A developer debugging a provider can still read the exact bytes the provider sent.
  - **success:** Every card exposes a Raw toggle revealing the original JSON, closed by default.
    This is the **only** place serialized JSON appears.
    Today's `canLoadFullOutput` / `onLoadFullOutput` flow keeps working.

### Write — steer the live agent

#### FR8 — CAP-8

- **CAP-8** — Compose while the agent works
  - **intent:** An operator watching a running node can write a message without disturbing it.
  - **success:** The composer is mounted and enabled while the node runs, and the send control reads `Queue`.
    Sending holds the message; the node is untouched, no tool call is interrupted and nothing is lost.
    The message is delivered as the next turn when the current turn ends naturally.
    The words `this tab only` apply only to unsent composer draft content, including when that draft shares one combined surface with queued items.
    A queued message is node-scoped, shared across operators and tabs, and server-process-local; it survives a tab close and dies only on a server restart.

#### FR9 — CAP-9

- **CAP-9** — Interrupt the agent's thinking; the node keeps running
  - **intent:** An operator can stop the agent's _current generation_ to redirect it, without stopping the node or abandoning the run.
  - **success:** An interrupt control in the node's composer dock ends the agent's **current turn** — via the provider's own primitive (claude `interrupt()`) or a stream-abort — and the **provider session stays alive**.
    The **node stays `running`** throughout (its agent moves to a projected `idle-after-interrupt` sub-state) — never paused, never `pending`, never `node_failed`.
    The transcript shows the in-flight tool call as _interrupted_ rather than _failed_ (this is CAP-1's status glyph `⚠`).
    Interrupting the agent is **not** stopping the node: that is the existing **Cancel** feature, separate and untouched.
    Nothing suggests the interrupt undid work already written.

#### FR10 — CAP-10

- **CAP-10** — Redirect and continue on the same live session
  - **intent:** After interrupting, the operator sends what to do instead and the agent carries on from there — on the same session, in the same node.
  - **success:** Once the agent is `idle-after-interrupt`, the send control reads `Send now`.
    Sending dispatches the newly typed message (the queued messages are already on the registry); the executor then flushes the registry — the already-queued messages followed by this one, **in receipt/written order** — as the **next turn on the same provider session** (reusing the `attemptResumeId` re-ask seam).
    The node **continues** — it never "resumes" from a pause, because it never paused.
    Ordering is enforced by us, not assumed of the provider.
    Both controls follow the agent's projected sub-state, not a remembered mode.

#### FR11 — CAP-11

- **CAP-11** — The exchange is part of the record
  - **intent:** Anyone reading the transcript afterwards can see what the operator said, and when, relative to what the agent did.
  - **success:** The operator's messages and the interrupted tool call appear as ordinary transcript rows in the order they happened — between the call they interrupted and the one they caused.
    An operator row is visibly the operator's, never mistakable for the agent's own text.
    _This is where the write half writes into the read half — see Cross-half dependency._

#### FR12 — CAP-12

- **CAP-12** — Mid-turn delivery, as fast as each provider's transport allows
  - **intent:** The operator's message reaches a running agent without interrupting it, and sooner on a provider whose transport can take it mid-turn.
  - **success:** Sending to a running agent is an **ordinary prompt**, not a separate steer primitive.
    `Queue` remains available on every provider and delivers at the next natural boundary without an interrupt.
    Per-item `Send now` appears only while the agent generates on an Archon transport proven to accept live input.
    Selecting it sends exactly that queued item into the active turn without Stop, a natural-end wait, an interrupted tool call, or another turn.
    On provider transport acceptance, only that item leaves the shared queue and appears immediately as an operator transcript row with `sent` and no visible sender name.
    A queue-only mode omits the per-item action; a direct unsupported request returns a typed refusal without changing the queue.
    Claude streaming input, a new Grok live-input path, and OMP RPC mode must pass current release proof gates G2, G3, and G4.
    G3 requires Grok per-item Send now during the active turn in this release with causal agent receipt; Archon's current Grok `--single` path is queue-only.

#### FR13 — CAP-13

- **CAP-13** — The interface claims only what it knows
  - **intent:** An operator can tell whether a message merely left the browser or actually reached the agent.
  - **success:** A queued receipt remains `queued`; only provider acceptance into the active turn changes the selected item to `sent` and creates its single transcript row.
    The row changes to `delivered` only when a matching native lifecycle event or a stream causally tied to that item proves agent consumption.
    An RPC acknowledgement, unrelated ongoing stream, text match, or timestamp does not prove consumption.
    The caller-stamped `message_id` stays the row and idempotency key; an echoed id is sufficient but is not the only valid proof.
    G1 is a current release proof and implementation gate, and an unconfirmed accepted row stays `sent`.

Total numbered functional capabilities: 13.

### Non-Functional Requirements and Binding Constraints

The SPEC does not assign NFR numbers.
The NFR numbers below are assessment identifiers for each full top-level constraint block; they do not replace the source headings.

#### Shared — both halves land in `packages/web`

**NFR1.**

- `@archon/web` must **not** import from `@archon/workflows`; wire types come from `api.generated.d.ts` through `lib/api.ts`.

**NFR2.**

- **Console must not import from `@/components/`.**
  Shared logic lands in `packages/web/src/lib/` and the JSX is written twice, thin — duplicating a little JSX for a surface scheduled for deletion beats refactoring code on its way out.

**NFR3.**

- Both node rooms (Legacy + Console) ship **together**, on the owner's explicit and re-confirmed decision, even though Legacy is scheduled for deletion and Console is the default route.

**NFR4.**

- Strict TypeScript, no unjustified `any`, ESLint at zero warnings.
  `bun run validate` is the pre-PR gate.

**NFR5.**

- Code comments and test names carry **no** plan/section/finding references — comments explain the invariant.

#### Read half

**NFR6.**

- Ships **no schema change, no migration, and no backend change.**
  Anything that would need new persisted data is out of scope by definition — that is the line that keeps it retroactive.

**NFR7.**

- **Serialized JSON is never a default presentation, and unclassifiable assistant text fails closed to its original bytes.**
  Tool payloads keep CAP-7's explicit Raw affordance — that toggle remains the only place tool input/output JSON appears.
  Assistant text is classified losslessly or left alone: when the matched definition node's `output_format` is an object schema declaring exactly one `type: 'string'` property and the stored text is the canonical serialization of that one-key envelope, the transcript renders the envelope's string value through the existing Markdown path; every other shape — absent or ineligible schema, malformed or non-canonical text — renders the original bytes unchanged.
  The shared `buildAgentHistory()` projector applies this rule with the schema forwarded by the three production surfaces — `LegacyNodeRoom` via `NodeTranscriptPane`, `ConsoleNodeRoom`, and `ConsoleInspectPane` via `ConsoleExecutionHistory`.
  Stored rows, API output, and the engine's structured result are never mutated.

**NFR8.**

- Status must be decodable **without colour** — a glyph character carries it, colour only reinforces.

**NFR9.**

- A chip shows the tool name only when that name is **a single token of at most 24 characters**; otherwise it shows the **family name**.
  The 24-character cap is the guard behind the rule, never an instruction to truncate with an ellipsis.

**NFR10.**

- Tool identification **duck-types over alias sets**; no name-keyed mapping table.
  Match **exact tokens, never substrings** (`search_replace` is an edit, not a search).

**NFR11.**

- **Provider shape differences are normalized at the edge, never branched on in a renderer.**
  A small normalizer in `lib/` converts each provider shape to one shared shape; `ToolPresentation` stays render-neutral and neither renderer learns a provider name.

**NFR12.**

- Path headlines elide in the **middle**; commands and patterns elide at the **end**.

#### Write half

**NFR13.**

- **Current release scope.**
  G1 truthful delivery and G2 Claude, G3 Grok, and G4 OMP active-turn delivery are current release proof and implementation gates.
  The candidate Archon paths are not proven by an SDK feature or another product's adapter.
  Grok must prove a real active-turn path in this release; its current `--single` adapter remains queue-only and an advertised hook is insufficient.
  The universal interrupt plus `Queue` path remains available on every provider.

**NFR14.**

- **Steering acts on the live agent, never on the node lifecycle.**
  _Send_ and _interrupt_ both operate on the running node's **live provider session**.
  The node stays `running` throughout — no pause, no `pending`, no resume, no `node_failed`.
  There is **no durable steering state**: no marker, no phase, no CAS, no attempt-key.
  Stopping the whole node is the existing **Cancel**/abort path, out of scope and untouched.

**NFR15.**

- **Interrupt is the provider's own primitive (or a stream-abort) on a per-turn signal, never the node-level one.**
  The executor's `nodeAbortController` (`dag-executor.ts:2209`) is **one-shot and Cancel's** (its `:3124` check fails the node; the re-ask loop stops on it, `:3032`).
  Steering interrupts through a **fresh per-turn signal** combined with the node-level one (`AbortSignal.any`), so each new turn gets a fresh signal — which is what lets the node run multiple turns and never trips `:3124`.
  `operatorInterrupt` is a **per-turn flag resolved by placement** in the existing flow (`stream → validation → canReask :3032 → :3124 Cancel check → completion`): `canReask` must also stop on it; validation is skipped on an interrupted turn; its branch sits immediately after the `:3124` Cancel check so Cancel dominates by position; it is reset at turn N+1.
  **End cause** is a five-case rule the executor resolves, not result-presence alone: a `result` with no abort marker is a natural end; a `result` carrying an abort marker (DeepSeek `stopReason:'aborted'`) or a thrown abort (OMP `Query aborted`) with `operatorInterrupt` set is an **interrupted end** into idle-await, never `node_failed`; a throw without the flag is a real failure; Cancel dominates by position.
  The executor classifies the abort-marked `result` and the abort throw — the provider adapter never suppresses them.
  This applies on both `executeNodeInternal` and `executeLoopNode` (AI loop nodes are steerable in v1).
  See `engine-integration.md`.

**NFR16.**

- **A steered node runs multiple provider turns on one live session — the one new engine behaviour.**
  It reuses the structured-output re-ask seam that re-invokes `sendQuery` with `attemptResumeId` on the same session (`dag-executor.ts:2290`).
  **Turn-end has two causes:** a **natural** end **auto-drains** (next turn on a queued message, else completes the node when the queue is empty); an **interrupted** end produces a **partial** result that must not be validated, completed, or advanced, and **always enters idle-await whatever the queue holds** — draining only on the operator's `Send now`.

**NFR17.**

- **In-process only; no durable steering state.**
  The **in-process registry** — keyed `(runId, nodeId)`, holding the node's live session handle plus an in-memory inbound queue, valid only while the node runs in this process — is the sole mechanism.
  Web dispatch runs the executor in the API server's process; a detached CLI run has no reachable handle, so steering is **unavailable** for it in v1 (Cancel and normal resume still work; the UI states this).
  A server restart drops the live session, timer, and any in-flight steer, leaving a durable non-terminal run.
  Archon does not autonomously fail or resume that ambiguous run from staleness; the operator must use explicit recovery.

**NFR18.**

- **Two queues, split on typing vs queued.**
  The pre-`Queue` **draft** is text still being composed and is per-tab browser state with no table or migration.
  The `this tab only` label qualifies only this unsent draft, even when draft and queued content share one combined surface.
  Pressing `Queue` dispatches the message to the send route with `intent: 'queue'`, and from that moment it rides the node-scoped shared in-memory registry queue on the live handle.
  The executor drains it at the natural turn boundary, `Send now` after an interrupt flushes the queue, and per-item `Send now` during generation attempts soft-inject.
  Delete of a queued message calls an idempotent withdraw route.
  A queued message survives a tab close and dies on a server restart.
  The crossover from client to server is `Queue`-press, not the drain moment.

**NFR19.**

- **An operator message is an ordinary `text` transcript row carrying three additive `metadata` fields:** `origin = 'operator'`, `operator_user_id` (the sender, for CAP-11 attribution on a multi-user install), and `message_id` (the caller-stamped id, so the client can reconcile which `sent` messages became rows).
  No new table, no widened `kind` enum — the `.strict()` metadata schema takes additive fields plus a regenerated `api.generated`, not a migration.
  The executor is the **sole** writer; the row is a receipt for the record, not the delivery vehicle.
  Only the row created by accepted per-item `Send now` during generation hides its visible sender name; stored attribution and other operator-row display paths remain.

**NFR20.**

- **Send and Interrupt are new routes; the queue absorbs races and epoch checks fail closed.**
  `POST /api/workflows/runs/:runId/nodes/:nodeId/send` and `…/interrupt` resolve identity through `resolveAuthContext` under the steering-specific actor grant.
  Any authenticated identity may steer and is attributed by `operator_user_id`; unauthenticated calls return 401; identity-less runs are allowed.
  Every mutation carries `retry_epoch`.
  A stale epoch returns a typed refusal and changes no node, queue, or transcript state.
  A Send arriving while an interrupt is in flight waits in the queue for the operator's `Send now`.
  A node no longer running returns 409, a detached run returns `not steerable here`, and a direct per-item request on a queue-only mode returns a typed capability refusal without removing the item.
  Queue-only UI omits that per-item action.
  On any terminal event, the client reconciles queued receipts against operator-row ids only after the executor's terminal write; an item proven unaccepted may return as `Never sent`.
  An accepted id with a failed transcript write is a recording failure, never a sendable `Never sent` draft.
  With two docks on one node, global order is the registry receipt order and each row keeps its `operator_user_id`.

**NFR21.**

- **The dock does not appear on a finished node.**
  Settled by the owner.
  The field and both controls are absent — a control that cannot act must not be drawn.
  Re-running a finished node is `workflow retry-node`, its own capability with its own confirmation, not this dock.
  An undelivered draft box stays rendered **read-only**, stating the node finished and the messages never left.

**NFR22.**

- **Interrupt is not undo.**
  Session state is saved up to the last completed tool call, but files already written stay written — nothing is rolled back.
  The control must not imply otherwise.

**NFR23.**

- **A steer delivery must not emit a turn-start event** — a stray one opens a phantom turn boundary and corrupts the record the transcript is built from.

**NFR24.**

- **Delivery requires causal consumption proof, never matching text or timestamps.**
  A matching native lifecycle event or stream causally tied to the selected message can prove consumption.
  An RPC acknowledgement proves only transport acceptance, and unrelated ongoing stream output proves neither selected-message consumption nor `delivered`.

**NFR25.**

- **Selected-item live input is separate from dock Send now.**
  A server-owned queued `message_id` and selected `retry_epoch` identify the item; the caller cannot replace its stored text or `operator_user_id`.
  The registry claims that one item against the active turn token and asks a provider-owned live-input port for acceptance.
  Acceptance removes only that item and writes one idempotent `sent` operator row immediately; consumption changes only that row to `delivered`.
  A rejected attempt releases the claim without moving the item; a turn-end or Stop race never silently falls back to a new turn.
  An uncertain timeout must not trigger blind reinjection, and a transcript-write failure after acceptance must not return the item to a sendable queue.
  Repeated receipts, concurrent operators, and reconnects preserve one send and one row per stamped id.
  Dock `Send now` after Stop remains the separate next-turn flush of queued items plus the new draft.

**NFR26.**

- **Steering is universal over a queue-and-flush floor; mid-turn is the acceleration.**
  Every provider delivers an operator message — at worst as the next prompt at turn-end.
  A provider earns _soft-inject_ only from a transport exercised against it, never one merely advertised.
  Providers differ on two honest axes: **boundary granularity** (mid-turn vs turn-end) and **delivery confirmation** (CAP-13).

**NFR27.**

- **Approved queue layouts are context-specific.**
  The full Legacy and Console rooms use the full-bleed queue band.
  The compact dock and state-review layout uses the inset queue well.
  These are two layouts for two contexts, not alternatives for one context.

**NFR28.**

- **Approved room controls are product behavior.**
  The in-product `Execution` selector, Cancel, Re-run, Log or Logs, Graph, counted Artifacts, and node-room close control act as shown in the approved mockups.
  Outer numbered review-state, node-kind, and provider-transport controls are fixture scaffolding.

**NFR29.**

- **The 30-minute idle-await fail is an inactivity timer (owner-ratified, SC 2.2.1).**
  While the agent is `idle-after-interrupt` and nothing is sent, a **fresh 30-minute timer** (an explicit fail branch — not the existing idle-timeout, which _completes_ the node) fails the node (`interrupted by operator, no redirect received`).
  Idle-await runs its own timer-driven status poll so `/workflow cancel` still reaches it, and **resolves exactly once** (`Send now`, cancel-poll, or timer).
  The timer is an **inactivity** timer: a debounced, authorized composing keepalive **re-arms** it without resolving idle-await, so the node fails only after 30 minutes of genuine operator inactivity — the limit is disclosed in the dock and adjustable by activity; the 30-minute value is unchanged.
  Resuming a 30-minute-failed node re-runs it with a **fresh session** — the interrupted context is gone.

Total binding constraint blocks: 29.

### Additional Requirements

The `Cross-half dependency` section requires the shared reader to recognize operator rows and interrupted tool outcomes before the steering writer emits them.
The `Non-goals` section excludes node Cancel changes, individual tool-call cancellation, durable steering across server restarts or detached runs, draft persistence, other provider steering, and unrelated transcript surfaces.
The approved mockup manifest makes visible product behavior current scope, while explicit transport proofs are release gates.
The local companions are `tool-presentation-contract.md`, `todo-fold-contract.md`, `test-plan.md`, `engine-integration.md`, `provider-steering-matrix.md`, `control-states.md`, `steering-api-contract.md`, and `steering-test-plan.md`.

### PRD Completeness Assessment

The canonical SPEC states all thirteen numbered capabilities and twenty-nine binding constraint blocks.
The current live-turn, queue-only, sender display, delivery-proof, occurrence-label, TODO-position, and fixture boundaries are explicit.
Completeness against the frozen mockup inventory is assessed in the field-level matrix below.

## Epic Coverage Validation

The target Epic's `FR Coverage Map` lists all thirteen canonical capabilities.
Completed Stories in Epics 1–3 are historical delivery evidence.
Epic 4 owns new acceptance work where historical criteria conflict with the current contract.

| FR            | Canonical requirement                                               | Epic and Story coverage                                                   | Coverage |
| ------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- |
| FR1 / CAP-1   | Scannable tool rows and truthful status glyphs                      | Epic 1 Story 1.1; Epic 3 Story 3.1                                        | Covered  |
| FR2 / CAP-2   | Family-shaped bodies and bounded generic fallback                   | Epic 1 Story 1.3                                                          | Covered  |
| FR3 / CAP-3   | Folded TODO state and pinned strip after transcript                 | Epic 1 Story 1.5; Epic 4 Story 4.1                                        | Covered  |
| FR4 / CAP-4   | Subagent batch context and normalized subtasks                      | Epic 1 Story 1.6                                                          | Covered  |
| FR5 / CAP-5   | Inline diff for qualified before/after rows                         | Epic 1 Story 1.4                                                          | Covered  |
| FR6 / CAP-6   | Occurrence grouping and primary `Run N` headings                    | Epic 1 Story 1.7; Epic 2 Stories 2.9–2.10; Epic 4 Story 4.2               | Covered  |
| FR7 / CAP-7   | Explicit Raw toggle and unchanged full-output flow                  | Epic 1 Story 1.2                                                          | Covered  |
| FR8 / CAP-8   | Composer Queue, shared queue, and local unsent draft                | Epic 2 Stories 2.1–2.2 and 2.9–2.10; Epic 4 Story 4.3                     | Covered  |
| FR9 / CAP-9   | Interrupt the current turn without stopping the node                | Epic 2 Stories 2.3–2.7                                                    | Covered  |
| FR10 / CAP-10 | Dock Send now after Stop flushes as next same-session turn          | Epic 2 Stories 2.3–2.7; Epic 4 Story 4.4 distinguishes the action         | Covered  |
| FR11 / CAP-11 | Ordered operator rows with preserved stored attribution             | Epic 2 Stories 2.8 and 2.13; Epic 4 Story 4.3 limits sender-name omission | Covered  |
| FR12 / CAP-12 | Universal Queue floor and proven selected-item active-turn delivery | Epic 2 Stories 2.1 and 2.3–2.7; Epic 4 Stories 4.3–4.4                    | Covered  |
| FR13 / CAP-13 | `queued`, `sent`, and causally proven `delivered`                   | Epic 2 Story 2.8; Epic 4 Story 4.4                                        | Covered  |

Missing FR coverage: none.
The Epic does not add a numbered FR absent from the canonical SPEC.
FR coverage is 13 of 13, or 100%.
This count shows traceability only; it does not by itself prove full behavior equivalence or implementation completion.

## UX Alignment Assessment

The target UX files exist: `ux-Archon-agent-node-room-2026-09-09/DESIGN.md` and `EXPERIENCE.md`.
Both are on the selected Agent Node Room lineage and describe the Legacy and Console rooms.
The following clauses align with the canonical SPEC and named Architecture spines.

| Manifest features | UX behavior citation                                                                                                                                                  | Architecture support                           | Assessment |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ---------- |
| M001, M002        | `DESIGN.md` Components → Checklist and `EXPERIENCE.md` Information Architecture → Todo strip place it after the transcript and before queue or dock.                  | Read spine AD-12(6), AD-17.                    | Aligned    |
| M003, M004        | `DESIGN.md` Components → Queue item and `EXPERIENCE.md` Component Patterns → Send control separate per-item active-turn Send now from dock Send now after Stop.       | Steering spine AD-3, AD-6–8, AD-11.            | Aligned    |
| M005, M006        | `DESIGN.md` Components → Queue surface and `EXPERIENCE.md` Context matrix define a full-room band, compact inset well, shared accepted queue, and local unsent draft. | Read spine AD-17; steering spine AD-11, AD-13. | Aligned    |
| M007              | `DESIGN.md` Components → Context header and `EXPERIENCE.md` Occurrence grouping use primary `Run N` for each multi-occurrence separator.                              | Read spine AD-7; steering spine AD-13.         | Aligned    |
| M008              | `DESIGN.md` Components → Operator text and `EXPERIENCE.md` Component Patterns → Operator text omit the visible sender name only on the accepted per-item row.         | Read spine AD-17; steering spine AD-6, AD-12.  | Aligned    |
| M009              | `DESIGN.md` Components → Message status and `EXPERIENCE.md` Component Patterns → Message status require causal consumption proof before `delivered`.                  | Steering spine AD-8, AD-11.                    | Aligned    |

The UX documents also keep the in-product `Execution` selector and other actual room controls in scope while excluding the outer review fixtures.
The old UX source references and pre-merge CAP numbers are provenance, not an incompatible current rule; the canonical SPEC states the CAP offset.
The Design text that says only Claude currently emits an interrupted outcome describes the earlier runtime state; it does not prohibit the current cross-provider interrupted-row requirement.
No mutually exclusive normative UX and Architecture claim was found for the nine changed behaviors.

## Epic Quality Review

| Epic                              | User outcome                                                        | Dependency and story quality assessment                                                                                                                                   |
| --------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — Readable Agent Transcript     | Operators scan and inspect historical or live node activity.        | Completed brownfield baseline; Stories 1.1–1.7 have observable Given/When/Then criteria and no dependency on a later Epic for their original scope.                       |
| 2 — Live Agent Steering           | Operators queue, interrupt, redirect, and audit a running agent.    | Completed brownfield baseline; Stories 2.1–2.13 use the existing runtime and the Epic 1 reader. No later Epic is required to understand their original acceptance.        |
| 3 — Approved-Behavior Remediation | Operators receive an earlier correction set.                        | Completed historical work. Its 64-row inventory and old criteria are explicitly superseded where they conflict with Epic 4.                                               |
| 4 — Current-Contract Correction   | Operators receive the current 70-item room behavior on both shells. | Current corrective work. Stories 4.1–4.5 cover observable behavior; Story 4.6 is the final owner conformance gate after those stories. No Story depends on a later Story. |

The project is brownfield, so starter-template, initial database, and greenfield setup checks do not apply.
Epic 4 uses existing transcript, queue, route, and provider seams and assigns proof work to the behavior that needs it.
No new table is specified solely to prepare a later story.
Stories 4.1–4.6 use testable Given/When/Then criteria and name the expected state, target, and error behavior where a mutation occurs.
The selected-item route, queue-only refusal, uncertain timeout, transcript-write failure, stale epoch, and causal delivery gate all have explicit acceptance rules in Stories 4.3–4.4 and the companion contracts.
The old Story 1.5 objective says the strip is above the transcript, old Story 1.7 permits other primary labels, old Story 2.8 shows a name, and old Story 3.6 treats fixture controls as product controls.
The Epic overview and new Stories explicitly identify these as completed historical criteria that Epic 4 supersedes.
They do not create a forward dependency or a new current requirement.

Critical structural violations: none found.
Major story-quality violations: none found.
Minor concern: Story 4.6 is a cross-story acceptance gate rather than an independently shippable user interaction; it is a reviewable product-owner outcome and is required by the approved 70-item conformance contract.

## Mockup Feature Behavior Matrix

This matrix compares each atomic changed-feature row with the current canonical requirements, Architecture, and new Story acceptance.
The mockup values below are copied from the frozen manifest; exact field-level provenance is in the Mockup Behavior Evidence Ledger above.
The citations name clauses that together cover the complete behavior, not only the visible label.

| ID and mockup location                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Precondition                                                                                            | Action                                                                                                                              | Target and cardinality                                                                                                 | Timing                                                                                                                         | Effect and result                                                                                                                                                                                                                                                                                                                         | Remaining or next state                                                                                                                                                                        | SPEC or PRD clause                                                           | Architecture decision                             | Epic or Story acceptance                                          | Status  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------- | ------- |
| M001<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · running prompt node · I007<br>Console TODO strip placement and disclosure<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=todo-strip`<br>One collapsible TODO strip on the Console node room directly below the transcript and above the steering dock                                                                 | The selected agent node has todo calls                                                                  | The selected node room renders or the operator toggles the strip                                                                    | The selected node todo strip<br>One strip for the selected node                                                        | Pinned and visible while the transcript scrolls                                                                                | Active work: None; expanding the strip changes only its display<br>Collection: None<br>Result: The current todo item and progress remain visible; expansion shows the checklist                                                                                                                                                           | The strip is collapsed or expanded while the node keeps its agent sub-state                                                                                                                    | `SPEC.md` CAP-3 success; `todo-fold-contract.md` Rendering rules             | Readable spine AD-12(6), AD-17                    | Epic 4 Story 4.1 AC1–2                                            | MATCHED |
| M002<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · running prompt node · I070<br>Legacy TODO strip placement and disclosure<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=todo-strip`<br>One collapsible TODO strip on the Legacy node room directly below the transcript and above the steering dock                                                                     | The selected agent node has todo calls                                                                  | The selected node room renders or the operator toggles the strip                                                                    | The selected node todo strip<br>One strip for the selected node                                                        | Pinned and visible while the transcript scrolls                                                                                | Active work: None; expanding the strip changes only its display<br>Collection: None<br>Result: The current todo item and progress remain visible; expansion shows the checklist                                                                                                                                                           | The strip is collapsed or expanded while the node keeps its agent sub-state                                                                                                                    | `SPEC.md` CAP-3 success; `todo-fold-contract.md` Rendering rules             | Readable spine AD-12(6), AD-17                    | Epic 4 Story 4.1 AC1–2                                            | MATCHED |
| M003<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating on soft-inject transport · I009<br>Per-item Send now during generation<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-send-now`<br>A Send now button on each queued item                                                                                                                      | An agent is generating on a soft-inject transport with a queued item in the live iteration              | The operator selects that queued item’s Send now button                                                                             | The queued message beside the selected button<br>Exactly one selected queued item.                                     | Immediately upon provider acceptance during the running turn, without waiting for turn end.                                    | Active work: The active turn and tool call continue without interruption or a new turn.<br>Collection: Exactly the selected accepted item leaves the shared queue; other queued items remain.<br>Result: The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name. | That item is absent from the queue; its transcript row reads sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then delivered. | `SPEC.md` CAP-12–13 success; Constraints → Selected-item live input          | Steering spine AD-3, AD-6–8, AD-11                | Epic 4 Story 4.3 AC1, AC3–5; Story 4.4 AC1–4                      | MATCHED |
| M004<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent generating on soft-inject transport · I029<br>Per-item Send now during generation<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-send-now`<br>A Send now button on each queued item                                                                                                                        | An agent is generating on a soft-inject transport with a queued item in the live iteration              | The operator selects that queued item’s Send now button                                                                             | The queued message beside the selected button<br>Exactly one selected queued item.                                     | Immediately upon provider acceptance during the running turn, without waiting for turn end.                                    | Active work: The active turn and tool call continue without interruption or a new turn.<br>Collection: Exactly the selected accepted item leaves the shared queue; other queued items remain.<br>Result: The selected ordinary prompt enters the active turn immediately and appears as an operator transcript row without a sender name. | That item is absent from the queue; its transcript row reads sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then delivered. | `SPEC.md` CAP-12–13 success; Constraints → Selected-item live input          | Steering spine AD-3, AD-6–8, AD-11                | Epic 4 Story 4.3 AC1, AC3–5; Story 4.4 AC1–4                      | MATCHED |
| M005<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html` · agent generating with accepted queued guidance · I008<br>Shared queued-message band and disclosure<br>`claude-design/design_handoff_node_room_transcript_steering/Console Node Room.dc.html#state=generating;element=queued-list`<br>A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label. | At least one accepted message is queued for the selected live node.                                     | The operator selects the queue disclosure header.                                                                                   | The selected node's shared accepted-message queue band.<br>One band for the selected node.                             | The band expands or collapses immediately when selected.                                                                       | Active work: None; disclosure changes only the queue display.<br>Collection: None; opening or closing the band does not add, send, or delete a queued item.<br>Result: The operator can show or hide the accepted rows while their shared count and order remain available.                                                               | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                                                                     | `SPEC.md` CAP-8 success; Constraints → Two queues and approved queue layouts | Steering spine AD-11, AD-13; readable spine AD-17 | Epic 4 Story 4.3 AC2–3; completed Story 2.9 shared-queue baseline | MATCHED |
| M006<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html` · agent generating with accepted queued guidance · I028<br>Shared queued-message band and disclosure<br>`claude-design/design_handoff_node_room_transcript_steering/Legacy Node Room.dc.html#state=generating;element=queued-list`<br>A collapsible QUEUED count band below the transcript with ordered items, ordinals, and a next-out mark; accepted items have no this-tab-only label.   | At least one accepted message is queued for the selected live node.                                     | The operator selects the queue disclosure header.                                                                                   | The selected node's shared accepted-message queue band.<br>One band for the selected node.                             | The band expands or collapses immediately when selected.                                                                       | Active work: None; disclosure changes only the queue display.<br>Collection: None; opening or closing the band does not add, send, or delete a queued item.<br>Result: The operator can show or hide the accepted rows while their shared count and order remain available.                                                               | The shared queue band is expanded or collapsed; accepted messages remain shared across tabs and operators.                                                                                     | `SPEC.md` CAP-8 success; Constraints → Two queues and approved queue layouts | Steering spine AD-11, AD-13; readable spine AD-17 | Epic 4 Story 4.3 AC2–3; completed Story 2.9 shared-queue baseline | MATCHED |
| M007<br>`claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html` · multi-occurrence node · I052<br>Run N occurrence separator<br>`claude-design/design_handoff_node_room_transcript_steering/Transcript States.dc.html#section=E;element=occurrence-options`<br>Run 1, Run 2, and later Run N separators, with a contextual suffix such as retry or iteration when applicable.                                                                              | The selected node has more than one occurrence.                                                         | The transcript renders occurrence groups.                                                                                           | The selected node's transcript occurrence groups.<br>One separator per occurrence when there are multiple occurrences. | When the multi-occurrence transcript is rendered.                                                                              | Active work: None; the separator labels existing transcript history.<br>Collection: None; displaying separators does not alter transcript rows.<br>Result: Each occurrence has a Run N header with the relevant suffix; Pass N and reason-only are not primary headers.                                                                   | The multi-occurrence transcript remains grouped under Run N headers; a single occurrence has no separator.                                                                                     | `SPEC.md` CAP-6 success; `tool-presentation-contract.md` Occurrence grouping | Readable spine AD-7, AD-17                        | Epic 4 Story 4.2 AC1–3                                            | MATCHED |
| M008<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · operator exchange · I063<br>Operator transcript row without sender name<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=operator-record;element=operator-rows`<br>An operator message row with full-strength text and a sent badge, without the sender's name.                                                                    | One selected message is accepted by the provider and recorded in the transcript.                        | That accepted operator message renders in the transcript.                                                                           | The transcript row for that selected stamped operator message.<br>Exactly one transcript row for the selected item.    | Immediately on provider acceptance.                                                                                            | Active work: None from the name display; stored attribution remains intact.<br>Collection: None from the visual name treatment; provider acceptance still removes the selected queue item and creates its operator transcript row.<br>Result: The row shows the operator message and sent badge without the sender's name.                | The row remains sent until a matching lifecycle event or a response stream causally linked to that item proves agent consumption, then reads delivered.                                        | `SPEC.md` CAP-11–12 success; Constraints → Operator message                  | Steering spine AD-6, AD-12; readable spine AD-17  | Epic 4 Story 4.3 AC3                                              | MATCHED |
| M009<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html` · message status after provider confirmation · I068<br>Delivered operator-message status<br>`claude-design/design_handoff_node_room_transcript_steering/Steering Dock States.dc.html#section=status;element=delivered`<br>A delivered badge on the matching operator message row.                                                                                                       | The selected operator transcript row is sent and agent consumption can be established for that message. | A matching provider lifecycle event or a response stream causally linked to the selected message proves that the agent consumed it. | The operator transcript row with that matching stamped message_id.<br>Exactly one matching operator message row.       | After evidence that the agent consumed the selected message, not merely after transport acceptance or unrelated stream output. | Active work: None; confirmation updates the displayed delivery status.<br>Collection: None; the row stays in the transcript and only its status changes.<br>Result: The matching row changes from sent to delivered.                                                                                                                      | The matching operator row reads delivered; unconfirmed rows continue to read sent.                                                                                                             | `SPEC.md` CAP-13 success; `steering-api-contract.md` Response schemas        | Steering spine AD-8, AD-11                        | Epic 4 Story 4.4 AC3–4                                            | MATCHED |

Mockup changed-feature coverage: 9 of 9 MATCHED, 0 PARTIAL, 0 MISSING, 0 CONFLICT, 0 UNCLEAR.
Context classification coverage: 61 of 61 unchanged-context rows copied above with current-implementation and completed-Epic evidence.
Total product inventory coverage: 70 of 70 classified exactly once.

## Scope Decision Audit

The selected sources contain no current deferral of an approved visible behavior.
The user's approved G3 correction makes Grok active-turn selected-item delivery a current release requirement; the present Grok `--single` mode remains queue-only until a distinct proven live-input path is available.
The scope labels below were tested against the frozen 70-item inventory.

| Label and current source                                                                                                                                                                  | Reconciliation with manifest and user decision                                                                                                                                                                                                                                                                                | Result                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| G1, G2, G3, and G4 current release proof gates: `SPEC.md` Current release scope, steering spine AD-3 and Current Grok release proof, and Epic 4 overview and Stories 4.3–4.6.             | M003, M004, and M009 remain current visible behavior. The gates require actual same-turn and causal receipt proof before a provider mode shows the per-item action or a row reads `delivered`. A proof gate is not a feature deferral.                                                                                        | MATCHED                   |
| Historical G3 future/deferred label: completed Epic 3 overview, completion criteria, and Future capability.                                                                               | `epics.md` Overview and Epic 4 explicitly supersede this completed historical label after the user's 2026-09-24 G3 decision. G3 is required in this release even if the advertised hook fails; another Grok path must pass or release stays blocked. Current Grok `--single` remains queue-only context C023, C039, and C065. | MATCHED historical record |
| Detached steering and server-restart survival: `SPEC.md` Non-goals and Open questions, steering spine AD-5 and Deferred, `engine-integration.md` process boundary.                        | C020, C036, and C062 require the visible `not steerable here` disclosure now. A remote live-handle mechanism is outside the accepted in-process boundary and does not remove that current disclosure.                                                                                                                         | MATCHED                   |
| New node Cancel behavior, individual tool cancellation, persisted draft queue, auto-send, and unused providers: `SPEC.md` Non-goals.                                                      | Existing Cancel remains visible in C001 and C024. The exclusions do not remove or relabel any of the 70 current product items.                                                                                                                                                                                                | MATCHED                   |
| Other transcript surfaces, run-level file panel, agent thinking, triggering prompt, and advisor notifications: `SPEC.md` Non-goals and readable spine Deferred.                           | No separate approved node-room item is removed. Inline file diffs remain current context C048.                                                                                                                                                                                                                                | MATCHED                   |
| Resolver memoization, transcript-only error boundary, chat presenter adoption, Legacy deletion, very-long-transcript virtualization, and parent-spine amendment: readable spine Deferred. | These are other-surface or maintenance boundaries. Legacy and Console remain current required rooms.                                                                                                                                                                                                                          | MATCHED                   |
| Separate code-family hue: `DESIGN.md` Brand & Style.                                                                                                                                      | C040 still requires the approved family-chip treatment; the excluded new token is not a visible inventory item.                                                                                                                                                                                                               | MATCHED                   |
| Read-only operator item kind under the steering spine Deferred heading.                                                                                                                   | Steering AD-10 assigns the read projection to the readable core, while CAP-11 and Epic 4 require the operator row now. The heading groups ownership boundaries, not a feature delay.                                                                                                                                          | MATCHED; wording warning  |

No current-scope question remains open in the approved manifest or the selected planning sources.

## Action Identity Audit After Document Mapping

| Shared label or intent                                          | Frozen behavior signatures                                                                                                                                                                                     | Evidence and current planning alignment                                                                                                                                                                                                                                                                               | Result                   |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Per-item Send now in Console and Legacy: M003, M004             | Both require a queued item, generating state, proven live input, exactly one selected server-owned target, acceptance in the active turn, no Stop or next turn, removal of only that item, and one `sent` row. | The manifest group `send-queued-item-now` records `SAME_ACTION` and direct user answers `coordinator-260924-send` and `coordinator-260924-receipt`; its row-level signature fields carry rendered, interaction-code, and user provenance. `SPEC.md` CAP-12–13, steering AD-3/AD-11, and Epic 4 Stories 4.3–4.4 agree. | MATCHED                  |
| Shared queue disclosure in Console and Legacy: M005, M006       | Both immediately toggle display of one node-scoped accepted-message band; neither changes active work or queue contents.                                                                                       | The manifest group `toggle-shared-queue` records `SAME_ACTION`, both mockup `onToggleDraft` handlers, and direct user answer `coordinator-260924-queue`. `SPEC.md` CAP-8, steering AD-13, and Epic 4 Story 4.3 agree.                                                                                                 | MATCHED                  |
| Per-item Send now M003/M004 versus dock Send now C014/C031/C057 | The per-item control acts during generation on exactly one selected item in the active turn. The dock control acts after Stop and flushes all queued items plus the new draft as the next turn.                | M003/M004 field provenance and unchanged dock context C014/C031/C057 prove the distinct preconditions, targets, cardinality, timing, and remaining state. `SPEC.md` CAP-10/CAP-12, steering AD-3/AD-4, and Epic 4 Stories 4.3–4.4 preserve both meanings.                                                             | MATCHED distinct actions |
| Per-item Delete C010/C067 versus per-item Send now M003/M004    | Delete withdraws one queued item without delivery. Send now accepts one selected queued item into an active turn on a proven mode.                                                                             | The manifest classifies Delete as unchanged context and active-turn Send now as changed behavior. `steering-api-contract.md` Routes and Epic 4 Stories 4.3–4.4 keep the intents distinct.                                                                                                                             | MATCHED distinct actions |

The outer numbered state, node-kind, and provider-transport selectors are review fixtures outside the 70 product items.
The in-product `Execution` selector remains context C021 and C026, separate from scroll-only `Jump to`.
Console `Log`, Legacy `Logs`, Graph, Artifacts, Cancel, and close remain product context C001–C004 and C024–C026.

## Cross-Document Reconciliation

| Apparent difference and section claims                                                                                                                                       | Claim type and compatible implementation                                                                                                                                                                       | Result                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| Steering spine AD-4 says, “Each delivered operator message becomes the next turn's input.” AD-3 says per-item Send now sends exactly one item into the active turn.          | AD-4 describes natural and interrupted turn-end queue drain; AD-3 defines selected-item live input. A port can inject the selected item during the active turn while all other queue drains use the next turn. | Wording warning; no mutually exclusive current rule |
| Completed Story 1.5 says the TODO strip is above the transcript; current Story 4.1 places it after the transcript and before queue or dock.                                  | The first is completed historical acceptance; the second is current corrective acceptance for M001/M002.                                                                                                       | Reconciled                                          |
| Completed Stories 1.7 and 3.2 allow `Iteration N`, `Pass N`, or reason-only primary headers; current Story 4.2 requires primary `Run N`.                                     | The older labels are historical. The current suffix rule can carry their context without replacing the primary M007 label.                                                                                     | Reconciled                                          |
| Completed Story 2.8 shows a sender name and stays `sent`; current Stories 4.3–4.4 hide the name for the accepted per-item row and advance it after causal consumption proof. | Stored attribution and other operator-row display paths remain. The new criterion supersedes only this M008/M009 row treatment.                                                                                | Reconciled                                          |
| Completed Story 3.6 treats outer review switches as product UI and Story 3.7 targets 64 behaviors; current Stories 4.5–4.6 exclude fixtures and cover 70 product items.      | The completed criteria are historical and explicitly superseded by the current manifest-bound Epic 4.                                                                                                          | Reconciled                                          |
| Completed Epic 3 calls G3 future while `SPEC.md` Current release scope, steering spine Current Grok release proof, and Epic 4 make G3 current.                               | The explicit 2026-09-24 user decision and Epic overview supersede the completed gate note. Current Grok `--single` is a queue-only runtime mode; a new proven Grok mode must supply the current G3 capability. | Reconciled                                          |

No pair of current normative claims was shown to be mutually exclusive after testing these compatible implementations.

## Summary and Recommendations

### Overall Readiness Status

**READY for implementation planning handoff.**
This is a planning-source verdict, not evidence that the product or any provider path already passes the new behavior.
The manifest preflight passed, all 70 product items have exactly one proven classification, and all nine changed-feature matrix rows are `MATCHED` across the canonical SPEC, Architecture, UX, and Epic 4 criteria.
The 61 unchanged-context rows retain current-implementation or test evidence and completed-Epic evidence and do not need new Epic 4 requirements.
The selected Epic covers 13 of 13 canonical functional capabilities; the historical G3 future label is expressly superseded by the approved current-release requirement.
No blocking missing decision, current-scope deferral, incompatible normative claim, or structural story defect remains in this assessment.

### Findings and implementation gates

Blocking planning findings: **0**.
Changed-feature matrix: **9 MATCHED, 0 PARTIAL, 0 MISSING, 0 CONFLICT, 0 UNCLEAR**.
Context inventory: **61 of 61** classified with implementation and completed-Epic evidence.
Non-blocking wording or structure concerns: **3** — AD-4's broad next-turn sentence, the steering spine Deferred heading over a current operator-row ownership boundary, and Story 4.6's cross-story conformance role.
G1 causal `delivered` status, G2 Claude, G3 Grok, and G4 OMP active-turn paths remain required implementation and release proofs.
An advertised hook, SDK feature, RPC acknowledgement, or unrelated stream cannot satisfy them.
Current Codex, DeepSeek, and Grok `--single` modes stay queue-only and omit per-item Send now.

### Recommended next steps

1. Use the accepted Epic 4 Stories 4.1–4.5 as implementation input and Story 4.6 as the 70-item product conformance gate.
2. Prove G2 Claude, G3 Grok, and G4 OMP on actual Archon adapter modes, including tool-boundary and pure-text cases; keep the per-item action absent until each mode passes.
3. Prove G1 causal agent consumption before any accepted row changes from `sent` to `delivered`; keep unconfirmed rows `sent`.
4. Validate both rooms, queue-only controls, stale-epoch and selected-item races, stored attribution, fixture exclusion, and all 61 unchanged-context items.
5. Continue to sprint planning and implementation sequencing using this readiness report and the validated manifest.

### Assessment record and unresolved questions

Assessed on 2026-09-25 in the existing `anhle128/agent-node-room-readiness-260924` worktree.
The 2026-09-24 NOT READY reports remain historical and were not edited.
This run changed only this new readiness report.
Unresolved planning decisions: **none**.
