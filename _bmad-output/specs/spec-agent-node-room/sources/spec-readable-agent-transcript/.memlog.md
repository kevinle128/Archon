---
topic: Track A — make the agent transcript in both node rooms readable, replacing raw JSON with per-family tool cards; frontend only, applies retroactively to stored runs
updated: 2026-09-12T13:39
---

- (direction) Scope is Track A only: frontend, retroactive. Track B (thinking, prompt, advisor, mid-turn steering) is excluded; it has a prior contract at specs/spec-workflow-run-view-hitl.
- (note) Input is rich and committed (plans/260909-2130-live-interactive-agent-view/{design,findings}.md), so distill directly without elicitation.
- (capability) CAP-1 one collapsed line per tool call: family chip, status glyph, headline, right-aligned badges.
- (capability) CAP-2 expand a call into a body rendered per family; seven families, each with its own body arm.
- (capability) CAP-3 todo renders as folded state across the whole node, not per call.
- (capability) CAP-4 task dispatch shows context plus one card per subtask.
- (capability) CAP-5 file edit shows an inline diff whenever the provider sent before and after content.
- (capability) CAP-6 retries and loop iterations are visually separated, grouped by occurrence_id.
- (capability) CAP-7 raw JSON stays reachable behind an explicit per-card toggle, never the default.
- (decision) glob is its own family with a paths body, separate from content search's matches body: glob output is a file list with no line numbers (findings 3.5). 7 of 46 observed rows are glob.
- (decision) Status is a glyph character plus colour, never colour alone, so it survives for a colour-blind reader.
- (decision) Path headlines elide in the MIDDLE; commands and patterns elide at the end.
- (decision) Chip is the tool name when it is a single token of at most 24 chars, else the family name. Codex sets the name to the whole command, which is why the fallback exists.
- (constraint) No schema change, no migration, no backend change: Track A is a pure function of data already stored.
- (constraint) Shared logic lives in packages/web/src/lib/; JSX is written twice because Console must not import from @/components/ and Legacy is scheduled for deletion.
- (constraint) Zero raw JSON.stringify as a default presentation; it survives only behind the Raw toggle.
- (question) Do the Claude Agent SDK built-in tool schemas match the Claude Code harness schemas in findings 3.4? One real Claude run settles it; the test table asserts file_path and old_string.
- (event) Self-validate pass 1 (coherence): PASS. All 7 capabilities carry intent and success; intents are WHAT with the HOW pushed to companions; all 10 constraints rule something out; 5 non-goals; success signal is demonstrable against a stored run; CAP ids 1-7 first-issued.
- (event) Self-validate pass 2 (preservation): PASS. Every Track A claim in design.md sections 1,2,4.1,5.1-5.8,7,8,9,10 and open item 11.1 landed in SPEC.md or a companion. Wrapper-only drops: the corrections log, the beliefs-overturned table, and version-skew caveats (process metadata). Deliberate scope drops, all Track B: sections 3.6-3.9, 4.2, 4.3, 6, and open items 11.2-11.6.
- (decision) Companions: 3 spec-authored (tool-presentation-contract, todo-fold-contract, test-plan) plus 2 adopted (findings.md for the file:line evidence, project-context.md). design.md is a source, fully absorbed.
- (event) Spec finalized: 7 capabilities, 5 companions, 4 assumptions, 2 open questions.
- (event) Open question 1 RESOLVED from the published SDK 0.3.209 artifact sdk-tools.d.ts, no Claude run needed: FileEditInput{file_path,old_string,new_string,replace_all?} at 544-560, FileReadInput{file_path} 562, FileWriteInput{file_path,content} 580-588, BashInput{command} 482-486. The Agent SDK schemas match the harness schemas exactly.
- (event) Open question 2 RESOLVED, and it contradicts the contract: Claude GlobInput has pattern (required) plus path (the DIRECTORY to search). OMP glob has no pattern and path IS the pattern. The same key means opposite things per provider.
- (decision) glob headline reads pattern when present (Claude), else path (OMP); scope is path only when pattern is present. Without this, Claude glob rows would show the search directory instead of the pattern.
- (decision) Search body arm is selected by GrepInput.output_mode, not by family alone: content -> matches, files_with_matches (the DEFAULT) -> paths, count -> a count line. The matches/paths split was right; keying it on family alone was not.
- (question) CAP-3 assumes OMP's nine-op todo. Claude TodoWriteInput is {todos:[{content,status,activeForm}]} - a whole-list replacement with explicit status, three statuses, no ops and no phases. Fold contract is OMP-only as written.
- (question) CAP-4 assumes OMP's batch task {context,tasks:[...]}. Claude AgentInput is a SINGLE dispatch {description,prompt,subagent_type?,model?}. One-card-per-subtask does not fit Claude.
- (decision) User chose: normalize per-provider shapes at the edge rather than branching in the renderer. ToolPresentation stays render-neutral; each provider gets a small normalizer in lib/. Rejected alternative was narrowing CAP-3/CAP-4 to OMP, which would leave Claude nodes showing bare key:value in the same transcript, since one run mixes three providers.
- (decision) todo normalizes to TodoPhase[]: OMP folds nine ops; Claude's whole-list TodoWrite is a degenerate fold (last call wins) into a single Tasks phase, three statuses widened to five.
- (decision) task normalizes to TaskSubtask[]: OMP's batch yields one entry per tasks[] element; Claude's single Agent dispatch yields exactly one, description as name, subagent_type as agent. Claude has no batch-level context, so the context block is empty for Claude.
- (decision) output_mode 'count' renders the generic body rather than inventing a third search arm; the count also becomes a badge.
- (event) Re-derived SPEC.md and all three spec-authored companions with the four corrections: OQ1 and OQ2 closed from sdk-tools.d.ts, glob headline reads pattern-then-path, search body keyed on output_mode, CAP-3 and CAP-4 normalized at the edge.
- (event) Self-validate pass 1 (coherence) after re-derive: PASS. 7 capabilities still carry intent and success; CAP-3 and CAP-4 success now cover both provider shapes; the new normalization constraint rules out renderer branching. Open Questions section replaced by Resolved during specification - the spec now opens no unanswered question.
- (event) Self-validate pass 2 (preservation) after re-derive: PASS. New load-bearing claims from sdk-tools.d.ts 0.3.209 all landed: file/bash key confirmation, GlobInput pattern-vs-path inversion, GrepInput output_mode default, TodoWriteInput whole-list shape, AgentInput single dispatch. No source claim dropped.
- (decision) User re-confirmed Legacy stays in scope and is not deferred: both node rooms ship together. Deferring Legacy to a later slice was offered and declined. The second JSX pass and the second renderer test suite are accepted cost.
- (event) Assumption 2 REFUTED by the production corpus. Queried the Mac mini Archon SQLite read-only (1.7 GB, /Users/agent/.archon/archon.db): 22867 tool rows across 31 runs and 2369 distinct tool names, against the 46 rows from 1 run the spec was built on. The local sample was not representative.
- (event) Measured coverage of the alias table against all 22867 rows: file-read 5554, GENERIC FALLBACK 4914, codex tier-3 shell 4911, shell 3584, search 2625, file-write 803, glob 228, todo 198, task 46, web 6. 21.5 percent of real rows would have rendered as bare key:value.
- (decision) Alias table extended from the real corpus: read_file (1605) to file-read, run_terminal_command (1182) to shell, search_replace (904) to file-write, search_tool (48) to search, list_dir (14) to glob. These five recover 3753 of the 4914 fallback rows.
- (decision) Tier 2 path keys gain target_file (803 rows, used by read_file). Its absence would have mis-headlined every one of those rows.
- (constraint) Alias matching stays exact-token over a normalized name (case-folded, underscores and hyphens stripped); substring matching is rejected because search_replace contains 'search' but is an edit tool, so substring would misfile 904 rows into the wrong family.
- (question) eval (821 rows) carries code plus language and belongs to no current family. Generic truncates values at 80 chars, so a whole program would render as a stub. Needs a decision: add a code family or accept the stub.
- (decision) Codex tier-3 names are frequently multi-line shell scripts (4911 rows, a main path not an edge case). Headline is the first non-empty line plus an ellipsis when more follow; the full text goes to the terminal body. Feeding the raw name into a one-line row would break the layout.
- (decision) User chose to add a code family for eval (821 rows). Body is {language, source} rendered as a highlighted block via highlight.js and rehype-highlight, both already in the tree, so no new dependency. Headline is the source's first line, language becomes a badge.
- (event) Self-validate after the corpus round: PASS both passes. Nine families; the generic fallback now claims under 2 percent of the 22867 measured rows, down from 21.5 percent. All five measured aliases, target_file, the exact-token rule, the multi-line Codex headline, and the code family have test cases.
- (event) Spec finalized: 7 capabilities, 5 companions, 2 assumptions, 0 open questions. All questions raised during specification were answered from evidence, not deferred.
- (decision) Story Breakdown agreed: five vertical slices, each visible on both surfaces in the same story so the two renderers never drift and no Legacy catch-up story can be dropped.
- (decision) Story 1: presenter resolver plus collapsed rows, the bodies needing no extra module (terminal, matches, paths, code, generic), and the Raw toggle. CAP-1, CAP-7, part of CAP-2. This is the story that removes the defect; the other four are additive.
- (decision) Story 2: inline diff. Move git-hunk-adapter.ts into lib/ for the Console boundary, then render the diff body. CAP-5.
- (decision) Story 3: todo checklist. Both provider normalizers plus the nine-op fold. CAP-3.
- (decision) Story 4: subagent dispatch cards. Task normalizer for both shapes. CAP-4.
- (decision) Story 5: occurrence grouping. Threads execution identity through the shared history layer, so it goes last, once everything else is stable. CAP-6.
- (decision) Checkpoints set by the user: story 1 gets both spec_checkpoint and done_checkpoint because it sets every pattern the rest follows and is the last cheap place to change direction; story 3 gets spec_checkpoint because a wrong fold makes the checklist lie silently rather than fail; stories 2, 4 and 5 get none. invoke_dev_with left empty on all five.
- (event) stories.yaml validity check: PASS all four schema rules. 5 entries, ids are quoted strings, unique and prefix-free, no status field, title and description on every entry. Checkpoints as agreed: 1 both, 3 spec only, rest none.
- (direction) Update triggered by the completed UX run at planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09. Three inputs, each verified against source before being brought here.
- (decision) ADOPT DESIGN.md and EXPERIENCE.md from that run as companions. They are ADOPTED, not spec-authored: bmad-ux owns them, this skill references them by path and never edits them. Both are status final, updated 2026-09-10. This is the load-bearing part — [CA] Architecture and [CE] Epics discover the UX only through the companions list, so without it the next phase builds from a spec that has no visual or behavioural contract.
- (decision) CORRECT the status glyph set in tool-presentation-contract.md from four characters to five. The line read '✓ ✕ ◐ –, mapped from the existing AgentHistoryItem.outcome via deriveOutcome(), reused unchanged', but that type carries FIVE outcomes (agent-history.ts:36) and deriveOutcome returns interrupted at :133 — so the contract named four glyphs for five states and left one unrenderable. The UX run assigned interrupted a warning triangle in --warning, a token BOTH surfaces already declare (index.css:24, console/theme.css:79), so it introduces no token. Only Claude ever emits the outcome, from the PostToolUseFailure hook when is_interrupt is true (claude/provider.ts:952-959); Codex cannot, its union being success/error/unknown (codex/provider.ts:644-650).
- (note) Scope of that correction confirmed before making it: tool-presentation-contract.md:156 is the ONLY place in this spec that enumerates the glyph set. test-plan.md:67 asserts the colour-independence guarantee without listing glyphs, so it stays valid unchanged, and no other companion names the set.
- (note) The chip fallback rule at tool-presentation-contract.md:159 is CORRECT as written — the normalised tool name when it is a single token of at most 24 characters, ELSE the family name. The UX spines had contradicted it, teaching an ellipsis-inside-the-pill truncation instead, and were corrected to match this contract. Recorded so a later pass does not 'fix' the contract to match the mistake.
- (decision) Tighten the SPEC constraint that read 'Every chip label is at most 24 characters.' True but misleading: it states the CSS cap as if it were the rule, and that reading is exactly what produced the contradiction the UX run had to correct in three places. The constraint now states the fallback — over 24 characters, or not a single token, the chip shows the FAMILY name — with the cap named as the guard behind it.
- (event) Self-validate Pass 1 Coherence: PASS. Seven capabilities, IDs unchanged and unique, each with both intent and success. Eleven constraints, five non-goals, a testable success signal. No capability was added or renumbered — this update changed the contract's companions and one companion's glyph line, not its shape.
- (event) Self-validate Pass 2 Preservation: PASS. All three inputs landed. Both UX spines are in companions and both paths resolve, along with the five pre-existing entries. The glyph line reads five characters with the reason and the provider evidence attached. The chip constraint now states the family-name fallback with the character cap named as its guard. Zero occurrences of the old four-glyph string remain anywhere in the spec folder.
- (event) stories.yaml drift check: TWO entries no longer match, narrower than expected. Story 1 lists the bodies that need no extra module as terminal, matches, paths, code and generic — the web arm (url, title, output as markdown) needs no extra module either and is now assigned to no story. Story 5 renders the occurrence header only when a node spans more than one occurrence, and the UX run verified that this is reachable ONLY under a node-scoped selection: an occurrence-scoped one is filtered by the server, so the header can never appear there. The glyph and text-tier changes did NOT drift — both sit inside story 1's existing scope and reach it through the adopted companions. stories.yaml NOT rewritten; the update never rewrites it.
- (note) GUARD ENTRY CHECKED BEFORE PROCEEDING. An earlier entry warns: the chip rule at tool-presentation-contract.md:159 is correct as written, the UX spines had contradicted it, they were corrected to match, and a later pass must not 'fix' the contract to match the mistake. That guard is honoured, not overridden. It settled a different axis — FALLBACK: ellipsis-truncation inside the pill versus replacement by the family name. The family-name fallback stands unchanged. What was never examined is DISPLAY FORM: whether the surviving name is shown normalised or as the provider sent it. DESIGN.md:389 carries the corrected fallback and DESIGN.md:377 says 'as the provider sent it' in the same file, so the two coexist deliberately.
- (decision) The chip shows the tool name AS THE PROVIDER SENT IT — read_file, Edit, Grep, eval — not the normalised form. New evidence the earlier pass did not weigh: the contract defines normalisation at :68 as case-folding plus stripping underscores and hyphens, which renders read_file as readfile, and every concrete example in DESIGN.md:377 and EXPERIENCE.md:79 and :208 is the as-sent spelling. Normalisation is a MATCHING device for the resolver and never a display transform. The fallback is untouched: a single token of at most 24 characters, else the family name. Pinned by architecture spine AD-1.
- (note) RESIDUAL this skill cannot fix: EXPERIENCE.md:90 says 'normalized' and contradicts its own :79 and :208. That file is owned by bmad-ux and adopted here read-only, so it needs a bmad-ux update rather than an edit from here.
- (decision) ADOPT the architecture spine as a companion: ../../planning-artifacts/architecture/architecture-Archon-readable-agent-transcript-2026-09-12/ARCHITECTURE-SPINE.md, status final, 2026-09-12, feature altitude, paradigm 'functional core, imperative shell', 15 ADs, lint_spine 0 findings plus four reviewer lenses. Adopted, not spec-authored — bmad-architecture owns it. This is what lets [CE] Create Epics and Stories see the architecture at all.
- (decision) The diff body arm carries the computed hunks, not only before and after. Reason: :167 lists '+n −m' among the COLLAPSED-row badges and that count cannot exist without diffing, so the diff necessarily happens while the row is built. Shipping only the two strings would force a second diff at expand time or a differ inside each renderer, and jsdiff's context option defaults to 4 and is silently divergent between two callers. Spine AD-5.
- (constraint) The Inline diff section carried three factual errors, all verified by reading the file. The adapter's path is packages/web/src/components/workflows/source-control/git-hunk-adapter.ts at 57 lines, not source-control/git-hunk-adapter.ts at 58. It does NOT compute a diff — it converts an already-computed GitDiffHunk from the server's git routes into react-diff-view's shape — so 'compute a line diff and adapt it through' it cannot be followed as written. And packages/web has no line-diff algorithm at all: structuredPatch, createPatch, diffLines, myers and longestCommon all return nothing across packages/web/src, and none of its 38 declared dependencies is a differ.
- (decision) diff (jsdiff) 9.0.0 becomes a declared dependency of @archon/web, and packages/web/src/lib/diff-hunks.ts is the only caller of structuredPatch in the tree. Two details the implementation must not miss: jsdiff ships no default timeout and no default edit-length bound, so an unbounded synchronous Myers diff hangs the thread and the module must pass an explicit maxEditLength and refuse oversized inputs, both degrading to null and path-plus-preview; and the backslash 'No newline at end of file' marker can appear twice in one hunk with the first occurrence mid-array, excluded from oldLines and newLines, so every line beginning with a backslash is skipped wherever it sits and advances no counter.
- (decision) Tier 3 gains the Codex prefix strip. The resolver removes the fixed /bin/zsh -lc ' or /bin/bash -lc ' prefix and its matching closing quote BEFORE applying the first-non-empty-line rule; the untouched name stays available for the terminal body and the Raw toggle. Until now the rule existed only in EXPERIENCE.md:93, which would have forced each renderer to implement it across 4,911 of 22,867 rows — the fork spine AD-1 forbids.
- (decision) Two additions to 'Changes to the existing shared layer'. buildAgentHistory() RETURNS the node's folded todo state rather than merely producing it — an object carrying items plus TodoPhase[], because a flat per-item array cannot hold node-level state; its three call sites take a one-line edit each. And the exit code is carried through to the item: it is parsed at agent-history.ts:115 and consumed at :126 only to decide 'failed', then discarded, which leaves CAP-1's own success signal unimplementable as the code stands. Spine AD-7 and AD-14.
- (decision) test-plan.md gains coverage for the three traps this update introduced, plus the one it exposed. New: the Codex prefix strip asserts the headline does not begin with /bin/; the chip asserts the as-sent spelling with 'readfile' named as the regression to catch; a new diff-hunks.test.ts section covers the backslash marker appearing mid-array and twice, the byte-ceiling and maxEditLength degrades returning null without throwing or hanging, repeat-call determinism, and feeding the adapter without tripping requiredLine(); and the collapsed row asserts the exit 1 badge, which is CAP-1's own success signal and currently unreachable. Boundary checks gain two: no renderer imports diff, and the moved adapter takes its types from api.generated.
- (event) Self-validate Pass 1 Coherence: PASS. Seven capabilities, IDs unchanged and unique, each with intent and success. Eleven constraints, five non-goals, a testable success signal. No capability added or renumbered. Spec Law 2 held under pressure: the differ, its bounds and its marker handling are implementation and went to the companion, not the kernel — the only SPEC.md body edit was removing a false word, CAP-5 having claimed the diff renders through 'the existing react-diff-view adapter' when that adapter converts rather than computes.
- (event) Self-validate Pass 2 Preservation: PASS. All six input changes landed and were verified by grep: the spine is companion six of eight and all eight paths resolve on disk; the chip reads 'as the provider sent it' in both the interface comment and the Collapsed row section; the diff arm carries hunks and the block declares the GitDiffHunk import; Tier 3 opens with the Codex wrapper strip; the Inline diff section is rewritten with the real 57-line path, the fact that the adapter computes nothing, and the two jsdiff traps; and the shared-layer section states the widened return plus the discarded exit code. Three superseded strings confirmed absent folder-wide: '58 lines', the old chip rule, and the impossible 'compute a line diff and adapt it through' instruction.
- (event) stories.yaml drift RE-CHECKED, not rewritten. The two entries flagged on 2026-09-10 remain drifted, and this update adds a third divergence: story scope predates diff-hunks.ts entirely, so the CAP-5 slice now carries a module and a dependency no story mentions. Left alone deliberately — [CE] Create Epics and Stories supersedes stories.yaml, and this skill never rewrites it on an update.
- (constraint) SELF-CORRECTION on the import line added to the interface block this session. It read: import type { GitDiffHunk } from './api.generated'. That does not compile — api.generated.d.ts exports only paths, webhooks, components, $defs and operations; there is no named GitDiffHunk. The real form, and the one the spine's AD-6 describes, is to import components and re-alias, exactly as api.ts:472-473 and agent-history.ts:6 already do. Corrected to import type { components } plus a local alias.
