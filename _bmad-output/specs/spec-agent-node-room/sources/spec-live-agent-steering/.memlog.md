---
topic: Interacting with a running agent from its node room — stop it, and send it a message that changes what it does next
updated: 2026-09-13T19:53
---

- (direction) User's framing, verbatim intent: clicking a node of a RUNNING agent must stop being read-only review and become a place to work with that agent. The read-only transcript is a separate, already-finished contract.
- (direction) User specified the control model: while the node runs the send button reads Queue and messages wait; after Stop node the same button reads Send now and sending delivers the whole queue plus the newly typed message in written order, then the node resumes; when it resumes both controls revert automatically. Both follow NODE STATE, never a mode the reader must remember setting.
- (decision) The draft queue lives in browser state, by the user's explicit call — no table, no migration. Justified rather than assumed: the agent runs as a subprocess of the server, so a server death kills the run, and a persisted queue would have had nothing left to deliver to. The UI must say the queue is per-tab instead of letting the operator believe the server holds it.
- (capability) CAP-1 compose while the agent works — a message can be written without disturbing the running node.
- (capability) CAP-2 stop a running node from its own room — today only whole-run abandon exists; node-level stop does not.
- (capability) CAP-3 send the queue on resume — everything written is delivered in written order, then the node continues.
- (capability) CAP-4 the exchange is part of the record — the operator's messages and the interrupted tool call are ordinary transcript rows in the order they happened.
- (capability) CAP-5 mid-turn delivery where the provider allows it — no stop required, gated per provider and never assumed.
- (capability) CAP-6 the interface claims only what it knows — 'sent' until the provider echoes back the id we stamped, and only then 'delivered'.
- (constraint) THE BLOCKER IS OURS, NOT THE PROVIDERS'. IAgentProvider.sendQuery() is a one-shot async generator with no way to push a message in after it starts, and each provider keeps its child process private inside that closure. All five providers in use accept steering on their side; nothing in Archon can reach them. One steering seam on the provider interface, shared by all five.
- (constraint) STOP IS NOT UNDO. Session state is saved to the last completed tool call, but files the agent already wrote stay written — nothing is rolled back. Grok's own shipped docs state it, and Archon's existing abort path has always behaved this way. The control must not imply otherwise.
- (constraint) Delivery confirmation correlates by message id alone, never by matching text or timestamps.
- (constraint) A steer delivery must not emit a turn-start event — aion records that a stray one opens a phantom turn boundary mid-stream.
- (constraint) Provider support is a declared capability that defaults to UNSUPPORTED. An unproven provider is treated as incapable until shown otherwise, which is how aion gates the same feature.
- (constraint) omp — native steer in RPC mode, and steer is its DEFAULT streaming behaviour. Cheapest of the five: RPC emits the same event vocabulary the existing OmpEventParser already reads, wrapped in an envelope. TRAP recorded deliberately: use RPC mode, NEVER omp acp — ACP implicitly cancels a running turn when a new prompt arrives (acp-agent.ts:819-835), which looks like steering and behaves like a stop.
- (constraint) claude — streaming input plus interrupt(); interrupt() is documented as available ONLY in streaming input mode. Needs an SDK bump from the pinned 0.3.209 (latest 0.3.269; the pre-result echo needs 0.3.246). A caller-set uuid on the user message is echoed back as user_message_uuid, which is what turns 'sent' into 'delivered' — and the docs state a plain string prompt, which is what Archon passes today, carries none.
- (constraint) codex — JSON-RPC turn/steer, a soft injection queued into the active turn's input, explicitly NOT a hard cancel.
- (constraint) grok — SETTLED BY EXPERIMENT this session, not inference. x.ai/interject is NOT reachable by a third-party ACP client: over grok agent stdio it answers -32601 Method not found, byte-identical to the answer given to a method invented as a control. The reachable channel is the hooks extension the handshake DOES advertise — x.ai/hooks with blockingEvents including pre_tool_use and stopSignals including additionalContext, a text channel to the agent at each tool boundary. Advertised and reachable; the exact payload is read from the capability shape and wants one implementation spike.
- (constraint) deepseek — no native mid-turn, MEASURED: a second prompt is rejected in 2ms with -32602 'a prompt is already in flight for this session'. Warm cancel-and-continue on the same open connection measured at 31ms end to end, and the cancelled turn's partial output is RETAINED. The first turn costs about 3.6s of context assembly, so a cold stop-and-resume pays that again every time the operator speaks. Requires not closing the session and not reaping the child.
- (note) Convergence worth building on: three of the five deliver at the SAME point — the next tool-call boundary. omp checks steering between tool calls, grok blocks at pre_tool_use, claude's hook contract is the same shape. That is the natural rest point of an agent loop, so the seam should take 'deliver at the next boundary' as its default meaning and treat immediate interrupt as the special case, not the reverse.
- (decision) NON-GOAL: qodercli, pi, copilot and opencode. The user stated they do not use those harnesses today. Two of them were checked far enough to know they could join later.
- (decision) NON-GOAL: persisting the draft queue — see the browser-state decision and its justification.
- (decision) NON-GOAL: cancelling one individual tool call. No provider offers it: claude reports the command unsupported at that granularity and codex has nothing on the wire below turn level. Stopping is turn-level or session-level only.
- (decision) NON-GOAL: the read-only transcript itself. That is spec-readable-agent-transcript, already specced and reviewed, and it already renders the interrupted-tool outcome that a stop produces.
- (question) OPEN: what does the composer do on a FINISHED node — does a message re-run it, or is the composer simply absent? Not decided; deliberately left rather than invented.
- (question) OPEN: does deepseek's session/cancel interrupt a running TOOL CALL as cleanly as it interrupts a model stream? Only streaming was exercised.
- (question) OPEN: grok's hooks payload — the mechanism is advertised and reachable, but the exact additionalContext shape is inferred from the capability declaration and needs one live spike to confirm.
- (event) grok reachability question CLOSED by direct experiment: ACP initialize handshake plus three method probes over grok agent stdio, no model call, no cost, no orphaned process.
- (event) Self-validate Pass 1 Coherence: PASS. Six capabilities, ids unique and sequential, each with both intent and success. Seven constraints, four non-goals, a success signal written as a demonstrable scenario, three open questions left open rather than invented. Spec Law rule 2 held: every per-provider mechanism is implementation and went to provider-steering-matrix.md, and the control state machine went to control-states.md, so the kernel carries prose only.
- (event) Self-validate Pass 2 Preservation: PASS. All six companions resolve on disk. Every load-bearing claim landed and was grep-verified — the one-shot seam, the per-tab queue, stop-is-not-undo, id-only correlation, the no-turn-start rule, unsupported-by-default gating, and next-boundary-as-default in the kernel; the omp ACP trap, deepseek's measured 2ms rejection and 31ms warm path and 3.6s cold cost and retained partial output, grok's Method not found result, and omp's steer-is-default in the matrix. Wrapper-only content dropped: none.
- (event) spec created
- (constraint) Node-level stop has no engine state today: dag-executor.ts:3124-3157 records node_failed + recordFailedStatus('Cancelled by user') when the per-node AbortController fires during streaming, so reusing the existing abort path would FAIL the node, not suspend it.
- (constraint) Pause in Archon is run-level, not node-level: pauseWorkflowRun(runId, approvalContext) at dag-executor.ts:6832; the node returns 'completed' and the between-layer status check halts the run (:6744, :6746). There is no suspended-in-place node.
- (constraint) Non-running status (cancel/delete/pause) is checked only BETWEEN DAG layers (dag-executor.ts:9695), never inside a running node. Nothing outside the node's own closure can reach it mid-turn today.
- (question) Which lifecycle shape does 'stop a node' take: a new node state the executor respects, or run-level pause reusing the approval machinery with a synthetic context (the pattern container write-back already uses)? Undecided; blocks CAP-2 and CAP-3.
- (constraint) The executor is NOT always in the API server's process. Web dispatch imports and awaits executeWorkflow in-process (server/src/routes/api.ts:3200-3228); CLI --detach spawns a detached child in its own process group (cli/src/commands/workflow.ts:696-722). An in-memory steering registry therefore reaches in-process runs only.
- (decision) AskHuman does NOT prove a process-independent delivery path: listPendingInteractions (dag-executor.ts:10367) is a RESUME preflight read at run start, not a poll by a live executor. Pause ends the execution; resume re-enters. So the answer reaches a NEW execution, never a running one.
- (question) Is steering a detached run (CLI --detach, its own process) in scope, or is the feature scoped to runs the server itself executes? Scope decision, not a technical one.
- (constraint) Operator rows need an explicit speaker marker. workflow_node_messages carries kind text|tool|status with no speaker field (schemas/node-message.ts:39-43), and nodeTranscriptMetadataSchema is .strict() (schemas/node-execution.ts:29-43), so metadata is NOT free JSON: an origin field must be added to the schema. Additive, no DB migration (the column is JSON), but a typed contract change plus a regenerated api.generated for the web.
- (decision) Operator messages are stored as node transcript rows with metadata.origin='operator' rather than a new table or a widened kind enum. Reuses existing enum values, matching aion's own lesson (service.rs:3559-3568) and Archon's additive-only schema rule.
- (constraint) CROSS-SPEC: AgentHistoryItem in spec-readable-agent-transcript has kinds assistant|tool|lifecycle. An operator row is none of the three, so CAP-4 requires a new item kind and a row treatment on both shells in THAT spec. Logged as a dependency; that spec is not edited from here.
- (decision) Stale-steer guard: a steer carries node id plus retry epoch as optimistic concurrency, so a steer aimed at a superseded attempt is rejected rather than misdelivered. Shape taken from aion's expectedTurnId (codex_conn.rs:4043-4090, aion report section 5).
- (constraint) After a stop, do not call execute directly: wait for the runtime to report ready first, or hit the race aion named in AcpSendBox.tsx:771-775. The stop and the resume are two gated steps, never one call.
- (constraint) omp's steering queue defaults to one-at-a-time (steeringMode default, scoutcli report section 1.3), so delivering a whole queue in written order needs set_steering_mode 'all' or explicit per-item sequencing. CAP-3's ordering promise is not free.
- (constraint) omp RPC: send negotiate_protocol protocolVersion 2 immediately or stdout frames over 1 MiB are lossy (scoutcli report section 1.4).
- (decision) Codex row downgraded to unverified. turn/steer is aion's app-server JSON-RPC spoken directly (codex_conn.rs:4043-4090); Archon speaks @openai/codex-sdk 0.144.5 via thread.runStreamed (codex/provider.ts:1093), and whether that SDK surfaces steer is unchecked - node_modules is blocked by a hook. The aion report itself says 'assume not'.
- (decision) Corrected the overclaim that all five providers accept a message into a running session. Accurate count: omp yes natively; claude yes via streaming input; codex unverified through our SDK; grok only via the advertised hooks channel, never exercised; deepseek NO - a concurrent prompt is rejected in 2 ms, and cancel-and-continue is a cancel, not mid-turn.
- (note) The grok leader socket (~/.grok/leader.sock, grok agent leader, --leader: 'multiple clients share one backend', scoutcli report section 2.5) is the one out-of-band channel found into a session the caller did not spawn. Unexplored, and the only lead that could reach a detached run.
- (decision) The browser-state queue diverges from aion, which persists before delivering and flips a badge on the echoed id (aion report, copy item 2). The user chose frontend state explicitly; the cost is no crash safety and no consumed badge. Recorded as a divergence with its price, not as a free choice.
- (decision) Auto-send queue mode (aion's Auto/Manual pill, CommandQueuePanel.tsx:505-526) is a non-goal: every send stays operator-initiated.
- (event) Advisor review 2026-09-12: three blockers confirmed by source (engine node lifecycle, executor process boundary, operator-row storage), plus a fourth from reading the three adopted reports in full rather than their summaries.
- (event) Self-validate pass 1 (coherence): PASS - 6 capabilities each carry intent and success, constraints all bend a decision, 4 non-goals, success signal demonstrable, 6 open questions with the blocking one named first.
- (event) Self-validate pass 2 (preservation): re-ran against the three reports READ IN FULL, not their summaries. Recovered eight claims the first pass missed: the stop-then-ready race, omp one-at-a-time steering default, omp 1 MiB protocol negotiation, the expectedTurnId stale-steer shape, the grok leader socket, the codex SDK reachability gap, aion's persist-before-deliver divergence, and auto-send mode. All now land in SPEC.md or a companion.
- (event) Spec updated: engine-integration.md added as a fourth companion, provider-steering-matrix.md gained a Verified column and corrected three rows, SPEC.md gained the second blocking seam, the process-boundary constraint, the storage decision, the cross-spec dependency, and two open questions.
- (decision) CORRECTION, and it shrinks the build: a running node DOES poll the database mid-stream. dag-executor.ts:2304-2334 calls deps.store.getWorkflowRunStatus every CANCEL_CHECK_INTERVAL_MS (10s, :682) inside the streaming loop and aborts the stream when the status is no longer streamable. My earlier claim that nothing outside the node's closure can reach it while it runs was wrong.
- (decision) Consequence: an out-of-band stop reaches a node in ANY process, detached included - it travels through the database, not through memory. So the scope question is narrower than I framed it: stop works everywhere; only low-latency mid-turn delivery (CAP-5) needs the executor in the server's process.
- (constraint) shouldContinueStreamingForStatus (dag-executor.ts:700-702) returns true for both running and paused, deliberately - a concurrent gate must not kill a sibling mid-stream (:2306-2309). So setting the run to paused does NOT stop a streaming node; an operator stop needs its own signal.
- (constraint) A database-polled stop is not instant: up to 10 seconds (CANCEL_CHECK_INTERVAL_MS). The interface must not present the stop as immediate.
- (decision) AskHuman is the precedent for stopping a node without failing it, and it is the strongest of the three options: dag-executor.ts:3339-3347 catches AskHumanAwaitingError, calls pauseOnAskHuman, and returns state 'pending' - not failed. ask-human.ts:110-124 pauses the run then throws. That is CAP-2 plus CAP-3 minus the interface: interrupt a live turn, pause rather than fail, resume re-enters the same node with an injected message.
- (decision) The lifecycle question is now a narrow fork: route an operator stop into the AskHuman pause-and-resume-into-node path with an operator-originated trigger, or add a node state the executor respects. The synthetic-approval-context idea is the weaker analogy and is dropped.
- (decision) Only the CANCEL path fails the node, not every abort: the classifier at dag-executor.ts:3386-3394 excludes AskHumanNoStarterError and AskHumanPauseFailedError before recording 'Cancelled by user'. An operator stop needs the same kind of exclusion.
- (constraint) CAP-2's blast radius corrected: under run-level pause, nodes already streaming in the same layer finish their own output and NO later layer starts - independent nodes do not continue. The earlier wording ('independent nodes continue, dependents wait') pre-decided a per-node suspended state the spec has not chosen.
- (decision) Codex steer partially settled via published docs: thread.turn() returns a TurnHandle carrying steer(input) and interrupt(), documented in sdk/python/docs/api-reference.md. The TypeScript README shows only run() and runStreamed(). So the app-server supports steering and one official SDK exposes it; whether the pinned TypeScript 0.144.5 does is still unread - narrower than 'assume not'.
- (event) Second advisor pass resolved: the reach claim was wrong (a running node polls the run status every 10s), AskHuman is the lifecycle precedent rather than a synthetic approval context, CAP-2's blast radius no longer pre-decides the fork, and codex moved from 'assume not' to 'documented for Python, unread for TypeScript'. A 'stopping' state was added to the control table and diagram because the stop is not instant.
- (direction) USER DECISION: an operator stop pauses the whole run, reusing AskHuman's pause-and-resume-into-node path. The per-node suspended state is rejected. Blast radius accepted knowingly: nodes already streaming in the same layer finish their output, and no later layer starts - including independent ones. Rationale: coding workflows are mostly sequential chains, so the difference is near zero, and the reused path is already tested.
- (decision) Lifecycle fork closed. CAP-2 can now state its blast radius outright instead of deferring to the companion, and the open question is retired.
- (event) Spec re-derived after the lifecycle decision: CAP-2 states its blast radius outright, a new constraint records the decision and its accepted cost, the lifecycle open question is retired (5 remain), and engine-integration.md section 2 now reads as settled rather than as a fork.
- (decision) Wording synced with the UX run: the stop control lives on the composer dock, not in the panel header. The header cannot hold it - flex-wrap row, eight items, 460px (NodeRoomHeader.tsx:58-95). Changed in SPEC.md CAP-2 and in control-states.md heading, table columns and state diagram.
- (change) control-states.md synced twice more with the UX run: the stopping row now says aria-disabled rather than disabled - the native attribute blurs the element that carries it, so the operator who pressed the control lands on body for the whole poll - and the reason is stated in the row.
- (decision) Adopt the finished UX spines as companions: ../../planning-artifacts/ux-designs/ux-Archon-agent-node-room-2026-09-09/{DESIGN,EXPERIENCE}.md. The bmad-ux Update run made that spine serve this spec (composer dock, stop/send controls, operator row, four status channels), but companions: never listed them — downstream discovers the contract from frontmatter, not from prose in control-states.md, so bmad-architecture would never have seen the dock contract. Adopted, not spec-authored: bmad-ux owns them.
- (decision) Open question 'must mid-turn reach a detached run' moved to Deferred with revisit condition 'when the first provider declares mid-turn'. Not a user decision to spend: CAP-5 defaults to unsupported and no provider declares it, and stop already reaches detached runs via the DB poll — only mid-turn binds to the process. Also verified for the finished-node question: retry-node invalidates target + ALL descendants (getRetryInvalidatedNodeIds, retry-state.test.ts:101-111) and resets the tracked checkout to the node checkpoint (dag-executor.ts:276-308); the CLI targeted path has no terminal-status refusal (workflow.ts:3852-3909), so a message on a finished node is a cascading re-run, not a composer choice.
- (decision) Product owner settled the finished-node question: the dock does not appear on a finished node. Field and both controls absent. Rejected alternative was not a composer choice at all — sending to a finished node means retry-node, which resets the tracked checkout to that node's checkpoint (dag-executor.ts:276-308) and invalidates every descendant (getRetryInvalidatedNodeIds, retry-state.test.ts:101-111), with no terminal-status refusal on the CLI targeted path (workflow.ts:3852-3909); that is its own capability with its own confirmation. Open questions in SPEC.md now: zero blocking, one deferred.
- (event) Self-validate after the finished-node update. Pass 1 coherence: 6 capabilities each with intent+success (rule 1); 14 constraints, the new one bends the dock's rendering and cites the retry-node cascade it rules out (rule 3); non-goals present (rule 4); success signal unchanged and demonstrable (rule 5); CAP-1..CAP-6 stable and unique, none renumbered (rule 6); the new constraint runs long but every clause carries source, matching its neighbours (rule 8). Pass 2 preservation: 9 companions all resolve; the settled decision landed in SPEC.md Constraints, control-states.md finished row, and both spines; the retry-node evidence is preserved with file:line. No wrapper-only drops. Verdict: both passes clear.
- (direction) UPDATE from the bmad-architecture run (spine architecture-Archon-live-agent-steering-2026-09-12, status final). Owner (Kevin) reversed parts of this spec; propagating so spec + spine speak with one voice. CAP ids preserved.
- (decision) CAP-5 reworded (REVERSES prior): steering is a NORMAL PROMPT on the common provider interface IAgentProvider, not a per-provider opt-in capability. No steer() primitive; the only interface change is that sendQuery's INPUT may be an async iterable (stream) for providers that inject mid-turn. Universal floor: the executor queues the operator message and it becomes the next sendQuery at turn-end — every provider satisfies this (queue-and-flush), so none is disqualified by transport. Mid-turn (before turn-end) is an acceleration where the open stream accepts it (claude, omp) — same prompt, earlier.
- (decision) SUPERSEDES the constraint 'Provider support defaults to unsupported, and is claimed only from an exercised mechanism.' Steering is universal over a queue-and-flush floor; mid-turn acceleration is claimed where the provider's transport allows it. Kept: 'deliver at the next boundary is the default meaning; immediate interrupt is the special case.'
- (decision) provider-steering-matrix: deepseek is NOT 'No'. The 2ms rejection of a CONCURRENT prompt is a transport detail, not a contract incapacity; DeepseekProvider absorbs it via queue-and-flush (hold the message; send as the next prompt when the in-flight turn returns; boundary = turn-end, coarser than intra-turn, non-interrupting). The 'two yes / one no / two unknown' reading is replaced by the universal-floor model: every provider delivers; they differ in boundary granularity and in delivery-confirmation.
- (decision) Open question CLOSED = NO: the pinned @openai/codex-sdk does not expose thread.turn()/TurnHandle.steer(). Web-verified across 0.144.5 / 0.154.0 / alpha 0.155 — TS SDK exposes only run()/runStreamed(); the steer primitive lives in the app-server protocol, not the SDK. codex is queue-and-flush. The dep is a caret ^0.144.5 (floats to 0.154.0), not an exact pin.
- (decision) claude transport correction: streaming-input + interrupt() are ALREADY in the pinned 0.3.209 (interrupt >=0.3.205, AsyncIterable input <=0.3.142). Only CAP-6's user_message_uuid pre-result echo needs an upgrade (>=0.3.246). The steering transport does not need a claude SDK upgrade; only delivery-confirmation does.
- (question) NEW open question (unverified, spike not assumption): does claude AsyncIterable (streaming) input compose with HITL/AD-6's host-abort resume protocol? That protocol was exercised only on a string prompt at 0.3.209.
- (decision) engine-integration mechanism CORRECTED (was the live contradiction): the operator stop does NOT ride the :3386 cancel classifier. The poll does abort();break; (dag-executor.ts:2325) and falls to the INLINE post-stream check at :3123-3157 (unconditional node_failed); it never throws so never reaches :3386 or pauseOnAskHuman :3339. Fix: the stop sets operatorStopRequested; BOTH the inline :3123 site (the real path) AND the :3386 classifier (defensive) branch on it to the pause ending (node pending, run paused), never node_failed.
- (decision) engine-integration: stop DETECTION must be TIMER-DRIVEN, not the chunk-bound poll — the cancel-check sits inside the for-await(msg) body (:2304-2334) so it only fires on a chunk; a silent long tool call would defer the stop past the '<=10s' promise. It rides a timer on the withIdleTimeout precedent (callback :2298, already timer-driven). The EXECUTOR pauses at the inline check (like ask-human.ts:110-124), NOT the Stop route (which writes only the marker).
- (decision) engine-integration: a marker-stopped node has NO pending_interactions row, so the Ask re-entry predicate (:1892) never selects it — steering has its OWN re-entry that reads the durable marker messages and delivers them as a PLAIN PROMPT (not resumeInteractions, which is keyed to an answered pending row's tool_use_id an operator message lacks). Resume clears the marker atomically or the poll re-fires.
- (constraint) Stop-signal constraint UPDATED: the metadata.steering marker ALSO carries the ordered operator messages DURABLY — a stopped run is 'paused' = durable, so the stop-path queue cannot be browser/memory (only the PRE-SEND draft is browser state). Three durabilities: browser (pre-send draft) / durable marker (stopped run) / in-memory registry (live mid-turn, in-process). The stale guard's attempt key is {node_id, retry_epoch, iteration} — loop iterations share a retry_epoch, so node_id+retry_epoch alone cannot distinguish iteration N from N+1.
- (constraint) Stop and Send are NEW server routes POST /api/workflows/runs/:runId/nodes/:nodeId/stop and .../send (the existing resume route is {runId}-only, no body, not extended); a Send with a stale attempt key returns 409 (HITL/AD-7 superseded-write precedent). Kept: an operator message must not emit a turn-start event (phantom boundary corrupts the transcript record).
- (decision) Adopt the architecture spine as a companion of SPEC.md (../../planning-artifacts/architecture/architecture-Archon-live-agent-steering-2026-09-12/ARCHITECTURE-SPINE.md), since downstream now reads it for the engine/provider invariants.
- (event) Self-Validate after UPDATE. Pass 1 coherence: 6 CAPs each intent+success (rule1); CAP-1..6 stable/unique (rule6); 5 non-goals (rule4); success signal demonstrable (rule5); constraints all bend design (rule3); lean (rule8). Pass 2 preservation: every change-set item landed — CAP-5 reworded to universal-floor + mid-turn acceleration; 'provider defaults unsupported' constraint replaced; stop-signal/durability/attempt-key/routes constraints updated; Why rewritten; matrix rebuilt (deepseek + codex = queue-and-flush, claude SDK claim corrected, codex settled NO, honest-reading replaced by two-axis model); engine-integration §1/§2/flowchart/what-this-adds corrected to inline :3123 + timer + executor-pause + own-re-entry; spine adopted as companion. All tables consistent, mermaid balanced, 10 companions resolve. No wrapper-only drops. Verdict: both passes clear.
- (decision) engine-integration.md corrected to match spine AD-12 fix (advisor, post-sync): marker is CONSUMED by the executor at re-entry (read->deliver->clear first, before stream opens), not cleared in the flip-to-running transaction. Detached runs hydrate from DB, so clear-at-flip loses the messages. Fixed both sites: section 2 second bullet and 'What this adds' step 2. Timer abort scoped to phase:'stop'.
- (event) Propagate spine RE-GATE triage (owner full-plan) into SPEC + spec-authored companions. Spine amended AD-3/7/8/9/11/12/13/14/15, lint clean, ids stable.
- (decision) Constraint: the stop marker is PER-NODE keyed metadata.steering[node_id] (map, not one run-level record), each entry {attempt,phase,messages,provider_session_id}; written whole (replace), cleared by key-delete; executor enriches provider_session_id at the inline abort. Reason: one run-level marker is clobbered by concurrent stopped siblings or a siblings natural completion (A1), and a re-entry with no session home resumes a fresh session breaking CAP-3 continuity (A2); whole-write + key-delete also sidesteps the two-dialect jsonMerge array-drop (A3).
- (decision) Constraint: three-queues discriminator is NODE liveness, not run liveness — a node can stream while its run is co-paused by another nodes Ask gate (A4). live node -> registry memory; stopped node -> durable per-node marker.
- (decision) Constraint: Send is gated on the stop having landed — requires marker phase:stop AND node pending, else 409 (UI stopping maps to that 409, retries when projector reports stopped). Closes the race where Send flips phase before the <=10s timer reads stop and scopes the abort out (A4). Attempt key re-validated AT DELIVERY too (not only at the route); a mismatch at delivery is a failed-send that returns the message to the FRONT of the queue (A6).
- (decision) Constraint: the queue is delivered to the provider BEFORE the marker is cleared, in one transaction that appends the operator rows and deletes the node marker key; a crash before it re-delivers on the next hydration. AT-LEAST-ONCE, stated as accepted trade-off (never-discarded forbids at-most-once; exactly-once needs a provider ack that does not exist). The consume runs on EVERY executor hydration of a paused->running run, any resume trigger (H1/H2/A5/A9).
- (decision) CAP-6 sharpened: delivered needs claude SDK >=0.3.246 (user_message_uuid echo); at the inherited pin 0.3.209 delivered is UNREACHABLE for every provider incl claude — sent ships for all until the bump (H3). Open question reframed accordingly.
- (decision) provider-steering-matrix WV-1: codex-sdk lockfile pins 0.144.5 (caret not floated), npm latest 0.153.4 — surface conclusion unchanged. control-states: Send control disabled->409 mapping while stopping; A8 interrupted-glyph depends on transcript deriveOutcome (cross-spec, noted).
- (event) Self-Validate both passes clear after gate-triage re-derive: Pass1 coherence CAP 6/6/6 intents+success 6/6, mermaid balanced all files, no stale run-liveness/clear-first phrasing, 4 spec files one voice on per-node marker + deliver-then-clear + Send-gate 409 + delivered-at-pin; Pass2 preservation all triage claims landed; 10 companions resolve.
- (event) SPEC + engine-integration + provider-steering-matrix + control-states re-derived to amended spine; self-validate clear; handing to advisor per owner.
- (decision) Mirror advisor post-amend fixes: Send gate BRANCHES (stop-path vs CAP-5 registry enqueue vs detached 409), not unconditional; hydration consumes phase:stop too (delete key = spend the stop on any resume, flagged consequence); Send appends to drained registry messages (read-modify-write, CAS-guarded); codex version line corrected to lockfile 0.144.5/latest 0.153.4.
- (event) Post-advisor blocker fixes propagated (Send-gate branch, phase:stop delete-on-hydration, read-modify-write append, codex nit); Self-Validate re-checked: CAP 6/6/6, mermaid balanced, residuals clean, companions resolve.
- (decision) FUNDAMENTAL REFRAME (owner, aion-verified): steering = interrupt-in-session, node NEVER pauses; Cancel node = existing teardown, untouched. Supersedes the durable stop-and-resume model in CAP-2/constraints/engine-integration. CAP-2 must be rewritten: no run pause, no node pending, no resume — Stop means interrupt the agents current generation (provider primitive, session alive), operator message continues same session, node keeps running. In-process only v1; detached + restart-durability deferred. New engine behavior: steered node runs multiple turns on one live session (rides attemptResumeId re-ask seam).
- (decision) CAP-2 reframe -> 'Interrupt the agent's thinking; the node keeps running'. Success: an interrupt control ends the agent's CURRENT generation (claude interrupt() / stream-abort on the executor AbortController), the provider session stays ALIVE, and the node stays running (sub-state idle-after-interrupt) — never paused, pending, or failed. The transcript shows the in-flight tool call as interrupted. Interrupt is NOT undo (files already written stay). Cancelling the whole node is the EXISTING Cancel feature, separate and untouched.
- (decision) CAP-3 reframe -> 'Redirect and continue on the same live session'. Success: once the agent is idle-after-interrupt the send control reads 'Send now'; sending delivers every queued message plus the one just typed, in written order, as the next turn on the SAME provider session (attemptResumeId re-ask seam dag-executor.ts:2290); the node CONTINUES (never 'resumes' from a pause). Ordering enforced by us. Controls follow the projected agent sub-state, not a remembered mode.
- (constraint) DELETE all durable-model constraints (superseded): metadata.steering marker, phase:stop/send, CAS, per-node map, attempt-key {node_id,retry_epoch,iteration}, 409-on-phase Send gate, consume-on-every-hydration, two-gated-steps, pause-the-run, timer-vs-chunk detection, at-least-once delivery. Clean model has NO durable steering state. Replace with: (a) steering acts on the live agent never the node lifecycle; (b) interrupt = provider primitive or stream-abort, sets operatorInterrupt flag before abort() so the inline :3123 abort->node_failed check re-enters the loop instead of failing; (c) steered node runs multiple turns on one live session (the one new engine behavior); (d) in-process registry (runId,nodeId->live handle + in-memory queue) is the sole mechanism, detached = not steerable v1, restart drops in-flight steer; (e) delivery timing is the operator's choice (Queue default / Send-now / soft-inject), only soft-inject is provider-gated.
- (constraint) Turn-end has two causes (operatorInterrupt flag discriminates): natural end -> queue pending? next turn : node completes; interrupted end -> partial result (no schema-valid output_format) must NOT be validated/completed/DAG-advanced, enter idle-await -> queue pending? next turn : wait bounded. Routes: Send + Interrupt are new (POST .../nodes/:nodeId/send and .../interrupt), resolveAuthContext under HITL/AD-7, NO phase/marker gate. The registry queue absorbs races (Send-during-interrupt, Send-to-dead-turn fold into next turn); the ONLY refusal is a not-running node — finished->409 (draft stays), detached/no-live-handle->'not steerable here'.
- (assumption) [ASSUMPTION, product call for Kevin] idle-await bound: interrupt then empty queue -> node's existing idle timeout runs from the interrupt moment, on expiry node FAILS ('interrupted by operator, no redirect received'), operator resumes to retry. Not completes (partial output), not indefinite (in-process only, unbounded in-memory wait fragile). Alternative = approval-gate-style wait-forever. OPEN QUESTION for Kevin.
- (decision) Non-goals add: 'Cancelling the whole node' = the EXISTING Cancel/abort feature, untouched, out of scope (this feature only interrupts the agent, never the node). Keep qodercli/pi/copilot/opencode, persisting-draft, auto-send, cancel-one-tool-call, read-only-transcript-itself. Success signal reframe: operator interrupts the agent's thinking + sends -> agent continues on the SAME session against the right target, node never stopped; transcript later shows wrong cmd, correction, right cmd in order.
- (event) Reframe decisions fully logged; re-deriving SPEC.md + engine-integration.md + provider-steering-matrix.md + control-states.md to the clean in-session model (spine ARCHITECTURE-SPINE.md lint-clean, 11 ADs).
- (event) Self-Validate (clean model) PASS both passes. Pass 1 coherence: CAP-1..6 each have intent+success; CAP-2 reframed to interrupt-agent-thinking, CAP-3 to redirect-continue-same-session; constraints all bend (durable ones deleted, clean ones added); non-goals explicit incl. new 'Cancelling the whole node = existing Cancel'; success signal testable; CAP ids stable. Pass 2 preservation: durable-term scan finds ONLY intentional superseded/NOT-doing negations (no leaked assertions); clean-model anchors (operatorInterrupt, idle-after-interrupt, :3123, resumeThread, multi-turn-one-session, in-process registry, origin=operator) all present; all 10 companions resolve. SPEC + engine-integration + provider-steering-matrix + control-states re-derived to clean model.
- (constraint) CORRECTION (advisor round 2): interrupted turn-end ALWAYS enters idle-await whatever the queue holds, drains ONLY on operator Send now (never auto-fire) — preserves ratified Send now moment + matches control-states idle-after-interrupt row + spine in-session mermaid. Natural end auto-drains (Queue contract). Races wait in queue (no 409); only a not-running node refuses (finished->409 draft stays, teardown=last queue check). Idle-await bound = fresh timer + configured duration + explicit FAIL (NOT the existing idle-timeout which completes the node). Fixed in SPEC.md two-causes constraint + race constraint + open question; engine-integration.md §2 text+mermaid+bound.
- (decision) OWNER DECISION (Kevin): idle-await bound = FAIL after 30 minutes (resolves the [ASSUMPTION]). Fresh 30-min timer -> explicit fail; /workflow cancel during idle-await still ends node. SPEC open-question moved to 'Settled'.
- (constraint) CORRECTION (advisor round 3): interrupt uses a per-turn signal, NOT the node-level nodeAbortController (one-shot, Cancel's, :2209/:2221/:3031/:3123). sendQuery gets AbortSignal.any([node-level, per-turn]); fresh per-turn per turn enables multi-turn; :3123 stays Cancel-only; operatorInterrupt = which-signal-fired. Fixed SPEC interrupt constraint + process-boundary constraint ('all of steering needs executor in-process', was 'only mid-turn'). Fixed engine-integration §1/§2 text+mermaid+build-list. Polish: control-states Send-now phrasing, matrix omp interrupt cell.
- (constraint) GATE-FIX (round 4): mirror of spine AD-2/AD-4/AD-6/AD-11 fixes into SPEC + engine-integration + matrix. Interrupt constraint: canReask stops on operatorInterrupt, validation skipped on interrupt, branch after :3124 (Cancel dominates), flag per-turn reset, result-not-flag discriminator, synchronous per-turn abort. idle-await: own timer-driven cancel poll (:682/:700-702), resolves exactly once, 30-min fail->resume=fresh session. operator row = 3 metadata fields (origin/operator_user_id/message_id); every-terminal reconcile sent-vs-message_id -> 'Never sent'; global order = receipt order. soft-inject spike-gated, v1 floor interrupt+Queue. Anchors corrected :3124/:3032/:2223/:3387; codex 0.154.0.
- (change) control-states.md state diagram: added transition 'Interrupting --> Generating: turn finished before interrupt landed, queue auto-drains (turn N+1)' alongside the existing 'Interrupting --> Finished' (now labelled queue-empty). Mirrors spine AD-4's natural-end auto-drain; the diagram previously had only the finish branch for this race. Advisor-caught during bmad-ux re-check.
- (constraint) SC 2.2.1 reset-on-activity (owner-chosen) propagated: the 30-min idle-await timer is an INACTIVITY timer re-armed by a debounced authorized operator 'composing keepalive' over the Send/Interrupt route family (AD-11, not a new grant) — re-arm input, NOT a fourth resolution (idle-await still resolves exactly once on Send now / cancel-poll / timer). Added to SPEC.md line 93, engine-integration build-list item 2, control-states idle-after-interrupt row. Consistency with spine AD-4.
