---
topic: Agent Node Room — unified read (readable transcript) + write (live steering) contract for the agent node view, merged from spec-readable-agent-transcript + spec-live-agent-steering
updated: 2026-09-13T22:56
---

- (direction) MERGE (owner Kevin, this session): unify spec-readable-agent-transcript (read half) + spec-live-agent-steering (write half) into one spec 'Agent Node Room'. Two halves of the agent node view: READ = scannable transcript replacing raw-JSON tool rows (retroactive, no schema/migration/backend); WRITE = send + interrupt a live agent without stopping the node (in-process, no durable state). Both land in packages/web and share the two UX spines. Content preserved verbatim in meaning; only CAP ids renumbered + cross-refs reconciled.
- (capability) CAP-1 (was read CAP-1) Scan a node's work without expanding anything — intent: read tool calls one line each (what ran, on what, worked?); success: 40 calls -> 40 single-line rows (family chip + status glyph + headline + right badges), success collapsed / failed expanded, no serialized-data punctuation on a collapsed row.
- (capability) CAP-2 (was read CAP-2) Expand a call into a body shaped for the tool kind — success: each family renders its declared body arm per tool-presentation-contract; unmatched tool = <=3 scalar key:value ({..}/[n] collapsed), never JSON; generic fallback <2% of rows.
- (capability) CAP-3 (was read CAP-3) Follow the checklist as state not mutations — success: both provider shapes fold to one TodoPhase[] per todo-fold-contract; checklist renders once at last todo call, earlier ones collapse; rm-emptied phase disappears.
- (capability) CAP-4 (was read CAP-4) See what a subagent dispatch asked for — success: normalize to TaskSubtask[]; batch context as markdown then one collapsible card per subtask (name + agent).
- (capability) CAP-5 (was read CAP-5) See what a file edit changed — success: before+after -> line diff via react-diff-view (Claude always qualifies, SDK-typed old_string/new_string; Codex falls back to path+preview); never fabricate a diff from one side.
- (capability) CAP-6 (was read CAP-6) Tell attempts and loop iterations apart — success: >1 occurrence_id -> header per group, single-occurrence -> none; group on occurrence_id never attempt_id.
- (capability) CAP-7 (was read CAP-7) Keep the raw payload reachable — success: every card has a Raw toggle (JSON, closed by default); the ONLY place JSON is allowed; canLoadFullOutput flow preserved.
- (capability) CAP-8 (was steering CAP-1) Compose while the agent works — success: composer mounted+enabled while node runs, send reads Queue; sending holds the message (node untouched, nothing interrupted/lost), delivered as next turn at natural turn-end; queue stated per-tab.
- (capability) CAP-9 (was steering CAP-2) Interrupt the agent's thinking; the node keeps running — success: interrupt control ends the agent's CURRENT turn (claude interrupt() or stream-abort), session stays alive; node stays running (agent -> projected idle-after-interrupt sub-state), never paused/pending/node_failed; in-flight tool call shows 'interrupted' not 'failed'; distinct from Cancel (separate, untouched); nothing implies the interrupt undid written work.
- (capability) CAP-10 (was steering CAP-3) Redirect and continue on the same live session — success: when idle-after-interrupt send reads Send now; delivers every queued msg + the one just typed in WRITTEN ORDER as next turn on same session (attemptResumeId re-ask seam); node continues (never 'resumes', never paused); ordering enforced by us; controls follow projected sub-state.
- (capability) CAP-11 (was steering CAP-4) The exchange is part of the record — success: operator messages + the interrupted tool call appear as ordinary transcript rows in happened-order (between the call interrupted and the one caused); an operator row is visibly the operator's, never mistakable for agent text. INTERNAL DEPENDENCY on read half (see cross-ref reconcile entry).
- (capability) CAP-12 (was steering CAP-5) Mid-turn delivery as fast as transport allows — success: sending is an ordinary prompt (not a steer primitive); WHEN it arrives is operator's choice, only soft-inject provider-gated. Queue (every provider) = next turn at boundary, non-interrupting; soft-inject (claude streaming input) = before turn-end no interrupt/no interrupted-call/no turn-start. claude mid-turn SPIKE-GATED (AsyncIterable+resume unverified at pin) -> v1 floor = interrupt+Queue; no provider disqualified.
- (capability) CAP-13 (was steering CAP-6) The interface claims only what it knows — success: message reads 'sent' until provider echoes the stamped id -> 'delivered'; correlation by id alone; only claude echoes and needs SDK >=0.3.246; at pin 0.3.209 nothing echoes so all stay 'sent'; delivered becomes reachable (claude-only) post-bump.
- (constraint) SHARED (both halves, packages/web): @archon/web must not import @archon/workflows (wire types via api.generated.d.ts through lib/api.ts); Console must not import from @/components/ (shared logic in packages/web/src/lib/, JSX written twice thin); strict TS no unjustified any, ESLint 0 warnings, bun run validate is the gate; code comments/test names carry NO plan/section/finding refs (explain the invariant).
- (constraint) READ half: no schema change / no migration / no backend change (retroactive by definition); no raw JSON.stringify as a default presentation anywhere — survives ONLY behind CAP-7 Raw toggle; status decodable WITHOUT colour (a glyph char carries it, colour only reinforces); chip = tool name only when a single token <=24 chars, else family name (24 is the guard not 'truncate w/ ellipsis'); tool id duck-types over alias sets, no name-keyed table, match exact tokens not substrings; provider shape diffs normalized at the edge in lib/, never branched in a renderer; path headlines elide MIDDLE, commands/patterns elide at END. Full detail: tool-presentation-contract.md, todo-fold-contract.md.
- (constraint) WRITE half: steering acts on the live agent never the node lifecycle (node stays running; no durable steering state — no marker/phase/CAS/attempt-key); interrupt is a per-turn signal via AbortSignal.any, NEVER the one-shot nodeAbortController (Cancel's, :3124/:3032) — operatorInterrupt is a per-turn flag resolved by PLACEMENT (canReask also stops on it, validation skipped on interrupted turn, branch after :3124 so Cancel dominates, reset at turn N+1, result-at-:2533 discriminator); a steered node runs MULTIPLE turns on one session (the one new engine behaviour) reusing attemptResumeId (:2290); turn-end has two causes (natural=auto-drain; interrupted=always idle-await, drains only on Send now); in-process registry only (detached deferred); operator row = ordinary text row + 3 additive metadata fields (origin='operator', operator_user_id, message_id), no table/no widened kind, executor sole writer; Send/Interrupt new routes (resolveAuthContext under HITL/AD-7), queue absorbs races, only a finished node refuses (409); dock absent on finished node (undelivered draft stays read-only 'Never sent'); interrupt is NOT undo; delivery emits no turn-start; confirmation by id never text/timestamp; universal over a queue-and-flush floor, mid-turn is the acceleration. Full detail: engine-integration.md, provider-steering-matrix.md, control-states.md.
- (constraint) 30-min idle-await fail (owner-ratified): while idle-after-interrupt and nothing sent, a FRESH 30-min timer (explicit fail branch, NOT the existing idle-timeout which completes) fails the node ('interrupted by operator, no redirect received'); idle-await runs its own timer-driven status poll to still honour /workflow cancel; resolves exactly once (Send now / cancel-poll / timer). The timer is an INACTIVITY timer (SC 2.2.1): a debounced authorized composing keepalive re-arms it without resolving idle-await -> fails only after 30 min of genuine inactivity; 30-min value unchanged. Resume = fresh session (interrupted context gone).
- (decision) CROSS-REF RECONCILED: the two old specs referenced each other (steering CAP-4 'cross-spec dependency' on the transcript's AgentHistoryItem kinds; read Non-goal excluded 'mid-turn steering' as Track B). Merged -> now ONE spec, so this becomes an INTERNAL dependency: CAP-11 (operator row) adds a new AgentHistoryItem kind that the CAP-1/CAP-2 transcript renderer must recognize; an operator text row must not reach a live transcript until the reader recognizes origin='operator' (else renders as agent text). The read Non-goal 'Track B mid-turn steering' DISSOLVES (steering is now the write half); agent thinking / triggering prompt / advisor notifications REMAIN non-goals. The write Non-goal 'the read-only transcript itself' DISSOLVES (it is now the read half).
- (decision) NON-GOALS (unified): Cancel node (exists, untouched); cancelling one individual tool call (no provider offers it); surviving server restart mid-steer + steering a detached run (in-process only v1, deferred); persisting the draft queue; auto-send queue mode (every send operator-initiated); RunStream ToolCallItem.tsx + Chat ToolCallCard.tsx + backend tool-formatter (read half touches only the two node rooms); run-level 'Files changed' panel + node-level git attribution; qodercli/pi/copilot/opencode steering (not in use); agent thinking / triggering prompt / advisor notifications (Track B beyond this).
- (assumption) Both node rooms (Legacy + Console) ship together (owner-confirmed, re-confirmed) though Legacy is scheduled for deletion — render fork = second JSX pass + second renderer test suite; shared logic in lib/ written once. Alias sets absorb provider NAMING drift only (not semantic drift — normalizers handle that). Production corpus representative: 22,867 tool rows / 31 runs / 2,369 tool names, measured read-only.
- (question) Settled: 30-min inactivity fail (see constraint). Deferred: soft-inject to a detached run (needs a channel into a session not spawned here — grok leader socket the only lead); delivered chip (needs SDK >=0.3.246, pin is 0.3.209). Unverified spikes: claude AsyncIterable streaming input composing with resume protocol (soft-inject); grok hooks text-field payload shape. Resolved during read spec: Agent SDK schemas match harness; glob earns own family (Claude pattern vs OMP path); grep body arm from output_mode not family; Claude TodoWrite/Agent normalize at edge.
- (decision) STRUCTURE: companions = 6 spec-authored COPIED into this folder (tool-presentation-contract, todo-fold-contract, test-plan | engine-integration, provider-steering-matrix, control-states) + adopted by relative path (2 UX spines DESIGN/EXPERIENCE ux-Archon-agent-node-room-2026-09-09; 2 architecture spines architecture-Archon-readable-agent-transcript-2026-09-12 [read/Track A] + architecture-Archon-live-agent-steering-2026-09-12 [write]; the NEW mockup handoff claude-design/design_handoff_node_room_transcript_steering/README.md; evidence: findings.md + 3 plans/reports; project-context.md). sources = sources/spec-readable-agent-transcript/SPEC.md + sources/spec-live-agent-steering/SPEC.md (fully absorbed). stories.yaml NOT copied — bmad-create-epics-and-stories will (re)derive story breakdown against the unified CAP-1..13.
- (note) memlog seeded for derive; SPEC.md content preserved verbatim-in-meaning from the two sources listed in sources: — read them on re-derive for detailed prose.
- (event) Self-Validate Pass 1 (Coherence, Spec Law 1-6/8): PASS. 13/13 caps have intent+success; intents are WHAT; all constraints bend a decision; non-goals explicit (8); success signal concrete+testable (historical: scannable list no-JSON, failed bash visible w/o click; live: interrupt->continue same session, transcript order); CAP ids unique 1-13, no reuse.
- (event) Self-Validate Pass 2 (Preservation): PASS. Every load-bearing claim from both source SPECs landed — read CAP-1..7 -> CAP-1..7, write CAP-1..6 -> CAP-8..13; all constraints preserved (grouped Shared/Read/Write); non-goals reconciled; the two specs' mutual cross-references internalized as one Cross-half dependency (CAP-11 operator row -> read renderer recognizes origin='operator' first); assumptions + open questions carried. 16 companion paths verified resolvable. No load-bearing drops. Wrapper-only: each spec's 'the other half is a non-goal' line dissolved by design (documented).
- (event) spec merged + finalized: spec-agent-node-room, 13 caps, 6 spec-authored companions + 10 adopted, 2 sources. Old folders spec-readable-agent-transcript + spec-live-agent-steering kept as sources (audit); superseded for downstream by this unified spec. stories.yaml NOT carried — bmad-create-epics-and-stories re-derives against CAP-1..13.
- (note) next: bmad-create-epics-and-stories on spec-agent-node-room (Step 2) — names the epic, references the mockup handoff, slices stories so read (retroactive) can ship ahead of write (live-session).
- (note) Post-merge fix (advisor re-check): the 3 write companions (engine-integration, provider-steering-matrix, control-states) were copied verbatim but still cited pre-merge steering CAP ids; remapped 17 refs 1..6->8..13 so CAP-N resolves to the correct merged capability (was silently resolving to read-half caps).
- (decision) Added a canonical-contract preamble caveat: adopted steering artifacts (the steering ARCHITECTURE-SPINE.md binds:/AD Binds:/capability-map and EXPERIENCE.md 'Live steering' refs) keep pre-merge ids; read them via +7 mapping (steering CAP-1->8 .. CAP-6->13). A bmad-architecture/bmad-ux Update can renumber them natively later; until then the mapping is authoritative. bmad-spec does not edit adopted companions.
- (note) Added SUPERSEDED banners to the two source SPEC.md (spec-readable-agent-transcript, spec-live-agent-steering) pointing at spec-agent-node-room, with the CAP mapping, so no one builds from the retired specs. Safe hand-edit: they are retired sources, never re-derived.
- (question) UX-DR9 (pinned todo strip, a UX layer on CAP-3) and UX-DR10 (loop iteration selector, a UX layer on CAP-6) are design-session additions from the mockup with no spec CAP home. Decide with Kevin: in scope (promote to a new CAP each / fold into CAP-3 & CAP-6 success) or non-goal. Epics carry them flagged 'confirm scope before building' (Stories 1.4, 1.6).
- (decision) RESOLVED (Kevin, 2026-09-13): UX-DR9 + UX-DR10 are IN SCOPE. Folded into existing success rather than new CAP ids (they are UX layers on CAP-3/CAP-6, and Spec Law keeps 1-13 stable). CAP-3 success gains: a pinned todo strip at the top of the transcript mirroring TodoPhase[], visible while scrolling, absent when no todos. CAP-6 success gains: a loop-iteration selector to navigate between occurrence groups (headers stay the anchors), absent on single-occurrence. Supersedes the prior open question.
- (event) Self-Validate after UX-DR9/10 scope-in: Pass 1 (Coherence) PASS — CAP-3 & CAP-6 keep intent+success; intents unchanged and still WHAT (see current todo state / tell occurrences apart); added success clauses (pinned strip, loop-iteration selector) are concrete+testable at capability altitude, no HOW-prescription; ids stable 1-13. Pass 2 (Preservation) PASS — both mockup claims now land in CAP-3/CAP-6 success (were the two flagged UX-DRs); no load-bearing drop; companions (EXPERIENCE.md, mockup handoff) already carry the detail.
