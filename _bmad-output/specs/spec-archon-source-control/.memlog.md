---
topic: Archon UI: Source Control tab on the workflow-run screen — inspect the run's remote worktree (changed M/A files, content, commit history) via server-resolved read-only git, no local clone
updated: 2026-09-05T22:04
---

- (decision) Source: brainstorm-intent.md (fully absorbed); rich input, direct extract, no elicitation
- (decision) Artifact language English per config document_output_language; source intent was Vietnamese (assumption logged, flagged to user)
- (capability) CAP-1 Source Control tab: fourth tab beside Graph/Logs/Chat; uncommitted Changes region above commit-history region (VS Code SCM style) for the current run
- (capability) CAP-2 Changed-file listing tagged only M(modified)/A(added), for Now and per selected commit, matching that worktree
- (capability) CAP-3 Status-keyed diff-only viewer: M=two-pane diff (red before/green after), A=single-pane content no color
- (capability) CAP-4 Any commit in history opens its M/A files read via git -C working_path (run branch), not base dev
- (capability) CAP-5 Server-resolved read-only access pinned to runId (working_path+codebase_id), realpath, reject .., execFileAsync/@archon/git; UI never supplies a path
- (capability) CAP-6 Graceful absence: null working_path / env destroyed / missing dir -> explicit worktree-gone empty state, never crash
- (capability) CAP-7 Every file opens: large text streams in chunks (Load more)/M sends hunks+context; binary renders image or offers download/hex; skeleton+Cancel
- (capability) CAP-8 SHOULD durable git-snapshot artifact under output_root at run-end/per-commit checkpoints; live worktree fallback while it exists
- (constraint) Read from run worktree via git -C working_path, never base default_cwd (on dev); run branch invisible at base unless merged [brownfield.md]
- (constraint) Pin to run.working_path+codebase_id from GET /api/workflows/runs/{runId}; realpath (tree under two roots); handle null working_path (folder/--no-worktree) [brownfield.md]
- (constraint) New git-read API only; none exists; model on GET /api/artifacts/:runId/* + resolveRunArtifactDir; @archon/git has only boolean hasUncommittedChanges [brownfield.md]
- (constraint) Security: resolve path server-side from runId, never accept working_path from UI, execFileAsync/@archon/git no shell-string [brownfield.md]
- (constraint) Worktree may vanish mid-view (conversation/PR close, scheduler merged-6h/stale-14d, manual, codebase delete, orphan); terminal status != removed; check env active|destroyed + existence before read [brownfield.md]
- (constraint) Post-cleanup GC: after branch -D + checkout removal, unmerged run commits may be unreachable -> durable history needs capture-before-teardown (CAP-8) [brownfield.md]
- (constraint) Viewer/diff rules fixed: Now=HEAD->worktree, commit=parent->commit; open-strategy per status [viewer-rules.md]
- (constraint) Follow Archon package + coding rules: execFileAsync, registerOpenApiRoute, @archon/web import boundary, no SDK leakage [adopted project-context.md]
- (decision) Non-goals (WON'T): no standalone Snapshot mode for M; no full Explorer tree of unchanged files; no edit/commit from UI; no auto-refresh/polling; no authoritative change-view from events; not an embedded IDE
- (assumption) SPEC authored in English per config though brainstorm ran in Vietnamese
- (question) Worktree directory existence on the remote host was never stat'd (no fs endpoint/SSH) - confirm at implementation
- (question) CAP-8 snapshot trigger/format (run-end only vs per-commit; what it stores) not finalized
- (question) Large-file chunk size and binary hex-peek thresholds unspecified
- (note) companions: brownfield.md, viewer-rules.md, architecture-diagrams.md, adopted ../../../archon/_bmad-output/project-context.md
- (event) Self-validate Pass 1 (coherence) PASS: all CAP-1..8 have intent+success; CAP-5 intent trimmed to WHAT (execFileAsync moved to Constraints/brownfield); all constraints bend decisions; 6 non-goals; success signal concrete against the speckit-no-hitl-feature run; IDs unique; lean
- (event) Self-validate Pass 2 (preservation) PASS after adding roadmap.md: MUST->CAP-1/2/3/5/6/7+constraints, SHOULD->CAP-8, COULD->roadmap.md, WON'T->non-goals, evidence->brownfield.md, viewer+diff+open-strategy->viewer-rules.md, diagrams->architecture-diagrams.md, Archon rules->adopted project-context.md; only brainstorm ceremony (technique names) dropped as wrapper-only
- (event) Spec finalized: 8 capabilities, 4 spec-authored companions + 1 adopted, 1 source absorbed
- (note) Configured persistent_fact file:{project-root}/project-context.md is MISSING at repo root; not loaded. Actual Archon context read as ancillary: archon/AGENTS.md + archon/_bmad-output/project-context.md (adopted companion)
- (decision) Correction: working_path is NOT null for folder/--no-worktree; executeWorkflow writes working_path=cwd (executor.ts:1398-1404), folder in-place at workingCwd (workflow.ts:1970-1977), --no-worktree at cwd (2088-2089). Distinguish isolated worktree vs in-place/non-git by codebase kind/isolation provider, never by null; null = missing/legacy -> CAP-6 empty state
- (decision) v1 scope decision: Source Control git read presupposes a git worktree (isolated OR --no-worktree repo run); non-git folder projects render CAP-6 empty state (new non-goal)
- (decision) Spec Law rule-2 fixes: moved HOW out of CAP-4 (git -C), CAP-7 (chunk/hunk/hex/skeleton/Cancel), CAP-8 (checkpoint/output_root) intents into constraints/companions; intents now outcome-only
- (decision) Contradiction fixes: CAP-1 success now defers to CAP-6 when no worktree; CAP-8 truth model corrected (live worktree primary, durable capture fallback); CAP-2 success reframed as M/A projection with D/R/C/U/T open question; brownfield softened (terminal!=removed is code-only; two path roots are strings, symlink unconfirmed)
- (event) SUPERSEDING re-validation after corrections: Pass 1 (coherence) PASS - CAP intents outcome-only, no CAP-1/CAP-6 contradiction, CAP-8 direction correct; Pass 2 (preservation) PASS - all load-bearing claims retained, overclaims removed, gaps moved to open questions. Supersedes the earlier PASS/finalized events which were premature
- (decision) CAP-5 intent rewritten to outcome-only (read-only, run-confined, no path-injection); mechanics (runId resolve, realpath, reject ..) remain in Constraints/brownfield
- (decision) Reclassified --no-worktree/folder handling from a non-goal to an OPEN QUESTION: it is an unsourced scope decision, not user-approved; v1 as written targets the isolated-worktree case
- (note) architecture-diagrams.md env-active check represents the isolated-worktree v1 flow; non-isolated runs are the scope open question (classify by kind/git-checkout first if adopted)
- (event) Superseding verdict: earlier 'all intents outcome-only' was inaccurate (CAP-5 retained HOW); corrected. Coherence + preservation PASS after this fix; open questions now 5
- (decision) User simplification (scoped to host checkouts): no worktree-vs-non-worktree branching, no isolation-env gate; every host-checkout run read the same way by working_path; empty state on null/missing/non-git-checkout detected at read time
- (decision) Container-backend exception: folder-container runs keep the same host working_path but changes live in an overlay volume (host path stale until write-back, container.ts:4-16/types.ts:487-501); added as OPEN QUESTION (v1 unsupported/empty vs read overlay summary), not silently treated as truth
- (decision) Q1 non-isolated-scope DISSOLVED for host checkouts; OQ1 existence reclassified to implementation validation task in brownfield; open questions now 4: CAP-8 trigger/format, large/binary thresholds, D/R/C/U/T behavior, container-overlay handling
- (event) Re-validation after simplification + container caveat: coherence + preservation PASS; diagram env-check replaced by working_path-exists+git-checkout; supersedes prior verdicts
- (decision) User approved (a): container-backend runs unsupported in v1 -> CAP-6 empty state; reading-in-container (docker exec / overlay diff walk, handling suspended/finalized) deferred post-v1. Container open question resolved into a non-goal + roadmap entry
- (event) Re-validation after container decision: coherence + preservation PASS; open questions now 3 (CAP-8 trigger/format, large/binary thresholds, D/R/C/U/T); supersedes prior verdicts
- (decision) User approved (b): add D (deleted) badge -> states are M/A/D. Projection: rename -> D(old)+A(new), copy -> A, type-change -> M, unmerged -> M. D viewer = single-pane content of the removed file, no color. D/R/C/U/T open question resolved; open questions now 2 (CAP-8 trigger/format, large/binary thresholds)
- (event) Re-validation after D-status decision: coherence + preservation PASS; supersedes prior verdicts
- (capability) CAP-2 SUPERSEDED: file states are M/A/D (was M/A); projection rename->D+A, copy->A, type-change->M, unmerged->M
- (capability) CAP-3 SUPERSEDED: viewer adds D mode = single-pane removed-file content (M=two-pane diff, A=single-pane new content)
- (capability) CAP-4 SUPERSEDED: commit history file list is M/A/D
- (decision) User approved (a) for CAP-8: trigger = single run-end snapshot; stores name-status(M/A/D)+per-file diff+A/D content+git log under output_root; wire format (JSON manifest) decided at build serving the same read API; per-commit deferred post-v1. CAP-8 open question resolved; open questions now 1 (large/binary thresholds)
- (event) Fixed stale M/A-only text (SPEC success signal, brownfield status+show/diff endpoints) to M/A/D; re-validation coherence+preservation PASS; supersedes prior verdicts
- (decision) User approved (a) for thresholds (defaults, build-tunable), recorded in viewer-rules.md: large text first paint ~256KB/~2000 lines then Load more; M diff = hunks + 3 lines context; binary = NUL in first 8KB; images inline, else download + 4KB hex peek; >1MB streams with Cancel, >50MB download-only
- (event) All open questions resolved (0 remaining). Spec finalized: 8 capabilities, non-goals incl container v1-exclusion, 4 spec-authored companions + 1 adopted; states M/A/D; read-by-working_path host-checkout model; CAP-8 run-end snapshot. Coherence + preservation PASS; supersedes all prior verdicts
- (event) Fixed last stale M/A text in architecture-diagrams.md tab-layout (now M/A/D + D single-pane removed content). Broadened whitespace/backtick M/A search: derived artifacts (SPEC + 4 companions) clean; remaining matches are append-only memlog history, correctly superseded. Final coherence + preservation PASS
- (event) architecture-diagrams.md updated from downstream PRD review: resolution diagram adds container-backend -> CAP-6 empty-state branch before realpath/git (git-backed container was routing to host git); truth-model snapshot node fixed to run-end capture (per-commit post-v1), matching the locked CAP-8 decision
- (event) architecture-diagrams.md container branch + run-end snapshot fix confirmed
- (event) Restored spec folder into archon/_bmad-output/specs/spec-archon-source-control from parent workflow-engine/_bmad-output copy (memlog + companions); CAP-1..CAP-8 IDs preserved
- (decision) Adopt architecture spine architecture-Archon-source-control-2026-09-05 as companion; AD-1..AD-9 stable; bmad-spec does not edit the spine
- (constraint) v1 surface is legacy /legacy/workflows/runs/:id only; console not v1; HITL viewer reuse remains COULD
- (constraint) Missing conversation.isolation_env_id is not CAP-6; fall through to host dir+git availability (architecture AD-6)
- (constraint) Git routes inherit run-detail/artifact auth; no requireWebUser or per-run owner ACL in v1
- (constraint) CAP-6 CTA: container has no Reload; no_checkout has Reload. Region empty Changes/History are not CAP-6
- (assumption) ux-Archon-source-control-2026-09-05 DESIGN/EXPERIENCE missing on disk; quiet-copy and 30/70 layout carried by architecture AD-5/AD-6/AD-9 + viewer-rules.md
- (event) brownfield: dropped kind/provider branching; host reads uniform by working_path after container gate; commit reads cite AD-1 ls-tree+cat-file
- (event) Self-validate Pass 1 (coherence) PASS: CAP-1..8 keep intent+success; IDs stable; CAP-6 success names distinct empty reasons vs region empties; constraints still bend; non-goals include console and secret redaction; success signal unchanged
- (event) Self-validate Pass 2 (preservation) PASS: kernel WHAT retained; HOW stays in brownfield/viewer-rules/diagrams/roadmap + adopted architecture spine; wrapper-only: parent-folder restore ceremony
